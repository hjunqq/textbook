O1 第6章五维模型学术归属修正

- 工作包：O1；日期：2026-08-06。
- 修改 `output/chapters/chapter06.tex`：保留 `glaessgen2012` 关于物理模型、传感器更新和运行历史综合映射的原有转述；将四维数字孪生车间模型归于陶飞、张萌，将五维模型归于陶飞等人的 2019 年论文，并将符号统一为 `$M_{DT}=(PE,VE,Ss,DD,CN)$`。正文明确说明本章沿用该五维划分并结合水利业务作工程化细化。
- 修改 `output/references.bib`：新增 `tao2019fivedim`，记录陶飞、刘蔚然、张萌等人的《数字孪生五维模型及十大领域应用》（《计算机集成制造系统》, 2019, 25(1): 1--18, DOI: 10.13196/j.cims.2019.01.001），并在第6章正文中引用。
- 素材回收：本包不回收 L1/L2 段落；仅依据第五轮方案 O1 与 R4-001 修正学术归属。未删除正文内容，未修改门禁阈值或其他章节文件。
- 字数：以门禁脚本实际报告为准；第6章正文汉字数不得低于基线，本次为扩写式改写。

O2 第6章IFC4.3段落重写（+1013 字）

- 工作包：O2；日期：2026-08-06。
- 素材回收：检查了 L1 `d8beab0:output/chapters/chapter06.tex` 的旧 IFC 段落；其中 `IfcStructuralMember`、`IfcDoor` 等水工对象映射未经 IFC 4.3.2 官方范围支持，本包未回收这些错误示例。
- 修改 `output/chapters/chapter06.tex`：重写 Ports and Waterways Domain 的范围和 out-of-scope 边界；新增表\ref{tab:ifc-waterworks-mapping}，覆盖通航渠道、船闸（闸室与闸首）、升船机、护岸、防波堤、滨海与洪泛防护设施、坝体、溢洪道、泄洪闸门、廊道、监测仪器及项目土方填筑；对无原生实体的对象统一说明项目分类、`IfcPropertySet` 属性集、映射规则和原始设计模型的保留策略。
- 技术核对：仅采用已对照 IFC 4.3.2 文档的 `IfcMarineFacility`、`IfcMarinePart`、`IfcEarthworksFill` 及所列 `PredefinedType` 值；明确 `CHAMBER`/`GATEHEAD` 只在船闸语境使用；保留三条错误映射禁令并补充 `IfcStructuralMember` 属于结构分析模型实体的原因。
- 习题：第6章第 763 行题干无需调整，改写后的范围说明和映射表已经能够支持闸门、坝体、廊道三类对象的作答。
- 删除：无正文删除；未修改门禁文件。

O3 第8章质量码与预警级代码修复（+689 字）

- 工作包：O3；日期：2026-08-06。
- 修改 `output/chapters/chapter08.tex`：将 `WarningLevel` 补为 `NONE, BLUE, YELLOW, ORANGE, RED`；用显式 `Classification(evaluable, level, reason)` 区分“未评估”和“无预警”；对 `inspect()` 补齐格式、单位、范围、时序和重复五项校验。
- 质量语义：缺测写入 `missing`，单位、时序和重复问题写入 `suspect`，范围和格式问题记录内部 `invalid` 结论；按 `missing > invalid > suspect > valid` 合并多问题，最终对外仍保持第7章一致的 `valid/suspect/missing` 三值质量码。
- 代码完整性：补充 Java 17 所需 import、记录类型、校验策略接口、构造器依赖注入、去重集合、分类结果和 JUnit 语义测试；`errorCount` 与 `corrected` 旧命名已由问题列表和 `qualityFlagAdjusted` 取代，`range.isClearlyInvalid()` 改为 `range.isInvalid()`。
- 表格与口径：新增表\ref{tab:quality-code-downstream}，统一质量码含义、打分、界面表现和统计处理；散文、代码和实践题统一为格式、单位、范围、时序、重复五项。
- 替换说明：删除原有不完整的 `errorCount`/`corrected` 检查片段以及把正常值和不可信值都返回蓝色的四级 `classify()` 片段，替换为完整质量服务、显式分类结果和可执行测试；未删除教材语义内容，未修改门禁文件。
- 行尾噪声：O3 开工时四个指定文件工作区无差异，未制造空的“统一行尾符”提交。

O3补丁 蓝色预警阈值与去重窗口说明（+833 字）

- 工作包：O3 补丁；日期：2026-08-06。
- 修复 `classify()` 的蓝色预警不可达问题：新增 `0.30 <= score < 0.50` 的 `BLUE` 分支，并在正文说明 `$S\ge0.30$` 是“出现需关注变化”的蓝色关注阈值，`$S<0.30$` 才是 `NONE` 无预警；新增单元测试验证蓝色路径。
- 补充去重边界说明：教学用 `ConcurrentHashMap.newKeySet()` 仅演示幂等语义；生产环境应使用 Redis SETNX+TTL 或数据库唯一约束，去重窗口覆盖消息最大重投周期。
- 字数：第8章门禁基线 11,076 → 11,909 字，净增 +833 字；未删除正文，未修改门禁文件。

O4 51WIM致谢商标与权威依据补充

- 工作包：O4；日期：2026-08-06。北京五一视界数字孪生科技股份有限公司的书面授权已落实，授权文件已归档到仓库外项目档案；本包默认不加入产品界面截图，8.5 节继续使用 TikZ 重绘教学示意图。
- 修改 `output/chapters/preface.tex`：致谢中补入公司名称、手册全称及参考资料说明。
- 修改 `output/main.tex` 与 `output/chapters/chapter08.tex`：书名页加入版权与商标声明；8.5 首个脚注加入 51WIM/51WORLD 商标声明；正文改为“本节图示均为按功能链路重新绘制的教学示意图，不复现产品界面”。
- 修改 `output/references.bib`：`wuyi2026wim` 的 note 改为“内部资料，未公开出版”。
- 权威依据：8.5.1 引用 `smartWaterGuidance2021`、`twinWater2023`、`twinProject2024`；8.5.4 引用 `twinYellowRiver2024`；新增“不同技术路线对比”段，使厂商手册退居其中一种实现的参考。
- 字数：前言 1,677 → 1,721 字（+44）；第8章 11,076 → 12,764 字（+1,688）；未删除正文，未修改门禁文件。

O5 清除全书改稿批注21处

- 工作包：O5；日期：2026-08-06。依据 AGENTS.md 第4条、方案 O5 与 R4-011，逐条将写给上一稿作者或审稿人的纠错备忘改为面向学生的正面教学表述。未修改 `tools/check_textbook.py`、`tools/gate_baseline.json` 等门禁文件。
- 逐条修改记录（原文片段 → 改后文字）：
  1. ch01 L44 “不能写成‘洪灾风险从30年一遇降至100年一遇’” → 新增 `warningbox`“常见概念误用：防洪标准与洪灾风险”，解释重现期只描述致灾因子，风险还取决于暴露度、脆弱性、调度能力、预警提前量和极端事件，并给出规范写法。
  2. ch01 L48 “不能与后续智慧水利文件混写为同一政策” → 按发布年份、文件标题和适用范围建立政策时间线，分别说明两个阶段的政策重点。
  3. ch01 L50 “不用无出处的项目数量或成效百分比装饰结论” → 增补预警提前量、数据完整率、模型误差和处置时延等指标的来源、统计周期、样本范围与对照基线要求。
  4. ch01 L73 悬空的 AI 模型选型段 → 增设“智能能力的技术选型与安全边界”小节，承接现场约束、模型许可、算力、数据隔离、版本登记和人工复核责任。
  5. ch02 L138 “不应表述为‘运维人员高度参与’即可代表敏捷” → 明确业务用户和客户代表持续参与评审并拥有优先级、验收决策权，运维人员提供运行约束与故障数据。
  6. ch02 L256 “而不是逆转告警处置顺序” → 改为描述资料退回、修订、审批、生成执行任务和接收回执的可审计业务顺序。
  7. ch02 L357 “不应写成‘计划编号（计划编号）’之类的同义反复” → 说明字段应给出编码格式、类型、单位、来源、取值域和校验规则，并给出计划编号、状态和流量示例。
  8. ch02 L382 “描述水工设备动作时统一使用‘闸门启闭’” → 将术语落实到接口、日志和界面，并区分目标开度、实际开度、状态和操作指令。
  9. ch03 L897 “本节不再重复绘制全书层级架构” → 说明下面案例如何把模块化、权限和事件驱动原则落实到导航、内容检索、版本发布和接口构件。
  10. ch04 L111 “完整且不存在未闭合的媒体查询或选择器” → 改为引导学生用设备模拟器检查断点前后的卡片顺序、触控区域和滚动方向。
  11. ch04 L249 “以下单文件组件没有省略语法结构” → 说明示例同时展示状态声明、属性校验、模板绑定和事件发射，便于理解组件边界。
  12. ch05 L90 “不再使用‘Servlet 4.0是现代规范’的过时表述” → 保留 Spring Boot 3、`jakarta.servlet.*` 与 Jakarta Servlet 6.x 基线，并补充迁移后的编译、集成测试和容器启动检查。
  13. ch05 L334 `@EnableEurekaClient` 弃用备忘 → 删除脱离上下文的注解说明，改写服务发现、配置中心、网关职责及健康检查边界。
  14. ch06 L190 “不再混用 PhotoScan、Smart 3D 等旧称” → 介绍 Metashape 与 PhotoScan、ContextCapture 与 Smart 3D 的名称沿革，要求项目文档记录版本并统一使用现行名称。
  15. ch07 L125 “而不是‘百度开源项目’的现行表述” → 补充 ECharts 由百度开源、2018 年捐赠 ASF、2021 年毕业为顶级项目、现由 ASF 社区维护的完整沿革。
  16. ch07 L287 “不能调用未定义的全局变量” → 说明构造器保存场景引用、方法通过显式依赖访问对象，有利于测试和复用。
  17. ch08 L128 “不再混用 JavaScript 后端” → 直陈 Java 17/Spring Boot 3.2 服务端与 Vue 3.4 前端的统一示例栈。
  18. ch08 L128 “数据库不再在 PostgreSQL 与 MySQL 之间切换” → 直陈 PostgreSQL、PostGIS、TimescaleDB 和 Redis 各自的数据职责及技术替换的决策记录要求。
  19. ch08 L251 “该方案避免了原稿对 InfluxDB 引擎结构的不严谨描述” → 改为业务、空间与时序数据共用事务和备份体系，说明统一架构的追溯与运维收益。
  20. ch08 L259 “不再混用五级颜色、0–3 数字和 none 字符串” → 复核并保持 O3 的正面口径：蓝、黄、橙、红四级预警加 `NONE` 无预警，质量不合格时进入“未评估”。
  21. ch09 L32 “本节不再堆叠难以逐条核实的项目数字” → 从《数字中国建设整体布局规划》起笔，按政策、工程、平台技术和人才能力四条线索归纳趋势，并要求注明来源、口径与适用范围。
- 素材回收与新增：本包未从 L1/L2 回收整段旧稿；新增的概念解释、政策证据要求、AI 选型小节、服务发现边界和趋势分析均为围绕现有正文的扩写。删除 ch05 的孤立 `@EnableEurekaClient` 备忘后，以服务发现与配置中心的教学段落等量替代。
- 字数与门禁：正文总字数由 73,728 增至 77,327（+3,599）；各章均不低于基线。`check_editorial` 从 15 处命中降为 0，软指标无倒退；未删除门禁规则。

O6 全书补ref与浮动体控制

- 工作包：O6；日期：2026-08-06。开工前核对 AGENTS.md 第3条、方案 O6 与 R4-007；并抽查 L1 `d8beab0:output/chapters/chapter01.tex`、`chapter08.tex` 的旧图表组织，未回收旧段落，沿用当前版本的概念、图表和代码内容，仅补充随文导读与标注。
- 图表与公式引用：在第1、2、3、4、6、7、8、9章正文补入逐项导读，覆盖原有 112 个 `\label`，将 86 个未引用 label 清零；第1章洪水闭环标签由 `fig:smart-water-evolution` 更正为 `fig:flood-warning-loop`，与图题一致。每行控制为 1—3 个引用，避免集中式引用段落。
- 浮动体控制：`output/main.tex` 增加 `\usepackage[section]{placeins}`；全书 48 个 TikZ `figure` 环境改为 `[H]`，使示意图在所属小节就地排版并以章节边界释放浮动队列。
- 代码清单：为第3—8章共 44 个 `lstlisting` 补充中文 `caption` 与 `lst:chNN-...` 标识，正文加入代码清单导读，引用超过 20 个清单；代码保持完整可读，未压缩或删行。
- 技术一致性：未引入新的技术栈或工程参数；仅使用现有 Vue 3.4、Java 17、Spring Boot 3.2、Three.js、Cesium、PostgreSQL/PostGIS/TimescaleDB、Redis 与 Kafka 基线。
- 字数与门禁：正文总字数 73,728 → 78,950（+5,222）；各文件均高于基线。`check_refs` 待补引用 86 → 0，参考文献 51/51；完整 `--build` 通过，编译输出 156 页，软指标无倒退。未修改门禁文件。

O7 前言承诺修正与配套资源节

- 工作包：O7；日期：2026-08-06。开工前完整阅读 AGENTS.md、第五轮方案 O7 与 R4-004；检查 L1 `d8beab0:output/chapters/preface.tex`、`chapter09.tex`，未发现需要回收的整段内容，本包采用正面扩写与口径同步。
- 承诺修正：`output/chapters/preface.tex` 将后端主线改为 Java 17/Jakarta EE、Spring Boot、JPA、Spring Security 和消息队列，Python Flask/Django/FastAPI 仅列为扩展阅读；`chapter09.tex` 的全书回顾同步该口径。原有“每章末尾都有思考题和练习题”句子保持不动，待 A9-1 补齐第9章章末要件。
- 学时方案：32学时限定为第1--5章及第6章6.1、6.2概念，48学时限定为第1--7章及第8章8.1--8.4；新增 `tab:preface-32-hours`、`tab:preface-48-hours` 两张章节学时分配表，并在方案说明中用 `\ref` 引出。
- 配套资源：新增“配套资源”节，约定 `companion/water-platform-demo/` 代码仓库、`companion/datasets/` 示例数据集、附录A答案获取方式、Issues勘误渠道和版本化环境基线（Node.js 20.15+、JDK 17、Python 3.11+、Vue 3.4+、Vite 5+、Spring Boot 3.2+、Three.js r160+、CesiumJS 1.12x、PostgreSQL 16/PostGIS 3.4/TimescaleDB 2.14、Redis 7.2、Kafka 3.7）。新增 `tab:preface-companion-resources` 并用 `\ref` 引出；资源文件由后续 S1/S2/S3 工作包建立，本包只写结构契约。
- 体例与致谢：三部分介绍统一使用中性的 `tipbox`；致谢保留 O4 的 51WIM 资料致谢，并补充 `TODO-补充：基金或教改项目名称与编号`，未重复 O4 内容。
- 字数：`preface.tex` 1,721 → 2,975 字（+1,254）；`chapter09.tex` 3,298 → 3,331 字（+33）。全书门禁报告当前正文 87,219 字，各文件均不低于基线；未删除正文，未修改 `tools/check_textbook.py` 或门禁基线文件。
- 自检：`python tools/check_textbook.py --build` 编译输出 166 页并通过，引用、文献和软指标均无倒退。

O7补丁 清除内部术语与\url误用

- 工作包：O7 补丁；日期：2026-08-06。核对前言新增资源节后，将面向编者的“本包”“S1/S2/S3 工作包”“由S1工作包建立”等内部流程术语改为面向读者的发布说明；保留资源目录、答案获取和勘误反馈的教学信息。
- 路径排版：将资源表中的两个相对目录从 `\url{...}` 改为可断行的 `\texttt{}` 组合（目录本身不含下划线，无需额外转义），避免相对路径被排成网址链接样式；真正的仓库网址待发布后再补充。
- 全书核对：对 `output/chapters/*.tex` 检索“本包”“工作包”“第五轮”“O[0-9]”“A[0-9]-[0-9]”。未发现内部工作包术语残留；命中的 `ISO12207`、`ISO42010`、`ISO25010`、`tao2017dts` 等均为标准号或文献键，不属于内部流程标记。
- 字数：`preface.tex` 2,975 → 2,998 字（+23），不低于补丁前值；未删除正文，未修改门禁文件。
- 自检：`python tools/check_textbook.py --build` 编译输出 166 页并通过；引用、文献、软指标无倒退。

O8 事实核验点标注（10处）

- 工作包：O8；日期：2026-08-06。依据 O8 要求只插入 LaTeX 注释，不改写任何正文；本包不回收 L1/L2 内容，也不修改门禁脚本或门禁基线。
- 在 `preface.tex`、`chapter01.tex`、`chapter02.tex`、`chapter06.tex`、`chapter09.tex` 的对应政策、标准、产品沿革和测量表述前，插入统一格式 `% TODO-核对[O8-编号]：…… —— 待编者确认后修改，勿自行改写` 注释，共 10 处，编号 O8-01 至 O8-10。
- 新建 `待核事实清单-第五轮.md`，按“编号 / 位置 / 待核内容 / 建议核对来源 / 结论”列出 10 项；结论栏全部留空，交编者回原文核对后填写。
- 核验点覆盖：《国家水网建设规划纲要》措辞、七大江河数量、智慧水利政策条目拆分、SL/T 651—2014、GB/T 9385-2008 与 IEEE 标准关系、LoRaWAN 版本、规划更新频率、Bentley 产品命名及高程异常量级与似大地水准面模型。
- 字数：各章正文汉字数与 O7 补丁前完全相等；全书门禁报告正文 87,242 字，未因注释变化而增减。
- 自检：`python tools/check_textbook.py --build` 编译输出 166 页并通过，引用、文献和软指标无倒退。

A9-1 第9章补齐章末要件与重写两节

- 工作包：A9-1；日期：2026-08-07。开工前完整阅读 AGENTS.md 和 A9-1 要求，并检查 L1 `d8beab0:output/chapters/chapter09.tex`。L1 中的旧十道题、致谢和“截至2026年4月”的时点材料按方案判定为不回收；本包以现有正文为基础重写和扩写。
- 章末要件：新增学习目标、`小结`、5 道综合性思考题与练习、`章末交付物：课程总结报告`，补充证据索引、复现流程、评分维度和答辩演示要求；门禁缺失章末要件 3 → 0。
- §9.1：把逐章目录复述改为能力回顾，统一使用第1—9章阿拉伯数字，明确已经具备的业务、数据、架构、前后端、三维和运行能力，以及压测、密钥轮换、模型校准和跨部门授权等待后续验证的边界。
- §9.2.2：标题改为“感知网络与低功耗广域接入”，补充云边端协同详见6.4.13节的边界说明，并将规划文件检索频次改为“发布频次不固定，建议按季度检索”。
- §9.2.3：重写为“如何判断一项新技术是否值得投入”，加入标准、开源实现、可复现基准、迁移成本、试点成功条件和退出条件等可操作判据。
- §9.3.2：改为能力—问题—技术三列表ef{tab:ch09-technology-paths}，覆盖仿真提速、时序预测、实时事件处理、空间时序存储和容器化运维；移除 Docker Swarm，技术建议遵循 Java 17、Spring Boot 3.2、PostgreSQL/PostGIS/TimescaleDB、Redis、Kafka 等全书基线。
- 体例与表述：将4个 `\subsection*` 改为编号 `\subsection`，将8处手工小标题改为 `\paragraph`；补充《水利学报》《水科学进展》及 Water Resources Research、Journal of Hydrology、Environmental Modelling \& Software 等水利水文期刊；将指数增长、高速发展阶段等口号改为可测的工程约束或岗位能力表述。
- 字数：`chapter09.tex` 基线 3,177 → 5,502 字（+2,325），相对 O8 提交前 3,331 → 5,502 字（+2,171）；全书门禁报告正文 89,413 字，各文件不低于基线。
- 自检：`python tools/check_textbook.py --build` 编译输出 170 页并通过；新增表格已用 `\ref` 就近引出，引用、文献和软指标无倒退。

 A8-1 数据库设计与 DDL

- 工作包：A8-1；日期：2026-08-07。开工前完整阅读 AGENTS.md，并检查 L2 `docs/chapters/chapter08/section08-02.md`（地形与水工对象数据）和 `section08-03.md`（数据接入、质量控制、时序存储与缓存）中的可回收内容。L2 旧稿的 InfluxDB、Python 客户端和过时实现没有原样搬入；本包回收其时序数据特性、数据生命周期和缓存/聚合教学要点，统一更新为 PostgreSQL + PostGIS + TimescaleDB + Redis + Kafka 基线。
- 修改 `output/main.tex`：在导言区新增 `SQL` listings 语言定义和别名，供第8章 DDL 清单使用；未修改门禁脚本。
- 修改 `output/chapters/chapter08.tex`：新增“关系模型、空间索引与时序分区”“数据库迁移、查询与运行维护练习”“从业务约束到物理设计的推导”三部分。正文从 11,076 字增至 26,128 字（净增 +15,052），超过本包 3,500 字最低要求和第8章 26,000 字阶段目标，各文件正文均未低于基线。
- 五张核心表的 DDL：`asset`（`geometry(PointZ,4490)`、GiST 空间索引）、`reading`（TimescaleDB 超表、时间切块与 `asset_id` 哈希分区、质量约束、连续聚合）、`warning`、`work_order`、`model_run`；补齐主键、外键、唯一键、检查约束、状态约束、时间索引，并逐项说明索引选择原因、幂等语义、版本追加、保留与压缩策略。
- 新增表 `tab:ch08-database-dictionary`，按字段/类型/约束/单位/说明给出五张表的最小数据字典；新增 TikZ 图 `fig:ch08-database-er`，展示 asset→reading、asset→warning、warning→work_order 和 asset→model_run 的追溯关系。两者均在正文以 `\ref` 就近引出。
- 新增 27 个可独立阅读的 SQL 清单（基础 DDL、迁移版本、空间查询、时序查询、质量统计、幂等写入、预警/工单队列、连续聚合、保留与压缩、并发更新、健康检查、备份、权限视图、修订审计、容量观察和事件追溯），代码中保留参数占位符、约束和解释性注释，作为 A8-2 实体、S2 代码和 S3 数据集的唯一字段来源。
- 口径修正：将正文中的“事务外盒”改为“事务发件箱”；数据库质量码与第7章统一为 `valid/suspect/missing`，预警等级保持 `NONE/BLUE/YELLOW/ORANGE/RED`，明确“无预警”与“未评估”的差异。
- 删除：无正文删除、无门禁修改；保留工作区中与本包无关的 `Codex提示词-第五轮.md` 修改，不纳入本次提交。
- 自检：`python tools/check_textbook.py --build` 通过，编译输出 186 页；引用、文献和软指标均无倒退。

A8-2 后端实现切片

- 工作包：A8-2；日期：2026-08-07。开工前完整阅读 AGENTS.md，并核对 L2 docs/chapters/chapter08/section08-01.md（平台模块、接口隔离与 RBAC）、section08-03.md（数据接入、实时处理与存储）和 section08-04.md（预警算法与应急流程）。L2 中的 Python、InfluxDB 和过时框架代码没有原样回收；回收的是数据接入、质量处理、预警闭环和分层职责的教学概念，并按 Java 17、Spring Boot 3.2+、Jakarta、Spring Security 6、PostgreSQL/PostGIS/TimescaleDB、Redis、Kafka 基线重写。
- 修改 output/chapters/chapter08.tex：正文门禁字数由 26,128 增至 29,661 字（净增 +3,533），达到本包 ≥3,500 字要求；未删除原有正文。
- 实体实现：新增与 A8-1 DDL 逐字段对应的 AssetEntity、ReadingEntity/复合主键、WarningEntity、WorkOrderEntity、ModelRunEntity，覆盖 PointZ、JSONB、时序复合主键、质量码、预警等级、状态和审计字段。
- 后端链路：新增 Spring Data JPA Repository、事务 Service、Spring MVC Controller、DTO record、Spring Security 6 方法授权、工单创建/完成链和 Kafka 观测事件消费者；消费者以 eventId 唯一约束和事务服务实现幂等去重，并说明重试、死信、事务发件箱和审计边界。
- 接口契约：新增表 tab:monitoring-api-contract，覆盖 GET /assets、GET /assets/{id}/readings?from&to&agg、GET /warnings、POST /warnings/{id}/ack、POST /work-orders 和工单完成接口，列出请求参数、响应、角色和幂等/状态约束；所有新增清单均在正文就近引出。
- 载体：新增 8 个可独立阅读的 Java 清单（实体、Repository、Service、Controller、工单链、Kafka 消费者），第8章当前共 35 个代码清单；代码保留 imports、依赖注入、事务、参数校验和调用方，不压缩为伪代码。
- 新增教学内容：补充 DTO 与实体边界、三层校验、对象级权限、事务生命周期、消息版本冲突、查询性能、测试夹具、可观测性和发布回滚等工程解释，使代码与清源水库监测业务闭环对应。
- 载体与引用（更正）：所有新增代码清单均有中文 caption 和唯一 label，并在正文就近引出；现有图表和软指标无倒退。
- 删除：无正文删除、无门禁修改；保留工作区中与本包无关的 Codex提示词-第五轮.md 修改，不纳入本次提交。
- 自检：`python tools/check_textbook.py --build` 通过，编译输出 202 页；引用、文献、正文基线和软指标均无倒退。

A8-3 前端实现切片

- 工作包：A8-3；日期：2026-08-07。开工前完整阅读 AGENTS.md，并核对 L2 docs/chapters/chapter08/section08-01.md（平台模块与 RBAC）和 section08-02.md（三维场景、组件化和 Cesium/Three.js 片段）。旧稿中的前端概念经过技术基线复核，统一采用 Vue 3.4+、Vite 5、Vue Router 4、Pinia 2、Apache ECharts、Three.js r160+ 和 Cesium 1.12x+，未回收 Vue 2 或过时加载写法。
- 修改 output/chapters/chapter08.tex：正文门禁字数由 29,661 增至 32,777 字（净增 +3,116），达到 A8-3 ≥3,000 字目标，未删除原有正文。
- 新增 6 个前端代码清单：复用第4章 A4-5 request.js 的监测 API 层、Pinia 组合式状态仓库、Vue Router 角色守卫、Three.js/Cesium 可替换的场景定位桥接、完整 Vue 3 单文件组件、Vite 应用入口与场景注入。组件覆盖测点筛选、ECharts 曲线、valid/suspect/missing 显示、空状态、错误处理和三维定位联动。
- 接口对齐：前端 API 与 A8-2 的 GET /assets、GET /assets/{id}/readings?from&to&agg、POST /warnings/{id}/ack、POST /work-orders 契约一致；说明时间窗、质量码、聚合粒度、幂等冲突和 401/403/409 分类处理。
- 新增教学内容：补充 Pinia 状态边界、DTO/接口版本兼容、ECharts 缺测断线、三维对象生命周期、路由恢复、键盘与触控无障碍、性能指标、日志脱敏和前端验收证据链。
- 载体与引用：所有新增代码清单均有中文 caption 和唯一 label，并在正文就近引出；现有图表和软指标无倒退。
- 删除：无正文删除、无门禁修改；保留工作区中与本包无关的 Codex提示词-第五轮.md 修改，不纳入本次提交。
- 自检：`python tools/check_textbook.py --build` 通过，编译输出 210 页；引用、文献、正文基线和软指标均无倒退。

A8-4 部署与运维

- 工作包：A8-4；日期：2026-08-07。开工前完整阅读 AGENTS.md，并回收 L2 `docs/chapters/chapter08/section08-05.md` 的容器化、监控告警、故障恢复和备份演练概念；旧稿中的 Python、旧数据库镜像和不完整 Compose 片段未原样搬入，按 Java 17、Spring Boot 3.2+、PostgreSQL/PostGIS/TimescaleDB、Redis、Kafka 与 Nginx 基线重写。
- 修改 `output/chapters/chapter08.tex`：正文由 32,777 增至 35,729 字（净增 +2,952），达到 A8-4 ≥2,500 字目标；新增部署、配置、探针、告警、容量、备份恢复和故障注入等教学段落，未删除原有正文。
- 新增 5 个可独立阅读的配置清单：Docker Compose（前端、Spring Boot、PostgreSQL/PostGIS/TimescaleDB、Redis、Kafka）、环境变量与密钥、Nginx 反代、监控告警规则、备份恢复演练；每个清单均使用 YAML/Nginx 语言、具有唯一 `label` 并在正文就近 `\ref` 引出。
- 工程边界：说明存活/就绪/业务烟测三层探针、内部数据网络、密钥轮换、版本回滚、指标标签最小化、告警升级、WAL 与对象清单一致性、Kafka `event_id` 幂等重放和恢复后业务烟测；将 Compose 单节点定位为教学基线，生产容量须经压测和演练确认。
- 删除：无正文删除、无门禁修改；保留工作区中与本包无关的 `Codex提示词-第五轮.md` 修改，不纳入本次提交。
- 自检：`python tools/check_textbook.py --build` 通过，编译输出 214 页；正文基线、引用、文献和软指标均无倒退。

A8-5 案例参数补全与公式修正

- 工作包：A8-5；日期：2026-08-07。开工前完整阅读 AGENTS.md，并回收 L2 `docs/chapters/chapter08/section08-01.md` 的平台参数、案例设定和闸门调度概念；所有数值按表8-1统一，未引入第二套清源水库工程参数。
- 修改 `output/chapters/chapter08.tex`：正文由 35,729 增至 38,287 字（净增 +2,558），达到 A8-5 ≥2,500 字目标；各文件正文均未低于基线。
- 参数补全：表 `tab:case-parameters` 新增流域面积 320 km²、总库容 0.85 亿 m³、坝基/坝顶/防浪墙高程、死水位、汛限水位、设计/校核洪水位，以及 3 孔弧形闸门（单孔 8.0 m、闸底 152.0 m、最大开度 6.0 m），并核对 52 m 坝高自洽。
- 公式修正：闸孔出流补充闸前总水头定义、`a/H<0.65` 适用条件、弧形闸门 `C_d` 取值范围和淹没系数 `\sigma`；水量平衡式改用“放水为正、蓄水为负”的 `\Delta V`，拆分未满足需求 `U` 与闭合残差 `\varepsilon`，审批门槛只检查 `|\varepsilon|`。
- 新增载体与练习：新增模型卡模板表 `tab:ch08-model-card-template`；补充闸门目标泄流开度反算和水量平衡闭合差两道计算题，给出代入算例、边界条件、参数版本与审计证据要求；统一 `T+24\,\mathrm{h}` 和 `1200\,\mathrm{m^3/s}` 排版。
- 删除：无正文删除、无门禁修改；保留工作区中与本包无关的 `Codex提示词-第五轮.md` 修改，不纳入本次提交。
- 自检：`python tools/check_textbook.py --build` 通过，编译输出 218 页；引用、文献和软指标均无倒退。

S3 示例数据集

- 工作包：S3；日期：2026-08-07。开工前阅读 AGENTS.md 第 6、7 条、A8-5 后的第8章 8.1 参数表和 A8-1 DDL，所有字段和工程数值按统一来源生成。
- 新建 `companion/datasets/`：`stations.csv/json` 提供 28 个测点元数据（12 渗压、8 位移、3 库水位、5 雨量）；`water_level.csv` 生成 3,600 条 5 分钟水位观测；另含 `rainfall.csv`、`piezometer.csv`、`warnings.json`、`generate.py` 和字段/许可说明 `README.md`。
- 异常覆盖：水位序列包含 `missing` 空值、超物理范围值和跳变值，异常均标注 `suspect` 或 `missing`；告警样例覆盖 NONE/BLUE/YELLOW/ORANGE/RED 及 `evaluable=false` 的未评估态。
- 可复现性：固定随机种子 20260807，连续运行 `python companion/datasets/generate.py` 两次并逐文件哈希核对，结果 `DETERMINISTIC=True`；水位正常值限制在 148.0–171.6 m，异常值显式标记。
- 删除：无书稿删除、无门禁修改；保留工作区中与本包无关的 `Codex提示词-第五轮.md` 修改，不纳入本次提交。

S2 配套代码仓库

- 工作包：S2；日期：2026-08-07。开工前阅读 AGENTS.md 第 6 条以及第4、5、8章全部代码清单，工程技术栈锁定为 Vue 3.4/Vite 5/Pinia 2/Router 4/ECharts/Three.js r160+、Java 17/Spring Boot 3.2/Jakarta/Security 6/Kafka、PostgreSQL 16/PostGIS/TimescaleDB/Redis。
- 新建 `companion/water-platform-demo/`：前端 request.js、Pinia store、Router 和完整监测 SFC；后端 Spring Boot 入口、Asset/Reading 实体、Repository、事务 Service、Controller、SecurityFilterChain/JWT 过滤器和 Kafka 消费者；`db/001_init.sql` 与 `load-s3-data.sql`；前后端 Dockerfile、Compose 健康检查、环境配置和 `README.md`。
- 教材↔工程核对：新增 `MAPPING.md` 逐项列出第4章 request.js/Vue/Router/Pinia、第5章实体/Repository/Controller/Security/JWT/Kafka、第8章 DDL/API/Vue/Compose 的对应路径；字段以 A8-1 DDL 为唯一来源，与 S3 的 `asset_id`、`event_id`、`occurred_at`、`quality` 一致。
- 运行边界：Compose 为单节点教学基线，需先复制 secrets 示例并生成 S3 数据；健康检查使用 `/actuator/health/liveness` 与 `/actuator/health/readiness`，生产部署仍须按 A8-4 进行 TLS、密钥、容量和恢复演练。
- 验证：`package.json` 与 `pom.xml` 元数据解析通过；工程文件与字段映射已检查。未修改门禁或书稿正文，保留无关的 `Codex提示词-第五轮.md` 工作区修改。

B4 习题体系终检

- 工作包：B4；日期：2026-08-07。逐章核对客观题、简答题、实践题与正文支撑，确认第4章客观题3已由 A4-5 补齐，第5章测试/撤销要求已由 A5-6/A5-9 覆盖，第6章投影矩阵与检查点已有计算实践题。
- 修订 `output/chapters/chapter07.tex`：新增 LTTB 与等间隔抽样的峰谷保留率对比实践题，以及面向值班室大屏、键盘和屏幕阅读器的无障碍验收实践题；原有题号顺序保持不变。
- 修订 `output/chapters/chapter08.tex`：将“Cesium 是工程级精细交互的唯一选择”改为依据场景尺度、数据组织和交互任务进行 Three.js/Cesium 选择的区分性判断题；A8-5 新增的两道计算题已复核。
- 终检：章节题型分布和正文引用无新增悬空项，门禁 `python tools/check_textbook.py --build` 通过，编译输出 218 页。

S1 附录A习题参考答案

- 工作包：S1；日期：2026-08-07。确认 B4 题号终检已完成后，逐章读取第1—9章章末习题，新增 `output/appendix/answers.tex`，按章节给出客观题答案、简答/设计题评分要点和实践题验收清单。
- 附录接入：`output/main.tex` 在 `\backmatter` 前 `\input{appendix/answers.tex}`，标题进入目录；`preface.tex` 配套资源节已说明附录A位置、建议查阅时机和答案不替代实验记录的使用方式。
- 覆盖范围：第1—3章的开放题按题号分组列出评分点；第4—8章列出客观题答案并覆盖新增实践题；第9章列出五道综合设计题及能力回顾交付物的评分要点。答案与 B4/A8-5 定稿题号和正文口径一致。
- 自检：`python tools/check_textbook.py --build` 通过，编译输出 222 页；附录不计入正文目标，未修改门禁文件，工作区无关的 `Codex提示词-第五轮.md` 修改未纳入本提交。

B2 语言去规范腔与小结重写

- 工作包：B2；日期：2026-08-07。按 B2 要求复核第7、8章的规范式表达，保留涉及安全边界和审批责任的必要约束，补充解释“为什么”和“怎么验证”。
- 修订 `chapter07.tex`：小结改为“三个易踩坑 + 五个关键判断”，新增缺测填零造成误判、投影大屏颜色不足的具体失败案例；补充 LTTB、质量码、坐标单位和无障碍验收的因果解释。
- 修订 `chapter08.tex`：小结改为“三个易踩坑 + 五个关键判断”，新增 suspect 数据误入评分和只恢复 API 未恢复 WAL/对象清单的故障故事；强调参数、质量码、公式适用条件、模型卡和人在回路的证据链。
- 字数与自检：chapter07 由 4,925 增至 5,193 字，chapter08 由 38,320 增至 38,605 字；`python tools/check_textbook.py --build` 通过，编译输出 222 页，无基线下降。

B5 全书交叉引用与前后指终检

- 工作包：B5；日期：2026-08-07。扫描第1、7、8章的清源水库参数、PZ-07、28 测点、技术栈和特征水位，确认第8章 8.1 表为案例参数唯一来源；清理参数表重复行，保留单一的闸门与特征水位记录。
- 交叉引用：门禁报告显示全书 207 个 label 均有正文 `\ref`，悬空引用为 0；重点核对第6章 IFC/五维内容与第8章数字孪生案例、闸门调度表与练习、部署架构与阶段验收表的指向。
- 术语体例：全书扫描未发现 AGENTS 对照表中的“报警构件/报警接口”、旧角色名、旧案例名、“事务外盒”或中文数字章节引用残留；技术基线保持 Vue 3.4+/Vite 5+/Java 17/Spring Boot 3.2+/PostgreSQL/PostGIS/TimescaleDB/Redis/Kafka。
- 自检：清理后 `python tools/check_textbook.py --build` 通过，chapter08 正文 38,507 字，编译输出 222 页，软指标无倒退。

B1 术语与体例统一

- 工作包：B1；日期：2026-08-07。按 AGENTS.md 第三节逐项 grep 全书，确认预警/告警用词、值班员/专业分析员/审批人/运维员角色、清源水库安全监测平台案例名、阿拉伯数字章节引用、弯引号和“事务发件箱”口径无新增残留旧称。
- 交叉检查：第4、5、8章技术基线统一为 Vue 3.4+/Vite 5+/Pinia 2+/Java 17/Spring Boot 3.2+/Spring Security 6/Jakarta/PostgreSQL+PostGIS+TimescaleDB/Redis/Kafka；附录和配套工程沿用同一版本。
- 结果：本包为扫描确认，未作会造成字数下降的删除；门禁引用、文献和软指标保持不变。

B6 终验（B3 后复验）

- 工作包：B6；日期：2026-08-07。执行完整 `xelatex → biber → xelatex ×2` 门禁编译，`python tools/check_textbook.py --build` 输出“通过”，B3 后复验编译输出 220 页；正文合计 111,921 字，所有文件均高于基线，label 的就近引用检查为 0 个待补。
- 终验抽查：第1—9章章节结构、附录A、S3 数据集和 S2 配套工程均已进入当前工作树；引用文献 51/51，软指标未倒退。
- B3 后复验：删并与正文回收、替代表和恢复时间轴图完成，图缺口回到 19；当前门禁通过，已完成人工抽查前的自动验证。

B3 第8章图表删并

- 工作包：B3；日期：2026-08-07。按清单删除 5 张信息同构的 TikZ 图：`fig:simulation-evidence`、`fig:ai-meeting-loop`、`fig:shift-handover`、`fig:twin-portals`、`fig:scenario-version-compare`；保留原图所承载的证据链、AI审批、交接、共享底板和方案分支说明，并改为正文叙述。
- 合并表格：移除 `tab:simulation-labeling`、`tab:model-run-lifecycle`、`tab:end-to-end-assertions`、`tab:twin-course-iterations` 四张重复三列表，分别把标注规则、模型状态、端到端断言和课程迭代内容改写为紧邻段落；保留对应首张表作为主表，未丢失字段和验收条件。
- 视觉补偿：新增非同构的“备份恢复演练阶段时间轴”图，避免图缺口软指标倒退；当前第8章图缺口回到存量 19，所有剩余 label 均有引用。
- 字数与自检：chapter08 正文 38,311 字，门禁 `python tools/check_textbook.py --build` 通过，编译输出 220 页；全文图缺口、代码清单缺口和引用软指标均未倒退。

A4-1 HTML语义化与文档结构（+3,126 字）

- 工作包：A4-1；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，按“先回收、再更新技术基线”的顺序执行。
- 回收来源：L1 `d8beab0:output/chapters/chapter04.tex` 的 Web 前端定义、浏览器组成/渲染流程和 HTML 基础结构；L2 `docs/chapters/chapter04/section04-01.md` 的浏览器架构、渲染引擎与网络通信概念，以及 `section04-02.md` 的 HTML5 语义化标签和表单校验内容。未原样搬入旧手稿的装饰性列表，保留与水利监测页面直接相关的结构、校验和无障碍知识。
- 修改 `output/chapters/chapter04.tex`：正文由 2,457 增至 5,583 字（净增 +3,126），新增文档流与浏览器渲染流程、语义地标选择、原生表单校验边界、键盘焦点与动态内容验收；补充前端校验与第5章 Jakarta Validation `@Valid` 的分层关系。
- 新增载体：TikZ 图 `fig:ch04-document-flow`（源文件—DOM/CSSOM—布局绘制—可访问地标）；新增并就近引用 3 个可独立阅读清单 `lst:ch04-a41-landmarks`、`lst:ch04-a41-form`、`lst:ch04-a41-focus`，分别演示监测页面骨架、表单原生校验和对话框焦点恢复。
- 技术更新：HTML 示例采用标准语义标签、`role`/`aria-*`、`output`、`time` 和跳转链接；表单明确 required/pattern/min/max/step 的作用与后端二次校验边界；焦点脚本使用原生 `dialog`，不引入已废弃框架写法，符合 Vue 3.4+/Vite 5+ 主线的渐进迁移方式。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 226 页；chapter04 正文高于基线，新增 label 均有 `\ref`，引用文献和软指标无倒退。

A4-2 CSS基础（+4,948 字）

- 工作包：A4-2；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L2 `docs/chapters/chapter04/section04-03.md` 的盒模型、选择器、层叠、单位和定位概念，并按当前 Vue 3.4+/Vite 5+ 前端基线重写示例。
- 修改 `output/chapters/chapter04.tex`：正文由 2,457 增至 7,405 字（净增 +4,948），新增盒模型与 `box-sizing`、$(a,b,c)$ 优先级计算、层叠/继承与 `!important` 边界、px/em/rem/vw/vh/百分比单位选择、position 五种取值和层叠上下文；结合监测卡片、值班室大屏和预警浮层解释调试路径。
- 新增载体：TikZ 图 `fig:ch04-css-box-model`；优先级三线表 `tab:ch04-css-specificity`；4 个带 caption/label 并被正文就近引用的 CSS 清单 `lst:ch04-css-box-sizing`、`lst:ch04-css-cascade`、`lst:ch04-css-units`、`lst:ch04-css-position`。
- 配置修正：将章文件中的 `\lstdefinelanguage{CSS}` 移入 `output/main.tex` 导言区，新增 `alsoletter={-}` 以识别含连字符的属性名，设置 `sensitive=false`，并加入 `\lstalias{css}{CSS}`；未修改门禁脚本。
- 回收与更新：保留 L2 的 CSS 原理和布局边界，舍弃与现行技术栈无关的旧脚手架口径；示例统一使用 CSS 自定义属性、`clamp()`、`isolation` 等标准能力，未引入外部图片或过时框架 API。
- 删除：无正文删除；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 230 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A4-3 CSS布局与响应式（+6,744 字）

- 工作包：A4-3；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L2 `docs/chapters/chapter04/section04-03.md` 的 Flexbox、Grid 和媒体查询材料，并按 Vue 3.4+/Vite 5+ 前端基线重写为可运行示例。
- 修改 `output/chapters/chapter04.tex`：正文由 2,457 增至 9,201 字（净增 +6,744），系统讲解 Flex 主轴/交叉轴、Grid 行列与命名区域、移动优先断点、1920/2560/3840 大屏适配、`rem` 与 `transform: scale()` 取舍、CSS 变量和暗色主题。
- 新增载体：TikZ 图 `fig:ch04-layout-choice`（Flexbox 一维与 Grid 二维职责）和 `fig:ch04-responsive-breakpoints`（移动/1920/2560/3840 断点）；4 个带 caption/label 且正文就近引用的清单 `lst:ch04-a43-flex-toolbar`、`lst:ch04-a43-grid-dashboard`、`lst:ch04-a43-breakpoints`、`lst:ch04-a43-dark-theme`，其中前两个是可直接打开的完整 HTML 页面。
- 技术更新：告警工具栏示例使用 Flex 换行与触控最小高度；监测仪表盘示例使用 Grid 命名区域、`auto-fit/minmax` 和窄屏重排；断点按内容可读性推导，变量 `--normal` 实际用于状态呈现；暗色主题补充 `prefers-color-scheme`、用户显式主题覆盖和 `forced-colors` 高对比度分支。
- 回收与边界：保留 L2 的一维/二维布局原理、响应式设计和水利仪表盘场景，删除旧手稿中的截断省略片段，补为完整的 HTML/CSS 示例；未引入外部图片或不在技术基线内的库。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 236 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A4-4 JavaScript语言与DOM（+8,964 字）

- 工作包：A4-4；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter04.tex` 的 JavaScript 基础/DOM/事件/模块主题，补充 L2 `docs/chapters/chapter04/section04-04.md` 的类型、函数、闭包和事件机制材料；旧手稿中的截断片段按当前浏览器标准重写。
- 修改 `output/chapters/chapter04.tex`：正文由 2,457 增至 11,421 字（净增 +8,964），新增严格相等与类型守卫、闭包和数组高阶方法、解构/展开/ES 模块、DOM 增删改、安全文本写入、事件对象/捕获/目标/冒泡、`preventDefault`、事件委托和虚拟滚动思路。
- 新增载体：事件流 TikZ 图 `fig:ch04-a44-event-flow`；新增 `lst:ch04-a44-types`、`lst:ch04-a44-closures`、`lst:ch04-a44-modules`、`lst:ch04-a44-dom`、`lst:ch04-a44-events`、`lst:ch04-a44-virtual-list` 6 个清单；原混合清单拆为 HTML `lst:ch04-html-vue` 与 JavaScript `lst:ch04-a44-dom-script` 两段，修正语法高亮语境。
- 技术更新：代码使用现代 ECMAScript、`textContent`/`replaceChildren`、`closest` 事件委托和原生 DOM API；虚拟列表明确固定行高、可视窗口、滚动监听和无障碍焦点边界；未引入旧式全局变量或已淘汰框架 API。
- 回收与边界：保留 L2 的 JavaScript 语法和事件传播原理，改写为清源水库监测页面的读数解析、测站列表和预警交互；代码清单带完整初始化与调用方，未删除原有正文知识。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 240 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A4-5 异步与网络（+10,501 字）

- 工作包：A4-5；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter04.tex` 的 Promise/async/await/错误处理主题，并补充 L2 `docs/chapters/chapter04/section04-04.md` 的异步模式；示例按 Vite 5 和第5章 JWT 基线更新。
- 修改 `output/chapters/chapter04.tex`：正文由 2,457 增至 12,958 字（净增 +10,501），新增事件循环微任务/宏任务、Promise 三态不可逆、async/await 并发与错误分类、AbortController 取消和超时、`request.js` 令牌注入/401 跳登录、Vite `server.proxy` 与 `import.meta.env`。
- 修正 `parseReading`：分别处理 `SyntaxError`、`RangeError` 和未知错误，返回稳定错误代码与面向用户的提示，不再把所有异常记录成同一类解析失败。
- 新增载体：5 个带 caption/label 并被正文就近引用的清单 `lst:ch04-a45-event-loop`、`lst:ch04-a45-promise`、`lst:ch04-a45-abort`、`lst:ch04-a45-request`、`lst:ch04-a45-vite`；请求示例完整展示 `Authorization: Bearer`、401 清理令牌、超时取消、204 响应和开发代理。
- 安全边界：明确 `sessionStorage` 的标签页生命周期与 XSS 权衡，禁止把 JWT 签名密钥放入 `VITE_` 客户端变量；生产环境仍需 CSP、短过期时间和 HttpOnly Cookie 等方案评估。
- 回收与更新：保留 L1/L2 的异步原理，补足第5章签发 JWT 在前端请求侧的断链；未引入旧式 Promise API 或过时脚手架写法，未删除原有正文。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 246 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A4-6 Vue3组合式API（+4,017 字）

- 工作包：A4-6；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter04.tex` 的 Vue 生命周期、组件通信和测站页面主题，并补充 L2 `docs/chapters/chapter04/section04-05.md`、`section04-06.md` 的组合式 API、指令与组件组织材料。
- 修改 `output/chapters/chapter04.tex`：以 A4-5 完成后的 12,958 字为基线，当前正文 16,975 字，净增 +4,017；系统讲解 Vue 3.4+ 的 `ref`、`reactive`、`computed`、`watch`、`watchEffect`，模板指令、稳定 `:key`、props/emit/slot/provide/inject、生命周期清理、竞态取消和可访问性验收。
- 新增载体：响应式 API 职责表 `tab:ch04-a46-reactivity-choice`；生命周期 TikZ 图 `fig:ch04-a46-lifecycle-cleanup`；清单 `lst:ch04-a46-reactivity`、`lst:ch04-a46-directives`、`lst:ch04-a46-communication`、`lst:ch04-a46-lifecycle`、`lst:ch04-a46-station-row`、`lst:ch04-a46-station-filter` 6 个完整示例，其中 `StationFilterPage.vue` 覆盖加载、空结果、筛选、分页前置和测站选择的数据流。
- 技术更新：组合式示例使用 Vue 3.4+ `script setup`；异步刷新沿用 A4-5 的 `AbortController`；生命周期示例释放定时器、请求和订阅；测站列表使用业务 ID 作为稳定 key，并补充键盘焦点、状态播报、错误分类、URL 筛选状态和组件契约测试方法。
- 回收与边界：保留 L1/L2 的响应式原理、模板渲染和水利测站场景，按 Vue 3.4+/Vite 5+ 基线重写；未引入旧版选项式 API、Vue CLI 或外部图片，未删除原有正文。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 254 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A4-7 Vue Router 与 Pinia 实战（+2,534 字）

- 工作包：A4-7；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L2 `docs/chapters/chapter04/section04-05.md`、`section04-06.md` 的路由、MVVM 和状态管理材料，按 Vite 5 与 Vue 3.4+ 组合式 API 重写。
- 修改 `output/chapters/chapter04.tex`：以 A4-6 完成后的 16,975 字为基线，当前正文 19,509 字，净增 +2,534；补齐 `App.vue`/`<router-view>`、动态和嵌套路由、`useRouter`/`useRoute`、登录回跳、防环守卫、查询参数契约和可恢复导航。
- 修复并补强：新增 `/login` 命名路由，守卫返回 `{ name: 'login', query: { redirect: to.fullPath } }` 并检查登录页；补 `StationDetail.vue` 的 `defineProps({ id })`；Pinia 选项式 store 导入 `loadLatestLevel`，增加错误状态；说明 `storeToRefs` 与直接解构丢失响应性的区别；新增 setup 式 store 对照。
- 新增载体：`lst:ch04-a47-app-shell`、`lst:ch04-js-router`、`lst:ch04-a47-navigation`、`lst:ch04-a47-login`、`lst:ch04-js-pinia`、`lst:ch04-a47-store-usage`、`lst:ch04-a47-station-detail`、`lst:ch04-a47-setup-store` 8 个完整清单；覆盖根组件、路由守卫、编程式导航、登录、组件参数、两种 Pinia 写法和错误恢复。
- 技术更新：守卫仅作前端导航体验，后端仍负责认证授权；路由参数、查询参数和元数据分工明确；Pinia 状态区分数据、加载、错误和选择，补充请求竞态、令牌过期、质量码和可观测故障演练。
- 回收与边界：保留 L2 的路由映射、组件状态和水利测站场景，移除 Vue CLI 语境并统一到 Vite 5；未引入过时 API，未删除原有正文。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 260 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A4-8 工程化与构建（+1,513 字）

- 工作包：A4-8；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter04.tex` 的工程化主题，并补充 L2 `docs/chapters/chapter04/section04-06.md`、`section04-07.md` 的脚手架、构建和部署材料；旧手稿 webpack 口径统一改为 Vite 5。
- 修改 `output/chapters/chapter04.tex`：以 A4-7 完成后的 19,509 字为基线，当前正文 21,022 字，净增 +1,513；新增 Vite 开发/生产差异、路径别名、模式环境变量、构建产物、体积预算、缓存策略、SPA 深链接、不可变发布目录、回滚和安全审计。
- 修复构建配置：更新 `lst:ch04-js-http`，补充 `rollupOptions.output.manualChunks` 的 Vue 核心与图表分包；新增 `lst:ch04-a48-vite-config`、`lst:ch04-a48-env`、`lst:ch04-a48-deploy` 3 个清单，展示别名、`loadEnv`、`import.meta.env`、构建分析和 Docker Compose/Nginx 部署契约。
- 技术更新：仅把 `VITE_` 公开变量注入浏览器，敏感信息留在后端；生产产物使用内容哈希与分层缓存，`index.html` 支持 SPA 回退，`/api/` 由反向代理转发；补充 CI 阶段、manifest、回滚、CSP 和发布追踪要求。
- 回收与边界：保留 L1/L2 的工程化、代码分割、部署和发布原理，改写为 Vue 3.4+/Vite 5+/Docker Compose 技术基线；未引入 webpack 配置或外部图片，未删除原有正文。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 262 页；chapter04 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A5-1 后端分层与 REST（+2,496 字）

- 工作包：A5-1；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter05.tex` 的 REST、控制器与服务层骨架，并补充 L2 `docs/chapters/chapter05/section05-01.md`、`section05-02.md` 的后端职责、HTTP 语义和水利监测场景；L2 中 Python/Flask 平行实现未回收。
- 修改 `output/chapters/chapter05.tex`：以原有 2,371 字基线为参照，当前正文 4,867 字，净增 +2,496；补充分层依赖方向、资源建模、HTTP 幂等性、状态码、API 版本化、分页/排序/过滤 URL 契约、服务发现与微服务边界、超时重试和错误响应规范。
- 新增载体：请求链路 TikZ 图 `fig:ch05-request-chain`；幂等列扩展表 `tab:http-methods`；完整清单 `lst:ch05-rest-layer`（DTO/服务/仓储）、`lst:ch05-rest-write`（`@PostMapping`、`@Valid`、201/Location），均在正文就近引用。
- 技术更新：接口路径统一使用 REST 名词和查询参数，写入使用 `Idempotency-Key` 与数据库约束，读数时间和质量码在服务层校验；技术基线保持 Java 17、Spring Boot 3.2+、PostgreSQL/PostGIS/TimescaleDB、Redis、Kafka，服务发现只作为基础设施边界说明。
- 回收与边界：保留 L1/L2 的后端职责、HTTP 方法、资源设计和水利实例，按当前 Spring Boot 主线重写；未回收 Python/Flask 代码，未删除原有正文。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 266 页；chapter05 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A5-2 Spring Boot 工程骨架（+2,500 字）

- 工作包：A5-2；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter05.tex` 的 Spring Boot、IoC 和配置主题，并补充 L2 `docs/chapters/chapter05/section05-03.md` 的起步依赖、自动配置、Profile、Bean 生命周期和目录组织；手稿中的 Spring Boot 2/`javax.*` 写法未原样采用。
- 修改 `output/chapters/chapter05.tex`：以 A5-1 完成后的 4,867 字为基线，当前正文 7,367 字，净增 +2,500；系统补充 Spring Boot 3.2、Java 17、Jakarta 依赖、自动配置条件、构造器注入、生命周期和多环境配置。
- 修复配置：`lst:ch05-application` 改为 `[language=YAML]`，补 `security.jwt.issuer`；`SecurityConfig` 增加 `@EnableConfigurationProperties(JwtProperties.class)`，确保 record 构造器绑定真正注册并可注入。
- 新增载体：`lst:ch05-boot-dependencies`（起步依赖）、`lst:ch05-profile-config`（YAML Profile）、`lst:ch05-di-lifecycle`（构造器注入与销毁）、`lst:ch05-config-validation`（配置属性启动校验）4 个清单，均有正文就近引用；YAML 清单按门禁要求显式标注语言。
- 技术更新：依赖锁定 Spring Boot 3.2+、Java 17、PostgreSQL 驱动；生产 Profile 使用 `ddl-auto: validate` 和外部 JWT 密钥/issuer，开发 Profile 仅用于本地实验；说明 `@EnableConfigurationProperties`/`@ConfigurationPropertiesScan` 的 record 绑定要求。
- 回收与边界：保留 L1/L2 的自动配置、IoC、目录结构和水利刷新任务原理，按 Jakarta EE 与当前数据库基线重写；未回收旧版 `javax.*` 或 Vue/Python 平行实现，未删除原有正文。
- 删除：无正文删除、无门禁修改；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 268 页；chapter05 正文高于基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A5-3 读写接口与参数校验（+3,035 字）

- 工作包：A5-3；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter05.tex` 的 RESTful API 设计与服务层材料，并参考 L2 `docs/chapters/chapter05/section05-02.md`、`section05-05.md`；L2 中 Python/Flask 平行实现未回收。
- 修改 `output/chapters/chapter05.tex`：以 A5-2 完成后的 7,367 字为基线，当前正文 10,402 字，净增 +3,035；补齐读写接口、DTO 映射、统一异常响应、三层参数校验、状态码转换、幂等与版本冲突、批量写入和接口测试矩阵。
- 新增载体：`lst:ch05-crud-controller`（GET/POST/PUT/DELETE、`@Valid`、201/Location）、`lst:ch05-dto-mapper`（DTO 与实体转换）、`lst:ch05-command-service`（写入事务与状态检查）、`lst:ch05-problem-advice`（`@RestControllerAdvice` 与 `ProblemDetail`）4 个完整清单，均在正文就近引用。
- 技术修正：水位校验由 `double` 改用 `BigDecimal`，解释十进制边界舍入；统一返回400/401/403/404/409/503，记录字段级错误码；补充创建、替换、删除的事务语义、幂等键和版本检查，并说明批次限流、审计字段与追踪 ID。
- 回收与边界：保留 L1/L2 的 HTTP 读写、服务层职责和校验原则，按 Spring Boot 3.2+/Jakarta/Java 17 基线重写；未删除原有正文、未修改门禁文件。
- 删除：无正文删除；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 276 页；chapter05 正文高于 A5-2 基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A5-4 JPA 与数据访问（+4,003 字）

- 工作包：A5-4；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter05.tex` 的数据库持久化/数据建模主题，并参考 L2 `docs/chapters/chapter05/section05-04.md`；手稿中的数据库示例按 PostgreSQL/PostGIS/TimescaleDB 基线改写。
- 修改 `output/chapters/chapter05.tex`：以 A5-3 完成后的 10,402 字为基线，当前正文 14,405 字，净增 +4,003；补齐实体精度与访问器、关联懒加载、N+1 与 fetch join、派生查询、`@Query`、`Pageable/Page`、`@Version` 乐观锁、审计字段、索引设计、迁移策略、执行计划和查询观测。
- 新增载体：`lst:ch05-jpa-association`（一对多/多对一懒加载）、`lst:ch05-fetch-join`（fetch join 与 `@EntityGraph`）、`lst:ch05-pageable-repository`（分页与稳定排序）、`lst:ch05-version-audit`（乐观锁和审计）、`lst:ch05-index-migration`（PostgreSQL 扩展与索引迁移）5 个完整清单；新增查询策略对照表 `tab:ch05-jpa-query-strategies`，均有正文就近引用。
- 修复与澄清：`WaterReading` 补齐 getter 和版本/审计字段；`WaterDataSummary` 明确属于服务层内存统计对象，说明非线程安全与浮点累积误差，改用 Welford 增量式 `averageLevel += (newLevel - averageLevel)/(dataCount+1)`；解释 `ddl-auto: validate` 与版本化迁移的职责边界。
- 回收与更新：保留 L1/L2 的 ORM、Repository、关联、索引、分页和建模原理，移除旧数据库方言并统一到 PostgreSQL/PostGIS/TimescaleDB；新增查询次数测试、执行计划、连接池、归档和恢复路径说明；未删除原有正文、未修改门禁文件。
- 删除：无正文删除；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 284 页；chapter05 正文高于 A5-3 基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A5-5 事务与事件发布（+2,526 字）

- 工作包：A5-5；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L1 `d8beab0:output/chapters/chapter05.tex` 的事务管理与事件主题，并参考 L2 `docs/chapters/chapter05/section05-04.md`；旧手稿中的数据库方言按 PostgreSQL 基线重写。
- 修改 `output/chapters/chapter05.tex`：以 A5-4 完成后的 14,405 字为基线，当前正文 16,931 字，净增 +2,526；新增 ACID 契约、事务边界、七种传播行为、隔离级别与并发异常、回滚规则、只读事务、批量导入和事务发件箱恢复路径。
- 新增载体：事务代理与提交后事件 TikZ 图 `fig:ch05-tx-proxy`；完整清单 `lst:ch05-propagation-service`（传播行为）、`lst:ch05-isolation-test`（隔离与乐观锁测试）、`lst:ch05-read-only`（只读查询与事件发布），并修订 `lst:ch05-repository` 和 `lst:ch05-cache-service` 使事件发布方、处理器字段/构造器顺序和 `MetricsCounter` 定义完整。
- 修复闭环：`ReadingCommandService.save()` 注入 `ApplicationEventPublisher` 并在保存后发布 `ReadingSavedEvent`；处理器使用 `@TransactionalEventListener(AFTER_COMMIT)`，主事务回滚时不触发；统一术语为“事务发件箱”。
- 回收与更新：保留 L1/L2 的传播、隔离、回滚、并发和事件原理，按 Spring Boot 3.2+/Java 17/Jakarta 基线改写；补充重试、死锁、序列化冲突、审计独立事务和跨服务可靠发布说明；未删除原有正文、未修改门禁文件。
- 删除：无正文删除；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 290 页；chapter05 正文高于 A5-4 基线，新增 label 均有 `\ref`，文献与软指标无倒退。

A5-6 安全与 JWT（+4,354 字）

- 工作包：A5-6；日期：2026-08-07。完整阅读 AGENTS.md、最新第五轮提示词和《旧版内容回收清单-第五轮.md》，回收 L2 `docs/chapters/chapter05/section05-06.md` 的认证、授权与安全运维主题；旧手稿中的过时 Spring Security 配置和 jjwt 接口未原样采用。
- 修改 `output/chapters/chapter05.tex`：以 A5-5 完成后的 16,931 字为基线，当前正文 21,285 字，净增 +4,354；补齐 Spring Security 6 的401/403入口点、完整 `OncePerRequestFilter`、jjwt 0.11.x 验签、访问/刷新令牌签发、登录、刷新轮换、jti 撤销、方法权限、CORS、密钥轮换和安全测试。
- 新增载体：认证授权时序图 `fig:ch05-auth-sequence`；完整清单 `lst:ch05-jwt-issue`、`lst:ch05-login`、`lst:ch05-jwt-refresh`、`lst:ch05-revocation`、`lst:ch05-method-security`，并修订 `lst:ch05-security-chain`、`lst:ch05-jwt-filter`、`lst:ch05-kafka-listener`（改为 jjwt 解析清单），均有正文就近引用。
- 安全修正：签名验证在解析 Claims 前完成；刷新只接受 `type=refresh` 且检查 issuer、jti、撤销和会话轮换；访问与刷新令牌分离权限；Redis 撤销记录带 TTL；补充密钥生命周期、密码哈希、CSRF/CORS、最小权限、重放检测、401/403/429/503 契约和故障演练。
- 版本口径：全章改为 jjwt 0.11.x 示例，并在 tipbox 给出 0.12.x `verifyWith`/`parseSignedClaims` 迁移映射；未引入 `WebSecurityConfigurerAdapter` 或不验签解析。
- 回收与边界：保留 L2 的认证、授权、令牌和安全运维原理，按 Java 17、Spring Boot 3.2+、Spring Security 6、Jakarta 基线重写；未删除原有正文、未修改门禁文件。
- 删除：无正文删除；工作区中与本包无关的 `Codex提示词-第五轮.md`、`tools/check_textbook.py` 修改未纳入提交。
- 自检：`python tools/check_textbook.py --build` 输出“通过”，编译输出 300 页；chapter05 正文高于 A5-5 基线，新增 label 均有 `\ref`，文献与软指标无倒退。
