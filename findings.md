# 修订发现

## 已确认问题

### 第三章

- 主题仍偏通用软件架构教材，SafeHome 智能家居案例贯穿多处，不符合智慧水利主线。
- 同章内多次重复出现“思考题与练习”块。
- 章末结构冗余，存在 `\section*{小结}` 后再次出现多套练习标题。

### 第三章已完成修订

- 已将主案例统一替换为“智慧水库安全监测平台”。
- 已将需求分析、用例建模、构件协作与接口设计改写为智慧水利语境。
- 已删除章末重复的“思考题与练习”块，仅保留一组章末练习题。

### 第六章

- 前言承诺第六章覆盖 WebGL/Three.js、GIS 地图服务、BIM 建模与数字孪生。
- 实际生成后的 `chapter06.tex` 顶层只剩“倾斜摄影技术应用”，章结构明显失衡。

### 第六章已完成修订

- 已增补 `WebGL与Three.js三维渲染基础` 与 `GIS地图服务与三维场景集成` 两个顶层部分。
- 已将倾斜摄影内容收束到 `BIM与倾斜摄影三维模型构建` 主线下。
- 已将数字孪生提升为独立顶层部分，并补足与 BIM、模型成果分析之间的衔接。

### 第八章

- 存在将数学表达直接放入 `lstlisting` 的情况，如带下标和不等号的优化模型。
- 存在将非 Python 表达式放入 `language=Python` 代码块的情况。
- 存在偏互联网平台教材的术语堆叠，如 CAP、一致性哈希、计费服务等。
- `main.log` 已显示第八章多处 Unicode 字符缺字。

### 第八章已完成修订

- 已将优化模型、统计公式、可靠性指标、缓存与调度等数学表达改写为公式环境。
- 已将误标为 Python 的非代码内容改回正文说明、公式或 Java/伪代码表达。
- 已删减偏互联网平台的离题术语，并改写为智慧水利监测平台的一致性、可用性和服务边界表述。

### 第九章

- 全书回顾与当前第三章正文不一致，仍在回顾“版本控制、敏捷开发、微服务架构”等旧内容。

### 第九章已完成修订

- 已将第三章回顾改为软件架构基础、设计原则、常见模式与智慧水库安全监测平台案例。

## 修订清单（按执行顺序）

1. 第六章重建章结构：
   - 增补 `WebGL/Three.js` 基础部分。
   - 增补 `GIS 地图服务与三维场景集成` 部分。
   - 将现有倾斜摄影内容收束到“三维模型构建”主题下。
   - 补足 `BIM` 与数字孪生衔接内容，使其与前言承诺一致。
2. 第八章规范公式与代码表达：
   - 将放入 `lstlisting` 的数学模型改为公式环境。
   - 将非 Python 代码块改为伪代码、公式或正文说明。
   - 删减偏互联网平台教材的离题术语堆叠，回收到智慧水利平台集成主线。
3. 编译层清理：
   - 处理第 4、7、8 章中的 emoji、Unicode 下标和特殊符号缺字问题。
   - 在主要结构修订完成后重新编译并消除高优先级错误。

## 编译质量

- 当前 `output/main.log` 已无 `Missing character`、`Overfull \hbox`、未定义引用、`biblatex` 重跑提示与字体警告。
- 第五章已补做局部清稿：将混入代码块的说明文字拆回正文，并把相关示例统一改为 Java 代码块。
- `output/main.pdf` 当前可稳定生成，页数为 379 页。
- 剩余日志主要是 `Underfull \vbox` 41 处，属于低优先级版面松散提示。
- 本地环境存在辅助文件偶发锁定问题；若后续再次编译异常，优先结束残留 `xelatex/latexmk` 进程并以 UTF-8 空文件重建 `main.aux/main.toc/main.out`。

## 2026-04-19 新发现

- `output/BUILD.md` 仍写为“两遍 XeLaTeX”或 `latexmk -xelatex`，尚未把 `biber` 纳入正式构建流程；这与当前稿件的 `biblatex + biber` 配置不一致。
- `output/main.tex` 当前仍在 `\appendix` 后直接 `\printbibliography`，参考文献区块存在被附录语义包裹的风险。
- 用户本轮审阅意见明确要求将修订顺序调整为：硬伤清零优先，其次是教材化重写，最后才是语言润色。
- 当前下一轮最需要集中投入的章节是第 9 章；第 2、3 章的主要任务是“去通用化”，第 4、5 章的主要任务是“统一技术主线并补引文”。
- 经脚本扫描确认，当前全书 `\cite{}` 引用键共 7 个唯一键，和 `references.bib` 的交集完整，缺失键为 0。
- 脏字符问题已定位为第 4、6、9 章各 1 处，现已全部修复；第 9 章末尾截断句已补全为“不断向前发展”。
- 第 9 章原稿最大问题不是“缺几条文献”，而是包含过多部署现状、性能数字和案例断言；更稳妥的修法是删除高风险断言，改为基于公开政策和标准的审慎表述。
- 第 9 章现已改写为三层结构：政策与建设现状、教材主线之外的延伸方向、写作与教学上的把握；其中政策与标准部分已补入官方来源。
- 第 4 章的主要风险不在于知识错误，而在于“并列展示太多前端路线”，容易让学生误判教材主线；更有效的修法是固定 `Vue 3 + Vue Router + Pinia + Vite`，把 Vuex、Vue CLI 等降为兼容路线。
- 第 5 章的主要风险是“Spring Boot 主线”和“Python 路线”并列过重，同时残留较多企业管理、订单管理语境；更有效的修法是把 Python 明确降为扩展阅读，并优先替换最显眼的资源命名和说明文字。
- 第 2 章现已完成一轮“去通用化”收口：原先最显眼的银行储蓄 ER 图、订单流程图和通用数据流示例，已替换为测站、传感器、监测记录和告警处置主线。
- 第 3 章现已完成一轮“去互联网案例化”收口：Netflix/Amazon、电商导航、商品检索、门窗/烟雾传感器等示例已替换为省级智慧水利平台、测站检索、告警中心和渗压计/雨量计/GNSS 位移监测点。
- 第 2、3 章当前剩余的“客户/服务器架构”“客户端”等表述属于架构术语，不再视为需要清理的离题案例；下一阶段重点应转向“章末交付物”与第 8 章贯穿案例收束。
- 当前“教材像教材”的提升已开始见效：前言已有可直接采用的教学组织方式，第 1 章已增加术语统一入口，第 2、3、4、5、6、8 章已具备章末交付物意识。
- 第 8 章的结构性问题已完成本轮收束：章节标题、引言、一级标题和若干二级标题均已按“贯穿案例总复盘”重排，并补入“阶段验收与最小可运行版本”这一教材型收口。
- 当前下一阶段更值得投入的工作，已从“大结构调整”转向“语言压缩、图表前置解释、英文缩写首次出现格式统一，以及章末题分层整理”的细化修订。
