\chapter{软件工程基础与需求分析}

\paragraph*{学习目标}

学完本章，读者应能够：

\begin{enumerate}
\item 说明软件生命周期各阶段的目标、输入和交付物；
\item 根据需求稳定性、风险、合规和反馈频率选择合适的生命周期模型；
\item 组织干系方识别、需求获取、分类、优先级排序与验证；
\item 使用系统流程图、数据流图、数据字典和决策表描述水利业务；
\item 编制结构完整、可追踪、可验证的软件需求规格说明。
\end{enumerate}

\paragraph*{引言}

需求分析的目的不是尽可能多地记录用户意见，而是形成各方能够共同确认、设计人员能够实现、测试人员能够验证的需求基线。水利项目还涉及跨部门数据、现场设备、专业模型和高风险控制，更需要明确对象、单位、时间、权限、异常和验收证据。本章以“案例灌区用水计划申报与配水”为主要示例，逐步完成从访谈到需求规格说明的工作。案例灌区是贯穿主线（水利工程安全监测平台）之外的方法练习线——配水申报的多方冲突比安全监测更适合练习需求获取与建模；章末“从灌区回到水库”一节会把练成的方法迁移回主线。

\section{软件生命周期}

\paragraph{本节层次}核心：2.1.1、2.1.2、2.1.3、2.1.4；拓展：2.1.5。核心路线只要求读完核心小节。

软件生命周期是软件从提出构想到停止使用的全过程\cite{ISO12207,sommerville2015,bourque2014,zhang2013}。生命周期活动可以迭代或并行，但其责任与交付物不能省略。

\subsection{需求与规划阶段}

团队识别业务问题、干系方、范围、约束和成功标准，评估技术、经济、进度、安全与运维可行性。核心交付物包括范围说明、干系方清单、业务场景、需求基线和初步验收标准。需求分析的具体方法见2.3与2.4节。

\subsection{架构与详细设计阶段}

团队把需求转为系统边界、模块职责、数据模型、接口、权限和部署方案，并记录质量属性权衡。设计不仅覆盖正常路径，还要定义网络中断、数据异常、模型失败和无权限操作等异常路径。

\subsection{实现、集成与验证阶段}

实现阶段依据接口契约编码和配置；集成阶段按可运行切片连接前端、后端、数据、模型与设备；验证阶段检查产品是否正确实现规格，确认阶段检查产品是否解决真实业务问题。单元、接口、系统、性能、安全和用户验收测试分别提供不同证据。

\subsection{部署、运行与维护阶段}

上线前准备环境、数据迁移、培训、回滚和应急方案；运行中监控服务、数据、模型和业务闭环。维护包括缺陷修复、适应环境变化、功能增强与预防性改进。每次变更都要评估影响、更新文档并回归测试。

\subsection{退役与数据处置阶段}

系统停止使用时，需要迁移业务、归档或销毁数据、撤销账号和密钥、保留审计证据，并确认下游系统不再依赖旧接口。退役是受控活动，不是简单关闭服务器。表\ref{tab:sdlc-deliverables}把上述各阶段并排列出：第二列是该阶段必须回答的问题，第三列是回答之后应当留下的交付物。判断一个阶段是否真正完成，看的是第三列有没有产出可供下一阶段引用的东西，而不是日程表上的时间是否走完。

\begin{table}[htbp]
\centering
\caption{软件生命周期阶段、关键问题与交付物}
\label{tab:sdlc-deliverables}
\begin{tabular}{p{2.5cm}p{5.0cm}p{5.1cm}}
\toprule
阶段 & 关键问题 & 典型交付物 \\
\midrule
需求与规划 & 为什么做、为谁做、边界是什么 & 范围、场景、需求基线、验收标准 \\
架构与设计 & 如何分解、如何协作、如何失败 & 架构图、数据模型、接口与异常流 \\
实现与集成 & 组件是否按契约工作 & 代码、配置、构建产物、集成记录 \\
验证与确认 & 是否符合规格并解决业务问题 & 测试用例、缺陷、验收记录 \\
部署与运行 & 如何上线、监控、恢复和演化 & 发布包、运行手册、监控与工单 \\
退役 & 如何迁移依赖并处置数据 & 迁移、归档、销毁和关闭证明 \\
\bottomrule
\end{tabular}
\end{table}

\section{软件生命周期模型与选型}

\paragraph{本节层次}核心：2.2.1、2.2.2、2.2.3、2.2.5；指导实践：2.2.9；拓展：2.2.4、2.2.6、2.2.7、2.2.8。核心路线只要求读完核心小节。

生命周期模型规定活动如何排序、反馈和控制。模型不是固定模板；选型应服务于风险与协作，而不是追逐名称。

\subsection{瀑布模型}

瀑布模型按需求、设计、实现、验证和运行依次推进，阶段评审和文档边界清楚。它适用于需求相对稳定、合规审查严格、软硬件接口明确的部分，但反馈较晚，早期误解可能在后期放大。图\ref{fig:waterfall-model}中那条从“验证”折回“设计”的虚线，正是为缓解这一点而加的评审反馈；但它只跨越一个阶段，如果误解发生在需求环节，回退距离仍然很长。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=6mm and 7mm,
  model/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.0cm,
    minimum height=.75cm,align=center}]
\node[model] (r) {需求};
\node[model,right=of r] (d) {设计};
\node[model,right=of d] (b) {实现};
\node[model,right=of b] (t) {验证};
\node[model,right=of t] (o) {运行};
\draw[->,draw=Primary,thick] (r)--(d);
\draw[->,draw=Primary,thick] (d)--(b);
\draw[->,draw=Primary,thick] (b)--(t);
\draw[->,draw=Primary,thick] (t)--(o);
\draw[->,draw=Accent,dashed,thick,bend right=35] (t) to node[below,font=\scriptsize]{问题反馈} (d);
\end{tikzpicture}
\caption{带评审反馈的瀑布模型}
\label{fig:waterfall-model}
\end{figure}

\subsection{原型模型}

原型用于澄清用户难以直接表达的交互、报表或空间展示需求。团队快速构造可评估版本，收集反馈后修订需求。原型必须注明是探索性还是演化性；探索性原型不应在缺少架构、安全和测试的情况下直接上线。图\ref{fig:prototype-model}把这一过程画成小循环：构造、演示、收集反馈、修订需求，若干轮之后才进入正式开发。循环次数与退出条件应当事先约定，否则原型容易变成没有验收标准的长期返工。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=6mm and 8mm,
  model/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.2cm,
    minimum height=.75cm,align=center}]
\node[model] (ask) {初步需求};
\node[model,right=of ask] (quick) {快速设计};
\node[model,right=of quick] (proto) {构建原型};
\node[model,right=of proto] (review) {用户评价};
\node[model,below=of proto] (baseline) {形成需求基线};
\draw[->,draw=Primary,thick] (ask)--(quick);
\draw[->,draw=Primary,thick] (quick)--(proto);
\draw[->,draw=Primary,thick] (proto)--(review);
\draw[->,draw=Accent,thick,bend left=28] (review) to node[above,font=\scriptsize]{反馈} (quick);
\draw[->,draw=Primary,thick] (review)--(baseline);
\end{tikzpicture}
\caption{原型模型以用户反馈澄清需求}
\label{fig:prototype-model}
\end{figure}

\subsection{迭代与增量模型}

增量模型把能力分成若干可运行交付，例如先交付测点查询，再交付质量标记、预警和工单。迭代模型允许同一能力反复改进。两者常结合使用：每个增量经过需求、设计、实现和验证，小步获得真实反馈。图\ref{fig:incremental-model}把这种安排画了出来：每个增量都完整走过需求到验证，交付的是能独立运行、能独立验收的能力，而不是把做了一半的功能提前上线。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=5mm,
  inc/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=10cm,
    minimum height=.7cm,align=left}]
\node[inc] (i1) {增量1：测点、观测查询与质量码};
\node[inc,below=of i1] (i2) {增量2：规则预警、确认与工单};
\node[inc,below=of i2] (i3) {增量3：模型预演、会商与报告};
\draw[->,draw=Primary,thick] (i1)--(i2);
\draw[->,draw=Primary,thick] (i2)--(i3);
\end{tikzpicture}
\caption{智慧水利平台的可运行增量示例}
\label{fig:incremental-model}
\end{figure}

\subsection{螺旋模型}

螺旋模型以风险为中心，每轮识别目标和备选方案、分析风险、开发验证并规划下一轮。它适用于模型不确定、设备集成复杂或安全风险较高的大型项目，但需要成熟的风险分析和项目治理能力。

螺旋的一轮不是“做一遍再重来”，而是围绕一个可度量的风险问题安排证据。第一象限确定本轮目标、约束、候选方案和成功判据；第二象限列出风险并估计发生概率、影响范围和剩余不确定性，决定是否需要原型、仿真或现场试验；第三象限用最小可行实现验证方案，记录测试输入、环境和失败原因；第四象限由业务代表、专业分析员和运维员共同评审证据，决定进入下一轮、调整范围还是停止该方案。每一轮结束都要留下风险清单、决策记录和下一轮入口条件，不能只留下“已完成”的进度状态。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=10mm and 13mm,
  quad/.style={draw=Primary,rounded corners,fill=PrimaryLight,align=center,
    minimum width=3.5cm,minimum height=1.05cm,text=PrimaryDark},
  center/.style={draw=Accent,fill=Accent!15,circle,inner sep=4pt,align=center}]
\node[quad] (goal) {一、确定目标\\约束、候选方案、验收判据};
\node[quad,right=of goal] (risk) {二、风险分析\\概率、影响、验证策略};
\node[quad,below=of risk] (build) {三、开发与验证\\原型、仿真、联调、测试};
\node[quad,left=of build] (plan) {四、评审并规划下一轮\\采纳、调整或停止};
\node[center] (c) at ($(goal)!0.5!(build)$) {风险\\驱动};
\draw[->,thick,draw=Primary] (goal)--(risk);
\draw[->,thick,draw=Primary] (risk)--(build);
\draw[->,thick,draw=Primary] (build)--(plan);
\draw[->,thick,draw=Accent,dashed] (plan)--(goal);
\draw[->,thick,draw=Accent,dashed] (c)--(risk);
\end{tikzpicture}
\caption{螺旋模型的风险驱动四象限}
\label{fig:ch02-spiral-risk}
\end{figure}

图\ref{fig:ch02-spiral-risk}中的四个象限按顺时针形成闭环。水利项目不应为了“使用螺旋模型”而把所有需求都放进高成本试验；应先挑选不确定性最高、失败代价最大的风险作为本轮对象。一个普通的报表字段可以沿增量路线实现，而闸门控制接口联调则需要单独建立风险轮次，先验证通信、权限、超时和回退，不把未验证的控制动作带入生产环境。

以“闸门控制接口联调”为高风险轮次，可以把四个象限具体化。第一轮目标是确认只读状态、控制指令、回执和超时的契约；风险分析把现场网络中断、设备拒绝、重复指令和权限过期列为高影响项；开发验证阶段使用仿真设备和录制报文完成重试、幂等和回退测试；评审阶段由审批人确认安全边界，决定下一轮是否进入隔离环境。第二轮再增加硬件在环测试和故障注入，第三轮才讨论小范围受控试运行。每轮都要把未解决风险带入下一轮，而不是用“测试通过”掩盖尚未验证的环境差异。

螺旋模型的成本来自风险分析、试验环境和评审治理。若团队没有记录风险概率、影响和验证证据的能力，螺旋会退化成反复返工；若每轮都扩大范围而没有退出条件，迭代也无法收敛。教学案例中可以要求学生为每轮填写“风险—证据—决策”三列表，并说明哪一项证据足以把风险从高降为中，哪一项风险必须保留为上线前置条件。

\subsection{敏捷与迭代交付}

敏捷强调通过短周期、可运行软件和持续反馈适应变化\cite{agile2001}。业务用户和客户代表应持续参与评审，并对需求优先级和验收结果拥有决策权；跨职能交付团队据此拆分增量、演示成果并记录反馈。运维员可提供运行约束和故障数据，但敏捷的成立取决于持续反馈与可决策的协作机制。水利项目采用敏捷时，仍需保留法规、标准、架构、安全和验收所要求的文档与审批。

敏捷并不等于取消计划，而是把计划分成不同时间尺度。产品目标和范围基线回答“本阶段为什么做、哪些内容不在范围内”；发布计划回答“若干个迭代后要交付什么”；Sprint计划回答“本周期用什么可验证结果证明完成”。需求基线允许在评审后变更，但变更必须有编号、理由、影响和批准人，不能因为用户在演示现场提出一句意见就直接插入正在运行的Sprint。若变更涉及安全、数据结构或设备接口，应先进入评审队列，必要时用原型或风险轮次验证后再排入Backlog。

\subsection{Scrum与看板的落地}

Scrum把待办项按优先级组织成产品Backlog，团队在固定长度的Sprint中选择一组目标明确的条目，周期结束时演示可运行增量并根据反馈调整Backlog。产品负责人维护业务价值和优先级，Scrum Master帮助团队移除流程障碍，跨职能开发团队负责设计、实现、测试和文档。本书案例不把角色理解成行政职位：同一个人可以承担多个职责，但“谁能决定优先级”和“谁能确认验收”必须写进项目约定。

案例灌区的一个Sprint可以只交付“查看某地块最近一周用水计划与审批状态”。条目的验收条件包括：未登录用户看不到计划详情；审批人能看到退回原因；时间范围和时区显示一致；接口失败时页面给出重试入口；测试数据中存在资料不全和重复提交两条异常流。Sprint评审展示的是可运行链路和证据，不是静态页面截图；回顾会议则记录哪些假设被证伪、哪些接口等待外部团队、哪些自动化测试需要补充。

看板适合处理持续流入的故障、数据质量问题和运维任务。列可以设置为“待澄清—就绪—开发—验证—待发布—完成”，每列设定在制品上限，避免所有人同时开始新任务而没有任务真正完成。高风险控制接口可设置独立泳道和审批门，普通报表缺陷不能挤占它的验证资源。看板卡片至少包含来源、业务影响、责任人、验收证据和截止条件；卡片移动代表状态改变，不能用颜色代替文字状态。

Sprint和需求基线共存的关键是分层管理。基线约束系统边界、关键对象、接口兼容性、安全和合规；Backlog承载在边界内的可变细节。发生跨边界变更时，先更新基线版本和影响矩阵，再重新拆分待办项；只改变页面排序或提示文案的变更，可以在Sprint内完成并补充验收记录。这样既保留敏捷的反馈速度，又避免局部需求逐渐侵蚀整体架构。

\subsection{DevOps与持续交付}

DevOps把开发、测试、发布和运行看作一条有反馈的交付链。对水利平台而言，持续集成至少要完成代码编译、单元测试、接口契约检查、数据库迁移演练和安全扫描；持续交付还要生成可追踪的制品、记录配置版本、执行部署前审批，并提供可验证的回滚路径。自动化的价值不是“每天发布”，而是让每一次变更都能在较小范围内获得证据，出现问题时可以定位到提交、配置和数据迁移。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=6mm and 7mm,
  devstep/.style={draw=Primary,rounded corners,fill=PrimaryLight,align=center,
    minimum width=2.6cm,minimum height=.82cm,text=PrimaryDark}]
\node[devstep] (plan) {计划与Backlog};
\node[devstep,right=of plan] (code) {编码与评审};
\node[devstep,right=of code] (build) {构建与测试};
\node[devstep,below=of build] (release) {发布与审批};
\node[devstep,left=of release] (operate) {运行与监控};
\node[devstep,left=of operate] (learn) {反馈与改进};
\draw[->,thick,draw=Primary] (plan)--(code)--(build)--(release)--(operate)--(learn);
\draw[->,thick,dashed,draw=Accent] (learn)--(plan);
\end{tikzpicture}
\caption{水利平台的DevOps持续交付反馈环}
\label{fig:ch02-devops-loop}
\end{figure}

图\ref{fig:ch02-devops-loop}中的“运行与监控”不是交付终点。平台应把接口错误率、消息延迟、数据缺测率、模型运行失败和人工工单回执反馈到Backlog；当指标超过阈值时，先触发值班员和运维员的响应流程，再决定是否回滚或创建修复迭代。部署脚本、环境变量和密钥引用要与制品分离，生产配置不能通过手工改文件完成，否则即使代码可追踪，实际运行状态仍无法复现。

持续交付要设置分层门禁。合并请求阶段检查格式、单元测试和静态分析；集成环境检查接口、数据库迁移、消息幂等和权限；预发布环境使用脱敏数据进行性能、故障注入和回滚演练；生产发布由审批人确认变更范围、风险、观察窗口和回退条件。对于闸门控制等高风险能力，自动化流水线只负责构建和验证，实际下发仍需独立的授权、双人复核和设备侧安全策略。把所有动作都自动化并不等于把所有决策交给脚本。

\subsection{其他模型的适用边界}

喷泉模型允许需求、设计、实现和测试在相邻活动之间重叠，适合面向对象开发中模型逐步细化的场景，但需要清楚记录每次重叠产生的接口和评审结果。基于知识的模型适合已有成熟产品线、规范模板和可复用构件的项目，关键风险是知识库假设与当前工程不一致；变换模型强调从形式化规格逐步变换为实现，适合算法和规则可严格描述的局部模块，但前期建模成本较高。这些模型可以作为局部活动的组织方式，不必强行作为全平台唯一生命周期。

\subsection{模型选择}

选型不是挑一个名字，而是先判断风险集中在哪里，再确认团队能否承担该模型要求的治理成本。表\ref{tab:lifecycle-comparison}把前面几种模型放在需求稳定性、优势与风险三个维度下并列比较：螺旋模型要求成熟的风险分析能力，敏捷要求客户代表持续在场并能拍板，这些都是前提条件而非可选项，缺了前提，模型本身并不会自动生效。

\begin{table}[htbp]
\centering
\caption{生命周期模型选择对照}
\label{tab:lifecycle-comparison}
\begin{tabular}{p{2.0cm}p{3.4cm}p{3.8cm}p{3.4cm}}
\toprule
模型 & 适合情形 & 主要优势 & 主要风险 \\
\midrule
瀑布 & 需求稳定、审查严格 & 阶段和文档清楚 & 反馈较晚 \\
原型 & 交互或展示需求模糊 & 快速澄清理解 & 原型被误作产品 \\
增量/迭代 & 能力可拆分、需要早交付 & 小步验证、降低集成风险 & 整体架构被局部需求侵蚀 \\
螺旋 & 风险高、方案不确定 & 显式管理风险 & 成本与治理要求高 \\
敏捷 & 需求变化快、用户可持续参与 & 反馈频繁、适应变化 & 合规和架构治理被忽视 \\
\bottomrule
\end{tabular}
\end{table}

案例灌区项目可采用“总体基线+增量交付”：先按合同和行业约束确认范围、数据与安全基线，再按两到三周迭代交付用水计划、审批、配水和执行反馈。设备控制接口单独设置严格评审和联调窗口。

模型选择应把项目特征写成可讨论的证据，而不是凭团队偏好决定。需求稳定性关注业务规则是否已经由规程和合同确定；技术风险关注设备、模型、坐标和接口是否有成熟验证；反馈频率关注值班员、审批人和外部数据方能否持续参与；合规强度关注哪些交付物必须审查和留档；交付压力关注是否需要尽早让用户使用一部分能力。表\ref{tab:ch02-model-selection}给出四类典型特征组合和推荐策略，推荐不是绝对答案，最后一列的前置条件必须在项目启动评审中逐条确认。

\begin{table}[htbp]
\centering
\caption{水利软件项目特征与生命周期模型选择}
\label{tab:ch02-model-selection}
\begin{tabular}{p{3.0cm}p{3.8cm}p{3.5cm}p{3.5cm}}
\toprule
项目特征 & 主要证据 & 推荐模型或组合 & 进入条件与风险控制 \\
\midrule
规则稳定、审查严格 & 规程、合同、接口和验收条款已确认 & 瀑布或总体基线+增量 & 阶段评审、变更控制、早期接口样例 \\
交互和展示不确定 & 用户难以描述空间与报表需求 & 原型+增量 & 明确原型退出条件，原型与生产架构隔离 \\
能力可拆分且需早交付 & 查询、计划、审批、报表可独立验收 & 增量+敏捷 & 先定对象和接口，持续集成和回归测试 \\
设备或控制风险高 & 通信、权限、回退和安全边界未知 & 螺旋+受控联调 & 风险轮次、仿真/硬件在环、审批和停止条件 \\
\bottomrule
\end{tabular}
\end{table}

两个选型案例可以帮助区分“功能多少”和“风险在哪里”。水库安全监测系统若传感器协议、测点编码和报表格式已经由现行规程确定，可采用总体基线加增量：先完成观测查询和质量码，再交付趋势、预警和工单；若新增三维场景或模型预演存在较大不确定性，则用原型验证交互，避免把探索性代码直接当产品。智慧灌区管理平台的用水计划和审批规则通常可以从规程整理为稳定基线，但不同管理站对看板、地图和移动端操作的偏好差异较大，可采用“基线+Scrum”：先冻结数据、权限和审批状态，再用短Sprint验证页面和反馈；涉及闸门自动控制的部分独立进入螺旋风险轮次。

选型评审的输出至少包括：项目特征证据、模型组合、每个模型的前置条件、第一轮可验证交付物、主要风险、审批人和模型切换触发条件。项目进行中如果用户参与频率下降、接口风险暴露或合规范围扩大，应重新评估模型，而不是为了保持原计划继续沿用不再适合的流程。模型本身是治理工具，能够解释“为什么这样组织工作、如何知道组织方式有效”，才算完成选型。

实际评审可以采用“先排除、再组合、后试验”的顺序。先排除无法满足合规、现场条件或团队能力的模型；再把全局基线、局部原型、增量交付和风险轮次组合起来；最后用一个两周左右可观察的试验验证假设。试验不以代码行数为完成标准，而以是否获得关键证据为标准，例如接口是否能在断网后安全恢复、审批人能否独立完成退回与再提交、数据质量问题能否被追溯。试验结束后将结果写入选型记录，明确继续、调整或停止的决定，下一次评审直接引用这些证据。

选型记录还应说明“不选什么以及为什么”。例如，灌区自动控制若缺少设备仿真环境，就不能仅因为团队熟悉敏捷而把控制指令放入普通Sprint；监测报表若需求稳定，也不必为追求快速反馈而搭建昂贵的螺旋风险治理。把排除理由写出来，可以防止项目换人后再次重复争论，并让审批人看到时间、成本和安全边界之间的取舍。模型选择最终应落到可追踪的交付物、风险证据和变更门槛，而不是停留在方法名称的比较。

评审记录还应标出参与者、评审日期和所依据的基线版本，下一轮评审先核对这些字段，确认各方比较的是同一范围，再讨论模型是否需要调整。记录中还要写明模型选择后的第一个可验证交付物、风险负责人、预期证据、观察窗口和停止条件；若证据不足，就延长验证或退回组合设计，不能用“进度已完成”替代模型有效性。这样，选型记录不仅解释今天为何采用某种流程，也支持后续换人、换设备或扩大试点范围时复盘，避免把一次偶然成功误当成普遍适用的过程经验。

在水利项目中，过程模型还要和合同、招采及安全审查节奏对齐。涉及闸门控制或防洪调度的需求，即使采用短周期迭代，也应把设计审查、联调试验和上线审批列为不可跳过的里程碑；普通报表功能则可在同一基线下快速迭代。项目负责人据此安排不同节奏的工作流，并在每次评审时检查证据是否覆盖业务、技术和安全三类目标。

\section{需求获取与理解}

\paragraph{本节层次}核心：2.3.1、2.3.2、2.3.4、2.3.5、2.3.6；指导实践：2.3.3。核心路线只要求读完核心小节。

需求获取发生在深入分析之前。团队先理解业务语言、责任和当前流程，再判断哪些意见需要转化为系统需求。

\subsection{干系方与业务场景}

案例灌区的关键干系方包括用水户、管理站（含值班员）、调度人员、审批人、计量维护人员、运维员和外部数据提供方。每类角色拥有不同目标和权限；“用户”不能作为一个笼统角色。

业务场景使用“触发—角色—前置条件—主流程—异常流—结果”描述。例如：

\begin{quote}
灌溉季开始前，用水户提交分时用水计划；管理站检查地块、作物、需水量和渠道能力。资料不全时退回补充；通过后进入调度审批。批准计划生成配水任务，执行流量与计划偏差超过容限时触发复核。
\end{quote}

\subsection{需求获取方法}

访谈适合了解职责与例外；现场观察适合发现实际操作与制度文本的差异；工作坊适合跨部门澄清术语与边界；问卷适合大范围收集偏好；文档分析适合提取规程、表单、接口和历史问题；原型适合验证交互与可视化。团队应组合方法并记录来源。

每条原始意见保留提出人、时间、业务理由和关联资料。例如，不能只写“系统应支持快速查询”，而应追问查询对象、时间范围、并发人数、可接受响应时间、数据完整性和失败提示。

\subsection{需求获取方法的组合与陷阱}

单一方法只能看到业务的一部分。访谈能听见角色的解释，现场观察能看到实际操作，问卷能发现不同站点的共性，文档分析能固定制度边界，原型则能让难以言明的交互变得可讨论。项目组应先根据问题选择方法，再用第二种方法交叉验证，最后把原始证据、解释和待确认假设分开记录。表\ref{tab:ch02-elicitation-methods}把五种方法的适用场景、产出和典型陷阱放在同一张表中，便于制定一次调研的组合方案。

\begin{table}[htbp]
\centering
\caption{水利项目需求获取方法的适用场景与典型陷阱}
\label{tab:ch02-elicitation-methods}
\begin{tabular}{p{2.2cm}p{3.5cm}p{3.5cm}p{3.2cm}}
\toprule
方法 & 适用场景 & 主要产出 & 典型陷阱与对策 \\
\midrule
访谈 & 澄清职责、例外和决策依据；适合值班员、审批人与运维员 & 角色目标、业务规则、异常叙述、待确认问题 & 只访谈主管导致一线细节缺失；应按角色分层并回放纪要 \\
问卷 & 多管理站或多类用水户的偏好和频率调查 & 选项分布、排序结果、站点差异 & 选项预设造成引导；保留“其他”并抽样追访 \\
观察 & 发现交接、临时表格、手工计算和网络盲区 & 操作时间线、环境约束、未言明步骤 & 被观察者改变行为；应跨班次、跨天气复核 \\
原型 & 验证地图、表单、筛选和审批路径等交互理解 & 页面反馈、操作任务、可用性问题 & 只评价颜色和布局；演示时同步走功能、权限和异常流 \\
文档分析 & 提取规程、表单、接口协议、历史工单与审计要求 & 术语表、约束、字段和合规来源 & 把旧表单当现行规则；记录版本、发布日期和适用范围 \\
\bottomrule
\end{tabular}
\end{table}

以案例灌区为例，第一轮可用文档分析建立“用水计划、可供水量、审批状态”的术语表，再访谈审批人与值班员，确认计划退回的真实原因；第二轮到管理站观察高峰申报，记录网络不稳定时的纸面补录；第三轮用低保真原型让用水户完成一次计划填写。三轮证据的粒度不同，不能把观察到的偶然动作直接写成所有站点都必须遵守的功能需求。对每项意见标记“事实、解释、假设、决策”四种来源，并由提出角色和审批人共同确认，才能形成可追踪基线。

访谈的坑常常不是问题太少，而是问题带着答案。与审批人交谈时，应先让对方按最近一次真实计划复盘，再追问“资料不足时谁做了什么、依据什么判断、结果如何留痕”，而不是直接问“是否需要一个退回按钮”。问卷的坑是把频率当重要性，十个站点都选择“常用”并不代表该功能比安全校核更优先；应同时收集后果、时限和替代办法。观察的坑是只看正常班次，暴雨、夜班和设备离线时的处理往往才决定系统边界。原型的坑是把可点击演示误认为已验证的后端能力，必须在原型旁标出模拟数据、未验证接口和退出条件。文档分析的坑是忽略修订版本，任何规则都要写出来源文件、章节、发布日期和适用范围。

为避免证据在团队之间丢失，可把一次调研组织成“准备—采集—复核—入库”四步。准备阶段列出角色、问题和预计证据；采集阶段保存录音摘要、问卷原始数据、观察时间线或原型版本；复核阶段把结论回放给参与者，允许纠正误解；入库阶段为需求分配编号，关联来源、优先级、验收准则和风险。图\ref{fig:ch02-requirement-evidence}展示了这条从原始证据到需求基线的过滤链，重点是把尚未证实的假设留在待确认区，而不是悄悄写成系统承诺。

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[node distance=7mm and 10mm,
  ev/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.8cm,minimum height=.7cm,align=center},
  gate/.style={draw=Accent,diamond,aspect=2.1,fill=Accent!10,align=center,inner sep=1pt}]
\node[ev] (raw) {原始证据\\访谈·观察·文档};
\node[gate,right=of raw] (cross) {交叉验证？};
\node[ev,right=of cross] (req) {需求条目\\编号与来源};
\node[ev,below=of req] (base) {需求基线\\验收与追踪};
\node[ev,below=of cross] (assume) {待确认假设\\补证据或退回};
\draw[->,thick] (raw)--(cross);
\draw[->,thick] (cross)--node[above,font=\scriptsize]{是}(req);
\draw[->,thick] (cross)--node[left,font=\scriptsize]{否}(assume);
\draw[->,thick] (req)--(base);
\draw[->,thick] (assume) -| (raw);
\end{tikzpicture}
\caption{需求证据经过复核进入需求基线}
\label{fig:ch02-requirement-evidence}
\end{figure}

需求获取工作坊适合处理跨角色术语不一致的问题。主持人先给出同一条业务事件，例如“灌溉期内申请量超过可供水量”，请用水户、值班员、审批人和运维员分别写出触发条件、看到的数据、能够执行的动作以及需要留下的证据；随后把卡片按时间顺序排列，标出责任交接和等待点。工作坊的产物不是一张人人都同意的流程图，而是一组明确的分歧：谁拥有批准权、哪一个数据版本具有约束力、网络断开时是否允许保存草稿、何时必须升级给专业分析员。主持人把分歧转成待确认问题，安排现场观察、规则核对或原型演示，避免在会上用投票把安全约束稀释掉。

\subsection{需求分类与优先级}

需求可分为业务需求、用户需求、功能需求、质量属性、数据与接口需求以及合规约束。分类帮助发现遗漏，但不能代替优先级决策。

MoSCoW方法把需求分为Must、Should、Could和本次Won't。Must表示没有它就无法验收、无法合规或无法安全运行；Should具有高价值但存在可接受替代；Could在时间允许时实现；Won't表示明确不进入当前范围。优先级必须记录理由、依赖和批准人，并在迭代或变更时复核。表\ref{tab:moscow-example}按这四档整理了案例灌区的一组需求。读表时应重点看“理由”一列：同样标为Must，“缺了它无法通过合规验收”与“用户呼声很高”是完全不同的依据，只有前者在范围压缩时不可让步。

\begin{table}[htbp]
\centering
\caption{案例灌区需求优先级示例}
\label{tab:moscow-example}
\begin{tabular}{p{2.0cm}p{6.6cm}p{4.0cm}}
\toprule
级别 & 需求 & 理由 \\
\midrule
Must & 批准计划前校验渠道能力与可供水量 & 防止生成不可执行任务 \\
Must & 所有审批和调整保留人员、时间与理由 & 责任和审计要求 \\
Should & 地图联动显示地块与配水状态 & 提升研判效率，可由列表降级 \\
Could & 语音输入查询条件 & 非核心便利功能 \\
Won't & 本期自动下发闸门控制 & 控制安全论证不在当前范围 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{用例规约与用户故事}

用例规约适合描述一个完整业务目标，必须把角色、前置条件、主成功场景、扩展流、异常流和后置条件写全。前置条件说明开始前必须成立的事实，主成功场景只写在所有条件满足时的正常步骤；扩展流处理可恢复的分支，异常流说明拒绝、超时或数据损坏等失败结果，后置条件则规定系统留下什么状态和证据。表\ref{tab:ch02-usecase-template}给出可复用的规约模板，编号后的每一步都可以关联接口、页面或测试用例。

\begin{table}[htbp]
\centering
\caption{用水计划审批用例规约模板与案例灌区实例}
\label{tab:ch02-usecase-template}
\begin{tabular}{p{2.6cm}p{10.0cm}}
\toprule
字段 & 实例内容 \\
\midrule
用例名称/目标 & 提交并审批用水计划；让审批人依据可供水量校核后形成可执行计划 \\
参与者 & 用水户、审批人、值班员；系统调用渠道能力与计量数据服务 \\
前置条件 & 用水户已登录并具备目标地块权限；当前灌溉期开放；渠道与水量基线版本可查询 \\
主成功场景 & 1.用水户填写时段、地块和申请量；2.系统校验字段与重复提交；3.系统计算需求量并展示依据；4.审批人复核可供水量；5.审批通过后生成配水任务并记录审计信息 \\
扩展流 & 2a.资料缺失，系统标出字段并退回补充；4a.可供水量不足，审批人调整时段或申请量后重新校核 \\
异常流 & 网络中断、接口超时或基线版本失效时不生成任务，保存草稿和错误编号，恢复后由用户确认重试 \\
后置条件 & 成功时计划状态为“已批准”、任务拥有唯一编号；失败时无执行任务，所有输入、校核结果和处理人可追溯 \\
\bottomrule
\end{tabular}
\end{table}

规约完成后再把目标拆成用户故事，故事保留“角色—能力—价值”三部分，例如：“作为用水户，我希望看到申请量与可供水量的差额，以便在提交前调整计划，减少退回次数。”故事不是口号，必须附带验收准则。Given描述上下文，When描述一个可执行动作，Then描述可观察结果；异常和权限也应有独立准则。以同一故事为例：Given用水户已选择有权限的地块且可供水量为已发布版本，When提交不超过可供水量的计划，Then系统显示校核依据、生成待审批编号并保留提交时间；Given申请量超过可供水量，When用水户提交，Then系统拒绝生成执行任务、指出差额并允许修改；Given用户没有该地块权限，When打开计划，Then系统不返回敏感数据并记录拒绝原因。每条准则都可以转成接口测试或页面验收步骤，避免“体验良好”这类无法判定的表述。

用例规约还要划清系统边界。审批人“查看水量平衡报告”属于系统责任，专业分析员解释异常原因属于人的责任，渠道调度部门确认可执行时段则是外部协作。把人的判断伪装成自动规则，会导致需求无法验收；把系统能稳定完成的校核全部留给人工，又会造成重复劳动。编写每一步时可问三个问题：输入由谁提供、系统产生什么可观察输出、下一步由哪个角色承担责任。若答案含有“适当”“及时”“必要时”等模糊词，就继续追问时间窗、判据和交接证据，并将剩余不确定性登记为风险。

用户故事的粒度也需要控制。一个故事应在一个迭代内完成并能独立验收，若同时包含数据接入、地图渲染、审批和消息通知，团队很难判断失败发生在哪一层。可以先写成较大的史诗“管理站能够完成用水计划闭环”，再拆成“保存草稿”“提交计划”“校核可供水量”“审批发布”“查看执行偏差”等故事，每个故事都保留同一个计划编号和状态机上下文。拆分后仍要检查端到端链路，不能为了缩短故事而把关键的权限校验或审计记录放到“以后再补”。验收准则应覆盖正常、拒绝、重复提交和恢复重试四类路径，并明确测试数据的质量状态。

\subsection{非功能需求的分类与量化}

非功能需求描述系统在什么质量水平下完成工作，不能因为没有一个按钮就被忽略。水利平台至少要分别考虑性能、可用性、可靠性、数据质量、安全与审计、可维护性、可扩展性和可观测性。分类的价值在于找到责任人和测试证据；量化的关键是给出对象、负载、时间窗、阈值和统计口径。表\ref{tab:ch02-nfr-metrics}将“及时、稳定、安全、易维护”等模糊词翻译为可测指标。

\begin{table}[htbp]
\centering
\caption{案例灌区非功能需求的分类与量化示例}
\label{tab:ch02-nfr-metrics}
\begin{tabular}{p{2.4cm}p{3.8cm}p{3.4cm}p{3.2cm}}
\toprule
类别 & 需求表达 & 指标与口径 & 验证证据 \\
\midrule
性能 & 查询最近一日计量点用水趋势应及时返回 & 95百分位响应时间不超过2秒；说明并发数、数据范围与缓存状态 & 压测报告、慢查询样本 \\
可用性 & 值班员在设备短暂离线时仍可查看最近可信数据 & 月度服务可用率、降级持续时间和恢复时间分别统计 & 监控记录、故障演练 \\
可靠性 & 重复消息不能重复生成配水任务 & 相同事件标识的处理结果一致；重试窗口覆盖最大重投周期 & 幂等测试、消息消费记录 \\
数据质量 & 质量状态必须随测值进入查询与统计 & valid、suspect、missing的比例和缺测连续时长有明确口径 & 数据剖面、抽样复核 \\
安全审计 & 审批与调整可追溯到人、时间、理由和版本 & 审计记录完整率100\%；越权请求均有拒绝记录 & 审计抽查、权限测试 \\
可维护性 & 规则与接口变更不依赖大范围改代码 & 构建、迁移和回滚步骤可重复；关键模块有自动化测试 & 发布记录、回滚演练 \\
\bottomrule
\end{tabular}
\end{table}

量化时要同时写“测什么”和“怎么测”。例如“系统支持高并发”没有说明用户数、请求类型和时间窗，无法比较结果；改成“在脱敏的最近一日数据集、200个并发查询、每个查询返回同一测点集合的条件下，95百分位响应时间不超过2秒，错误率低于0.1\%”，测试人员才知道如何构造场景。可用性指标还应区分计划维护和非计划中断，可靠性应说明一次失败后的重试与重复提交，安全指标应给出授权边界、审计留存期限和抽查比例。所有阈值都要标注来源、责任人和复核日期；若是试点假设，则在验证后更新基线，而不能把一次压测结果当成永久承诺。

非功能需求之间可能相互牵制。把缓存时间拉长可以降低响应时间，却可能让值班员看到过期状态；把审计字段全部写入同步事务可以提高可追溯性，却会增加高峰延迟。分析人员应把目标、约束和取舍写成同一条决策记录，说明哪些指标是硬门槛，哪些指标可以在降级模式下暂时放宽，并给出恢复条件。这样，非功能需求就能进入架构、测试和运行监控，而不是停留在规格说明书的形容词列表。




非功能指标的基线要与业务容量一起维护。监测数据接入量、查询时间范围、并发角色数、消息重投次数和审计保留期限改变时，原有阈值可能不再适用。需求评审应把容量假设、采样方法、统计分位数和环境配置写入版本记录；压测只在单机环境通过，不能直接推断生产集群的可用性。运行阶段还要把指标转成监控告警和复盘问题，例如响应时间连续三个窗口超过阈值时记录慢查询与缓存命中率，设备离线超过规定时长时核对降级页面是否仍显示数据质量状态。需求、测试、监控三者使用同一编号，才能在故障后追溯是阈值不合理、实现退化还是现场条件变化。

在评审结束时，主持人应把每条需求的状态明确标为“已确认、待补证据、暂缓或拒绝”，并说明下一步动作。待补证据项必须有责任人和截止时间，暂缓项要说明进入哪个版本或迭代，拒绝项要保留业务理由和替代路径。状态字段与版本号一起展示，避免同一条意见在不同会议纪要中重复出现，也便于后续把变更影响传递给设计、测试、培训和运维。

\section{需求分析的任务与原则}

\paragraph{本节层次}核心：2.4.1、2.4.2、2.4.3。

需求分析把获取到的材料转为一致、完整、可行、可验证的需求集合。

\subsection{核心任务}

\paragraph{明确边界。} 定义系统内外部角色、设备、外部平台和人工活动，避免把组织管理问题全部推给软件。

\paragraph{建立对象和数据语义。} 统一地块、渠道、闸门、用水计划、配水任务和执行记录的身份、关系、单位与时间。

\paragraph{描述功能与异常流。} 除正常申报审批外，还要覆盖重复提交、资料缺失、可供水不足、设备离线、计划过期和撤回。

\paragraph{量化质量属性。} 把“及时”“稳定”“安全”转为响应时间、可用率、恢复目标、权限规则和审计留存等可验证指标。

\paragraph{定义接口与约束。} 说明外部数据来源、协议、频率、责任边界、错误处理和版本策略。

\paragraph{建立可追踪关系。} 需求连接到来源、设计、接口、测试和验收证据，变更时能够评估影响。

\subsection{基本原则}

需求应当必要、无歧义、一致、可行、可验证、可追踪并具有优先级\cite{wiegers2013,robertson2012}。一条需求尽量表达一个可识别义务，使用“应”描述必须满足的行为，避免“可能包括”“尽可能”“用户友好”等模糊措辞。

需求还要区分事实、假设与设计决定。例如“灌区现有闸门控制接口只读”是现状约束，“网络可用率不低于某值”是待确认假设，“采用PostgreSQL”是架构决定，三者不能都伪装成业务需求。

\subsection{需求验证与冲突处理}

团队通过评审、原型、模型走查和验收用例验证需求。验证既要检查文字是否完整，也要把需求放进真实业务场景，观察角色、数据、权限、异常和后置状态能否闭合。冲突由业务价值、安全、合规、成本和依赖共同决策，并把证据、参与者和批准人写入记录。变更后更新需求版本、影响分析和批准记录。表\ref{tab:requirement-quality-check}把这些要求整理成逐条可勾选的检查项，用于评审前自查。它的作用是把“需求写得好不好”这种主观判断，换成“这一条能否被测试用例验证、是否只有唯一解释、能否追溯到提出人与来源”这类当场就能回答的问题。

案例灌区有一个典型冲突：用水户希望提交后立即得到批准，以便安排劳动力和设备；审批人和专业分析员则要求先完成水量平衡校核，确认申请量、渠道能力和已批准计划不会超出可供水量。处理这类冲突不能简单地选择某一方的按钮速度，而要先拆出双方真正要解决的问题。用水户需要的是“尽快知道结果”，调度侧需要的是“结果可执行且可追溯”。因此，需求可以组合为两段式反馈：提交后立即返回受理编号、缺失字段和预计校核状态；水量平衡未完成前只提供“待校核”状态，不生成执行任务；校核通过后由审批人确认，系统再发布已批准计划。这样满足了反馈及时性，又把安全边界放在执行动作之前。

冲突决策按三层顺序记录。第一层是安全与合规：涉及可供水量、闸门控制、权限越界和审计留痕的约束属于硬门槛，任何快捷路径都不能绕过。第二层是业务价值：在满足硬门槛后，优先支持能减少退回、提高计划可执行性和降低值班负担的能力。第三层是成本与依赖：若两种实现都满足前两层，再比较开发量、运维复杂度和对既有接口的影响。评审记录应列出被保留的需求、被拆分或延后的部分、替代方案、验证证据和下次复核触发条件；若事实不足，就把结论标为假设并安排观察或原型试验，而不是把争论隐藏在一句“按实际情况处理”里。

\begin{table}[htbp]
\centering
\caption{需求质量检查清单}
\label{tab:requirement-quality-check}
\begin{tabular}{p{2.4cm}p{7.2cm}p{3.0cm}}
\toprule
属性 & 检查问题 & 证据 \\
\midrule
必要 & 不实现会损害哪个业务目标或约束？ & 业务理由 \\
无歧义 & 角色、对象、条件、动作和结果是否唯一？ & 场景走查 \\
一致 & 是否与术语、数据、接口或其他需求冲突？ & 冲突记录 \\
可行 & 技术、成本、进度和现场条件是否允许？ & 可行性验证 \\
可验证 & 能否设计明确的通过/失败判据？ & 验收用例 \\
可追踪 & 能否定位来源、设计和测试？ & 追踪矩阵 \\
\bottomrule
\end{tabular}
\end{table}

\section{结构化分析}

\paragraph{本节层次}核心：2.5.1、2.5.2、2.5.3、2.5.4；指导实践：2.5.5、2.5.6、2.5.7。核心路线只要求读完核心小节。

结构化分析从系统边界出发，自顶向下分解处理、数据流、数据存储和外部实体。它适合说明业务信息如何流动，不用于表达所有时间、控制和组织关系。复杂控制状态可在后续设计中使用状态机等方法补充。

\subsection{系统流程图}

系统流程图关注业务步骤、判断、输入输出和责任衔接。图\ref{fig:order-process-flowchart}描述用水计划从申报到执行反馈的闭环：资料不完整时从“资料完整？”退回“提交用水计划”；审批不通过时返回“修订计划”；审批通过后才生成执行任务并接收回执。这样的顺序让每个节点都有明确责任人与可审计状态。

绘制流程图之前先统一符号，否则同一个菱形在不同人笔下可能表示条件判断，也可能表示人工审核环节，图就失去了作为交流依据的价值。表\ref{tab:system-flow-symbols}给出本书采用的符号约定。

\begin{longtable}{p{2.5cm}p{3.2cm}p{6.6cm}}
\caption{系统流程图常用符号}\label{tab:system-flow-symbols}\\
\toprule
符号 & 名称 & 用途 \\
\midrule
\endfirsthead
\toprule
符号 & 名称 & 用途 \\
\midrule
\endhead
圆角矩形 & 开始/结束 & 表示流程入口、正常结束或受控终止 \\
矩形 & 处理 & 表示申报、校验、审批、生成任务等活动 \\
菱形 & 判断 & 表示资料是否完整、方案是否批准等条件 \\
平行四边形 & 输入/输出 & 表示表单输入、报告输出或外部消息 \\
箭头 & 流向 & 表示业务执行顺序；回退箭头应标明条件 \\
\bottomrule
\end{longtable}

按上述符号约定绘制的用水计划流程见图\ref{fig:order-process-flowchart}。图中两条回退箭头都标注了触发条件，这正是流程图区别于示意图的地方：回退路径必须说清在什么条件下发生、退回到哪个节点，否则读图的人无法判断异常情况该由谁接手。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=6mm and 12mm,
  term/.style={draw=Primary,rounded corners=8pt,fill=PrimaryLight,minimum width=1.9cm,
    minimum height=.65cm,align=center,font=\small},
  proc/.style={draw=Primary,fill=PrimaryLight,minimum width=2.5cm,
    minimum height=.75cm,align=center},
  decision/.style={diamond,draw=Accent,fill=Accent!10,aspect=2.0,align=center,inner sep=1pt}]
\node[term] (start) {开始};
\node[proc,below=of start] (submit) {提交用水计划};
\node[decision,below=of submit] (complete) {资料完整？};
\node[proc,below=of complete] (check) {校核需水与渠道能力};
\node[decision,below=of check] (approve) {批准？};
\node[proc,below=of approve] (task) {生成配水任务};
\node[proc,below=of task] (execute) {执行与计量回传};
\node[proc,right=of complete] (supplement) {补充资料};
\node[proc,right=of approve] (revise) {修订计划};
\node[proc,left=of execute] (review) {偏差复核};
\node[term,below=of execute] (end) {结束};
\draw[->,draw=Primary,thick] (start)--(submit);
\draw[->,draw=Primary,thick] (submit)--(complete);
\draw[->,draw=Primary,thick] (complete)--node[left,font=\scriptsize]{是} (check);
\draw[->,draw=Primary,thick] (complete)--node[above,font=\scriptsize]{否} (supplement);
\draw[->,draw=Accent,thick] (supplement) |- (submit);
\draw[->,draw=Primary,thick] (check)--(approve);
\draw[->,draw=Primary,thick] (approve)--node[left,font=\scriptsize]{是} (task);
\draw[->,draw=Primary,thick] (approve)--node[above,font=\scriptsize]{否} (revise);
\draw[->,draw=Accent,thick] (revise) |- (check);
\draw[->,draw=Primary,thick] (task)--(execute);
\draw[->,draw=Primary,thick] (execute)--(review);
\draw[->,draw=Primary,thick] (execute)--(end);
\draw[->,draw=Accent,dashed,thick,bend left=30] (review) to node[above,font=\scriptsize]{形成下一周期依据} (submit);
\end{tikzpicture}
\caption{用水计划申报、审批、配水与反馈流程}
\label{fig:order-process-flowchart}
\end{figure}

\subsection{数据流图}

数据流图（DFD）只表达数据如何在外部实体、处理和数据存储之间流动。上下文图把系统视为一个处理，先确定边界；再逐层分解，但父图输入输出必须与子图保持平衡。图\ref{fig:dfd-level-zero}是本例的上下文图：整个系统被画成单个处理，围绕它的外部实体确定了系统边界，后续任何一层分解都不能凭空引入图中没有出现的外部实体。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=12mm and 23mm,
  entity/.style={draw=Primary,fill=PrimaryLight,minimum width=2.3cm,minimum height=.8cm,align=center},
  process/.style={ellipse,draw=Accent,fill=Accent!10,minimum width=3.0cm,minimum height=1.2cm,align=center}]
\node[entity] (farmer) {用水户};
\node[process,right=of farmer] (system) {灌区用水计划\\与配水系统};
\node[entity,right=of system] (manager) {调度与审批人员};
\node[entity,below=of system] (meter) {计量与闸门系统};
\draw[->,draw=Primary,thick] (farmer)--node[above,font=\scriptsize]{申报/补充资料} (system);
\draw[->,draw=Primary,thick] (system)--node[below,font=\scriptsize]{状态/批准计划} (farmer);
\draw[->,draw=Primary,thick] (manager)--node[above,font=\scriptsize]{审批/调整} (system);
\draw[->,draw=Primary,thick] (system)--node[below,font=\scriptsize]{待办/分析结果} (manager);
\draw[->,draw=Primary,thick] (system)--node[right,font=\scriptsize]{配水任务} (meter);
\draw[->,draw=Primary,thick] (meter)--node[left,font=\scriptsize]{执行回执} (system);
\end{tikzpicture}
\caption{灌区用水计划与配水系统上下文图}
\label{fig:dfd-level-zero}
\end{figure}

把上下文图中的单个处理展开，就得到图\ref{fig:dfd-level-one}的一级数据流图。检查分解是否正确的办法是做平衡核对：一级图中所有跨越系统边界的数据流，必须与上下文图中进出系统的数据流逐条对应，既不能多出一条，也不能漏掉一条。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=5mm and 3mm,
  entity/.style={draw=Primary,fill=PrimaryLight,minimum width=1.5cm,minimum height=.65cm,align=center,font=\scriptsize},
  process/.style={ellipse,draw=Primary,fill=PrimaryLight,minimum width=1.7cm,minimum height=.8cm,align=center,font=\scriptsize},
  store/.style={draw=Primary,double,minimum width=1.9cm,minimum height=.6cm,align=center,font=\scriptsize},
  flow/.style={->,draw=Primary,thick},
  lbl/.style={font=\tiny}]
\node[process] (receive) {1.0 接收申报};
\node[process,right=of receive] (validate) {2.0 校验与分析};
\node[process,right=of validate] (approve) {3.0 审批计划};
\node[process,right=of approve] (dispatch) {4.0 生成配水};
\node[entity,above=8mm of receive] (farmer) {用水户};
\node[entity,above=8mm of approve] (manager) {调度与审批人员};
\node[entity,above=8mm of dispatch,xshift=10mm] (meter) {计量与闸门系统};
\node[store,below=11mm of validate,xshift=24mm] (plan) {D1 用水计划};
\node[store,below=11mm of validate,xshift=-15mm] (resource) {D2 水源与工程能力};
\node[store,below=of dispatch,xshift=8mm] (receipt) {D3 执行记录};
\draw[flow] (farmer.south west) -- ++(-1.5mm,0) |- node[pos=.28,left,lbl]{申报/补充资料} (receive.west);
\draw[flow] (receive.north)--node[right,lbl]{状态/批准计划}(farmer.south);
\draw[flow] (manager.south)--node[right,lbl]{审批/调整}(approve.north);
\draw[flow] (approve.north west) to[bend left=18] node[left,lbl]{待办/分析结果} (manager.south west);
\draw[flow] (dispatch.east)-|node[pos=.85,right,lbl]{配水任务}(meter.south);
\draw[flow] (meter.south east) to[bend left=32] node[right,lbl]{执行回执} (receipt.east);
\draw[flow] (receive)--node[above,lbl]{规范化申请}(validate);
\draw[flow] (validate)--node[above,lbl]{校核结论}(approve);
\draw[flow] (approve)--node[above,lbl]{批准计划}(dispatch);
\draw[<->,draw=Primary,thick] (validate.south east)--(plan.north west);
\draw[<->,draw=Primary,thick] (validate.south west)--(resource.north);
\draw[flow] (approve.south)--node[right,pos=.4,lbl]{审批结果}(plan.north east);
\draw[->,draw=Accent,dashed,thick] (receipt.south) |- ++(-10.2,-.6) node[below,pos=.55,lbl]{回执反馈} -| (validate.south);
\end{tikzpicture}
\caption{灌区用水计划与配水系统一级数据流图}
\label{fig:dfd-level-one}
\end{figure}

一级图只说明四个主要处理之间的边界，不能直接替代接口和数据校验设计。以“2.0 校验与分析”为例，二级分解可以拆成完整性与重复检查、需水量计算、渠道能力匹配、生成校核结果四个处理，再把每个处理的输出写回统一的数据存储。图\ref{fig:ch02-dfd-level-two}给出这一展开，同时保留从一级图传入的“申报资料”和传出的“校核结果”两个边界流；一级图中经 D3 的回执反馈在此层并入 D2 的工程能力更新，属于同一边界流的细化而非丢失。二级图新增的内部数据流可以更细，但不能改变父处理对外承诺的名称、方向和含义。

绘制二级图时要做三项平衡检查。第一，边界平衡：父图的每条输入和输出在子图中都必须找到对应流；第二，处理守恒：每个处理不能凭空产生业务数据，也不能吞掉没有去向的输入；第三，存储责任：读取和写入哪个数据存储要明确，不能用一条双向箭头掩盖读写时序。若子图需要引入“外部信用评分”一类新实体，应先回到上下文图讨论系统边界，而不是直接画进二级图。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=8mm and 7mm,
  process/.style={ellipse,draw=Primary,fill=PrimaryLight,minimum width=2.25cm,minimum height=.75cm,align=center,font=\small},
  store/.style={draw=Primary,double,minimum width=2.3cm,minimum height=.65cm,align=center,font=\small},
  ext/.style={draw=Accent,rounded corners,fill=Accent!10,minimum width=2.3cm,minimum height=.65cm,align=center,font=\small}]
\node[ext] (in) {申报资料};
\node[process,right=of in] (p21) {2.1 完整性\\与重复检查};
\node[process,right=of p21] (p22) {2.2 需水量\\计算};
\node[process,below=of p22] (p23) {2.3 渠道能力\\匹配};
\node[process,left=of p23] (p24) {2.4 生成\\校核结果};
\node[store,below=of p21] (d1) {D1 用水计划};
\node[store,below=of p24] (d2) {D2 水源与工程能力};
\node[ext,right=of p23] (out) {校核结果};
\draw[->,thick] (in)--(p21);
\draw[->,thick] (p21)--(p22);
\draw[->,thick] (p22)--(p23);
\draw[->,thick] (p23)--(p24);
\draw[->,thick] (p24)--(out);
\draw[<->,thick] (p21)--(d1);
\draw[<->,thick] (p23)--(d2);
\draw[->,dashed] (p24)--(d1);
\end{tikzpicture}
\caption{校验与分析处理的二级数据流图}
\label{fig:ch02-dfd-level-two}
\end{figure}

\subsection{数据字典}

数据字典定义数据流、数据存储和数据元素的结构与约束。每个字段的说明栏应给出编码格式、数据类型、单位、来源、取值域和校验规则，例如计划编号采用项目编码加年度序号，状态字段只能取受控枚举，流量字段注明精度与上限。清晰的字段契约能让接口、数据库和测试用例使用同一口径。表\ref{tab:water-plan-dictionary}按这个要求整理了用水计划的主要字段。填写时最容易省略的是“取值域”与“校验规则”两栏，而恰恰是这两栏决定了接口能否拒绝非法输入——把它们留空，等于把校验责任推给了下游每一个调用方。

\begin{longtable}{p{3.4cm}p{2.0cm}p{1.7cm}p{4.3cm}}
\caption{用水计划数据字典示例}\label{tab:water-plan-dictionary}\\
\toprule
数据项 & 类型/长度 & 单位 & 说明与校验 \\
\midrule
\endfirsthead
\toprule
数据项 & 类型/长度 & 单位 & 说明与校验 \\
\midrule
\endhead
planId & 字符串/26 & — & 全局稳定ID，不使用名称替代 \\
applicantId & 字符串/36 & — & 关联用水户主数据的代理键；不得承载居民身份证号等敏感身份信息 \\
canalId & 字符串/12 & — & 关联渠道对象编码 \\
startTime & 时间戳 & — & ISO 8601，本地时区UTC+8，与endTime构成左闭右开时段 \\
endTime & 时间戳 & — & ISO 8601，本地时区UTC+8，必须晚于startTime \\
requestedVolume & 十进制 & m$^3$ & 大于0，精度和上限由业务规则规定 \\
maxFlow & 十进制 & m$^3$/s & 不得超过渠道批准能力 \\
priority & 枚举 & — & emergency、must、normal等受控值 \\
status & 枚举 & — & draft、submitted、checking、approved、rejected、executing、closed；checking 对应状态图中的“待校核” \\
version & 整数 & — & 每次修订递增，批准后不可覆盖 \\
\bottomrule
\end{longtable}

数据流也要定义。例如“执行回执”至少包含任务ID、设备ID、指令时间、实际启闭时间、目标流量、实测流量、质量码和回执状态。涉及水工设备动作时，接口、日志和界面统一使用“闸门启闭”，并分别记录目标开度、实际开度、启闭状态与操作指令，避免把动作命令和观测结果混为一项。

\subsection{实体关系与规范化}

数据流图回答“数据如何移动”，实体关系模型回答“数据对象如何长期保存以及彼此如何关联”。案例灌区至少要区分用水户、地块、渠道、用水计划、配水任务和执行回执；一个用水户可以拥有多个地块，一个计划可以拆成多个任务，一个任务可以产生多条带时间的回执。图\ref{fig:ch02-er-water-plan}用主键、外键和基数表达这些关系，图中英文实体名分别对应“用水户、地块、用水计划、渠道、配水任务、执行回执”。实体名称对应业务对象，属性名称对应数据字典，二者不一致时应先修订概念模型再写表结构。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=9mm and 12mm,
  ent/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.5cm,minimum height=.85cm,align=center,font=\small\CJKfamily{zhsong}},
  rel/.style={draw=Accent,diamond,aspect=2.0,fill=Accent!10,inner sep=1pt,align=center,font=\scriptsize}]
\node[ent] (user) {WaterUser\\PK applicantId};
\node[ent,right=of user] (plot) {Plot\\PK plotId\\FK applicantId};
\node[ent,below=of plot] (plan) {WaterPlan\\PK planId\\FK plotId, canalId};
\node[ent,left=of plan] (canal) {Canal\\PK canalId};
\node[ent,right=of plan] (task) {DispatchTask\\PK taskId\\FK planId};
\node[ent,below=of task] (receipt) {ExecutionReceipt\\PK receiptId\\FK taskId};
\draw[-,thick] (user)--node[above,font=\scriptsize]{1:N}(plot);
\draw[-,thick] (plot)--node[right,font=\scriptsize]{1:N}(plan);
\draw[-,thick] (plan)--node[above,font=\scriptsize]{1:N}(task);
\draw[-,thick] (task)--node[right,font=\scriptsize]{1:N}(receipt);
\draw[-,thick] (canal)--node[above,font=\scriptsize]{1:N}(plan);
\end{tikzpicture}
\caption{案例灌区用水计划核心实体关系}
\label{fig:ch02-er-water-plan}
\end{figure}

规范化不是为了把表拆得越细越好，而是用函数依赖减少更新异常，再结合查询和审计场景决定是否保留合理冗余。若把用水户名称、地块名称和计划字段全部重复存入一张大表，修改名称时容易出现新旧值并存；若把每一条回执都覆盖到计划当前状态，又会丢失历史。应先写出“谁决定谁”的依赖，再选择主键、候选键和外键。表\ref{tab:ch02-normalization}用同一个计划实例说明从未分解记录到第三范式的判断路径。

\begin{table}[htbp]
\centering
\caption{用水计划数据的规范化判断}
\label{tab:ch02-normalization}
\begin{tabular}{p{2.1cm}p{4.0cm}p{5.8cm}}
\toprule
阶段 & 主要依赖与问题 & 调整后的关系 \\
\midrule
未分解记录 & 一行同时含用水户、地块、计划和多条回执；回执重复造成更新与删除异常 & 将重复的回执组识别为独立数据集合，保留计划与回执的关联键 \\
第一范式 & 字段保持原子值，回执不再以“回执1/回执2”多列出现 & 用水计划(planId, plotId, startTime, endTime, status)；执行回执(receiptId, taskId, occurredAt, value, quality) \\
第二范式 & 非主键字段完全依赖所属主键，计划字段不依赖回执编号 & 渠道与申请量(canalId, requestedVolume)属于用水计划；配水任务(taskId, planId, allocatedVolume)只保存本次分配量；回执只依赖receiptId及其任务外键 \\
第三范式 & 用水户名称、渠道名称等描述字段不由计划编号间接决定 & 用水户、地块、渠道单独维护，计划只保存代理键；展示时通过受控关联读取名称 \\
\bottomrule
\end{tabular}
\end{table}

规范化结果还要回到业务查询验证。值班员查看趋势需要按测点和时间范围快速过滤，审批人查看计划需要按状态和版本排序，运维员追查回执需要按任务编号定位全部事件；这些访问路径可以通过索引、物化视图或缓存优化，但优化不能破坏主数据的唯一性和审计历史。实体关系图、数据字典和接口契约应使用同一组编号，任何新增字段都要说明来源、依赖、迁移方式和回滚策略。

\subsection{状态转换图与判定树}

实体关系描述“有什么”，状态转换描述“对象在什么事件下如何变化”。用水计划至少经历草稿、已提交、待校核、已批准、执行中、已完成和已退回等状态；每条转换都应标出触发事件、前置条件和操作者。状态图能暴露两个常见遗漏：退回后是否允许修改并再次提交，以及执行中发现水量偏差后回到哪个受控状态。图\ref{fig:ch02-plan-state}把正常路径、退回路径和撤回边界画在一起，读者可以据此编写状态转换测试。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=9mm and 9mm,
  state/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.0cm,minimum height=.62cm,align=center,font=\small}]
\node[state] (draft) {草稿};
\node[state,right=of draft] (submitted) {已提交};
\node[state,right=of submitted] (checking) {待校核};
\node[state,right=of checking] (approved) {已批准};
\node[state,right=of approved] (running) {执行中};
\node[state,below=of running] (closed) {已完成};
\node[state,below=of checking] (returned) {已退回};
\draw[->,thick] (draft)--node[above,font=\scriptsize]{提交}(submitted);
\draw[->,thick] (submitted)--node[above,font=\scriptsize]{受理}(checking);
\draw[->,thick] (checking)--node[above,font=\scriptsize]{校核通过}(approved);
\draw[->,thick] (approved)--node[above,font=\scriptsize]{进入时段}(running);
\draw[->,thick] (running)--node[right,font=\scriptsize]{回执完成}(closed);
\draw[->,thick] (checking)--node[left,font=\scriptsize]{资料或水量问题}(returned);
\draw[->,thick] (returned) -| node[near start,below,font=\scriptsize]{补充后再提交} (draft);
\draw[->,dashed,thick] (running) to[bend left=35] node[right,font=\scriptsize]{偏差中止（受控）} (returned);
\end{tikzpicture}
\caption{用水计划状态与受控转换}
\label{fig:ch02-plan-state}
\end{figure}

判定树适合把连续的条件判断展示成从根到叶的路径。它与决策表互相校验：树强调阅读顺序和早期分流，表强调所有组合是否覆盖。绘制时先把安全和合规硬门槛放在靠近根的位置，再放业务偏好和效率条件；每个叶节点都应有唯一动作，不能让同一条路径同时落到批准和调整两个结果。

\subsection{决策表}

当业务结果由多个条件组合决定时，决策表比长段落更清楚。下表示例只用于需求澄清，真实阈值应由批准规则确定。表\ref{tab:water-plan-decision}给出三个条件的组合及其对应动作，并把“水量不满足时渠道状态无关”显式写成“—”。三个二值条件共有$2^3=8$种组合：规则1覆盖1种，规则2用一个无关项覆盖2种，规则3覆盖4种，规则4覆盖1种，合计8种且互不重叠。检查决策表时先列出全部组合，再标记每条规则覆盖的集合；若有组合没有被接住，或同一组合落入两个不同动作，就必须回到业务规则修订。

\begin{table}[htbp]
\centering
\caption{用水计划审批决策表示例}
\label{tab:water-plan-decision}
\begin{tabular}{p{4.2cm}cccc}
\toprule
条件/动作 & 规则1 & 规则2 & 规则3 & 规则4 \\
\midrule
资料完整 & 是 & 是 & 否 & 是 \\
水量可满足 & 是 & 否 & — & 是 \\
渠道可用 & 是 & — & — & 否 \\
\midrule
批准计划 & $\checkmark$ & & & \\
进入方案调整 & & $\checkmark$ & & $\checkmark$ \\
退回补充资料 & & & $\checkmark$ & \\
\bottomrule
\end{tabular}
\end{table}

把同一规则改画成判定树，可以帮助审批人按固定顺序阅读。图\ref{fig:ch02-approval-tree}先判断资料完整性，再判断水量是否满足；只有水量满足时才继续判断渠道是否可用。这样，规则2的渠道状态不会被错误地当作必填条件，树上的每个叶节点都能对应表中的一个动作。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=7mm and 12mm,
  decision/.style={draw=Accent,diamond,aspect=2.0,fill=Accent!10,align=center,inner sep=1pt,font=\scriptsize},
  leaf/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.0cm,minimum height=.55cm,align=center,font=\scriptsize}]
\node[decision] (complete) {资料完整？};
\node[leaf,below left=of complete] (return) {退回补充};
\node[decision,below right=of complete] (water) {水量可满足？};
\node[leaf,below left=of water] (adjust) {进入方案调整};
\node[decision,below right=of water] (canal) {渠道可用？};
\node[leaf,below left=of canal] (adjust2) {进入方案调整};
\node[leaf,below right=of canal] (approve) {批准计划};
\draw[->,thick] (complete)--node[above left,font=\scriptsize]{否}(return);
\draw[->,thick] (complete)--node[above right,font=\scriptsize]{是}(water);
\draw[->,thick] (water)--node[above left,font=\scriptsize]{否}(adjust);
\draw[->,thick] (water)--node[above right,font=\scriptsize]{是}(canal);
\draw[->,thick] (canal)--node[above left,font=\scriptsize]{否}(adjust2);
\draw[->,thick] (canal)--node[above right,font=\scriptsize]{是}(approve);
\end{tikzpicture}
\caption{配水审批规则的判定树}
\label{fig:ch02-approval-tree}
\end{figure}

\subsection{结构化分析的交叉校验}
结构化分析的工作顺序可以概括为“先边界、后处理；先输入输出、后内部细节；先数据定义、后规则判断”。在案例灌区案例中，上下文图先回答谁与系统交换什么，一级图再回答接收、校验、审批和生成配水分别负责什么，二级图才展开校验内部的字段、重复、计算和能力匹配。三层图的编号应保持父子对应，例如一级的2.0在二级只能拆成2.1、2.2、2.3等，不应在没有说明的情况下突然出现另一个“校核服务”。这样，需求、设计和测试可以沿编号逐层追踪。

每一层都要检查“黑洞、白洞和灰洞”。黑洞是处理有输入却没有输出，通常表示处理结果没有定义；白洞是处理凭空产生输出，说明漏写输入或外部约束；灰洞是输入输出虽然存在，但处理规则无法解释数据如何变换。以“校核需水与渠道能力”为例，输入应包括计划时段、申请量、渠道能力版本和已批准计划，输出应至少包括校核结果、差额、使用的版本和失败原因。若只画一个“校核通过”箭头，测试人员无法判断差额如何展示，审批人也无法复核系统采用了哪一份能力数据。

DFD中的数据流名称应使用名词短语，处理名称应使用动词短语，数据存储名称应说明保存的对象。把“审批人”画成数据流会混淆角色与消息，把“处理所有事情”画成处理会失去可验证边界。对于同一条流，父图和子图的名称可以缩短，但含义必须一致；例如父图的“批准计划”在子图中可以拆成“批准结果”和“审计记录”，但两者都要能回到父图的输出契约。平衡检查完成后，再为每个处理补处理说明、前置条件、业务规则、异常结果和事务边界。

实体关系模型也需要经历从概念到逻辑的两次校验。概念模型先确认业务对象是否真实存在以及关系基数是否符合业务，例如一个计划可以对应多个配水任务，但一个任务只能来源于一个计划；逻辑模型再补主键、外键、唯一约束、状态枚举和时间字段。若审批人要求“按计划版本查看当时的渠道能力”，就不能只保存渠道当前名称，而应在计划或校核结果中保存能力版本标识，并把版本来源写入审计记录。关系基数、唯一性和历史保留要求会直接影响后续接口的幂等键与数据库约束。

规范化检查可从函数依赖开始。设关系$R$依次包含计划编号、地块编号、用水户编号、用水户名称和申请量五个字段，则计划编号决定地块编号和申请量，地块编号决定用水户编号，用水户编号决定用水户名称；把这些字段放在同一张关系中会产生传递依赖。拆分为计划、地块和用水户三张关系后，修改用水户名称只需更新一处，查询时通过外键关联。规范化并不意味着禁止冗余：为报表保留经审计的快照是合理的，但快照必须标注生成时间、来源版本和不可回写属性，不能与主数据混为一张可编辑表。

状态模型的每条转换都应形成至少一个正向和一个拒绝测试。提交动作要验证草稿字段完整、用户权限有效且版本未过期；校核通过要验证水量和渠道能力来自同一版本；进入执行中要验证当前时间落在计划时段；完成要验证回执达到结束条件。对于重复提交、超时、撤回和回执迟到等事件，状态机必须规定幂等结果和人工处理入口。若一个事件在不同状态下意义不同，应在事件名或状态守卫中写清边界，不能让同一个“确认”动作既代表审批确认又代表设备执行确认。

判定树与状态图结合使用时，树负责一次决策，状态图负责决策后的长期变化。资料不完整的计划在树上落到“退回补充”，随后在状态图中回到草稿；水量不足的计划进入方案调整，调整后要重新走完整性和能力校核；渠道不可用则不能直接批准，而要记录替代渠道或人工会商结果。为每个叶节点指定唯一的后置状态，可以把业务规则转成一组覆盖测试：三条件决策表需要覆盖8个组合，状态转换测试还要覆盖每条合法边、每个拒绝边和至少一次重复事件。

完成模型后，应召开一次跨角色走查。值班员检查异常是否有可执行的接手动作，审批人检查规则和责任是否可追溯，专业分析员检查数据口径与版本，开发人员检查每个处理是否有输入输出契约，测试人员检查每个叶节点和状态转换是否有验收证据，运维员检查日志、指标和回滚所需的数据是否存在。走查结果按模型编号记录问题，修订后重新执行边界平衡、依赖检查和规则覆盖检查，直到模型可以直接支撑SRS、接口设计和测试用例。

模型之间还要建立互相校验的证据链。DFD中的“用水计划”应能在ER图中找到对应实体，在状态图中找到生命周期，在决策表中找到影响审批的条件，在数据字典中找到字段和取值域；如果其中一处使用了不同名称，评审人就应追问是否是同一个对象或两个不同概念。这样的交叉核对可以尽早发现“计划状态”与“审批结果”被混成一个字段、回执时间与计划时段单位不一致、以及一条规则没有对应测试用例等问题。最终交付时，可把模型编号、需求编号和测试编号放进同一张索引表，支持从任一证据反向定位上下文。

对于含有人工判断的流程，还应把“系统给出的候选结果”和“人员作出的最终决定”分开建模。水量平衡校核可以由系统计算并给出差额，审批人仍需确认是否采用替代时段；替代方案的理由、版本和批准人应成为审计数据，而不是写在备注中的自由文本。若专业分析员修改了阈值，系统应记录修改前后值和生效范围，避免下一轮模型或测试误把一次临时会商当成永久规则。结构化分析因此不仅是画图，也是在定义责任、证据和变化边界。

在课程实践中，学生可以先用纸笔完成三层DFD，再用实体关系图核对数据存储，最后从状态图和判定树各抽取一条正常路径与一条异常路径，写成验收用例。教师评审时重点查看编号是否贯通、边界是否平衡、条件是否穷尽，以及每个叶节点是否有明确责任人和证据；这比单纯检查图形是否美观更能检验需求分析质量。

\section{软件需求规格说明与验收}

\paragraph{本节层次}核心：2.6.3；指导实践：2.6.1；拓展：2.6.2。核心路线只要求读完核心小节。

\paragraph{一条含糊需求的改写过程}写规格说明前，先看一条真实访谈里会出现的话怎样一步步变成可以验收的条目。灌区管理处的调度员说：“渠道水位异常时系统要及时提醒我。”这句话里有四个没有说清的地方：什么算“异常”、多快算“及时”、提醒谁、通过什么方式提醒。表\ref{tab:ch02-vague-to-acceptance}记录了逐轮追问后每个空缺是怎么被填上的，以及最后写进规格说明的验收条件。数字不是分析师拍的：水位阈值来自灌区的运行规程，时限来自值班交接制度，这两者是需求的权威来源；访谈里得不到的性能数字，就明确标为待确认，而不是先写一个好看的数。

\begin{table}[htbp]
\centering
\caption{从含糊陈述到验收条件：“渠道水位异常时系统要及时提醒我”}
\label{tab:ch02-vague-to-acceptance}
\begin{tabular}{p{2.0cm}p{4.6cm}p{6.6cm}}
\toprule
空缺 & 追问与答复 & 写入规格说明的表述 \\
\midrule
什么算异常 & 问：多高算异常？答：干渠 1 号闸前水位超过运行规程的警戒线 3.20 m，或 10 分钟内上涨超过 0.15 m & 需求 REQ-ALM-01：当干渠 1 号闸前水位测点最新有效观测 $\geq$ 3.20 m，或最近 10 分钟内有效观测上涨 $\geq$ 0.15 m，系统生成一条黄色预警 \\
多快算及时 & 问：晚多久你会觉得没用？答：值班交接制度要求 5 分钟内响应 & 从观测入库到预警可见不超过 60 s；观测本身的采集间隔（5 min）不计入 \\
提醒谁 & 问：只提醒你吗？答：当班值班员，夜间转值班长 & 提醒对象取自值班表的“当前值班员”角色，不写死人名 \\
通过什么方式 & 问：页面弹窗够吗？答：不盯屏时要有手机短信 & 页面显示预警条目；短信通道为可选项，属于另一个需求 REQ-ALM-03 \\
未能确认 & 问：一次最多同时多少条预警？答：不知道 & 标注“待需求确认”，不写数字；进入需求评审待办 \\
\bottomrule
\end{tabular}
\end{table}

判断改写是否到位的标准只有一条：另一位同学不参加访谈，只看右栏，能不能设计出一个实验来判定“做到了”还是“没做到”。左栏的原话做不到这一点，右栏可以：往数据库写入一条 3.25 m 的观测，60 秒内页面上应出现一条对象为该测点、等级为黄的预警条目。2.6.3 节把这一条带回案例水库时，只需换阈值来源与角色，改写方法不变。


软件需求规格说明（SRS）把业务目标、系统边界、功能行为、质量属性、接口约束和验收证据组织成可评审的基线。现行国家标准 GB/T 9385-2008《计算机软件需求规格说明规范》给出了 SRS 的章节组织与编写要求，其内容与 IEEE Std 830-1998 相对应\cite{gbt93852008}；国际上 IEEE 830 已于 2011 年被 ISO/IEC/IEEE 29148（现行 2018 版）取代，后者把需求工程的过程与文档要求合并到一个标准中。课程项目按“目标—范围—需求—验证—追踪—变更”的骨架编写，可同时满足两者的核心要求，并在参考资料栏记录所依据的标准版本。表\ref{tab:ch02-srs-structure}将骨架映射到案例灌区的交付物，读者可以用它检查一份SRS是否遗漏关键章节。

\begin{table}[htbp]
\centering
\caption{案例灌区软件需求规格说明的结构与交付物}
\label{tab:ch02-srs-structure}
\begin{tabular}{p{2.5cm}p{4.2cm}p{4.2cm}}
\toprule
章节 & 应回答的问题 & 案例灌区交付物 \\
\midrule
引言与范围 & 为什么建设、服务谁、明确哪些内容不在范围 & 建设目标、角色清单、术语、范围边界 \\
总体描述 & 运行环境、假设、约束和业务背景是什么 & 管理站网络、灌溉期、数据来源与责任边界 \\
功能需求 & 系统在输入、条件和权限下必须产生什么结果 & 申报、校核、审批、配水、执行回执用例 \\
数据与接口 & 对象、字段、单位、协议和错误如何约定 & 数据字典、DFD、接口契约、质量码映射 \\
质量与安全 & 速度、可用性、审计和权限达到什么水平 & 指标阈值、降级策略、审计字段、验收场景 \\
验证与追踪 & 如何证明每项需求被实现并可验收 & 测试用例、追踪矩阵、待确认问题清单 \\
附录与变更 & 哪些资料支持正文，版本如何演化 & 模型、术语、评审记录、变更历史 \\
\bottomrule
\end{tabular}
\end{table}

课程项目可采用下列精简目录，但不能省略关键内容：

\begin{enumerate}
\item 引言：目的、范围、术语、参考资料；
\item 总体描述：产品背景、用户特征、运行环境、假设与约束；
\item 外部接口：用户、软件、硬件和通信接口；
\item 具体需求：功能、数据、质量属性、安全与合规；
\item 业务规则与异常处理；
\item 验收标准与需求追踪关系；
\item 附录：数据字典、模型、待确认问题与变更记录。
\end{enumerate}

一条可验证需求可以写为：“当已批准用水计划进入执行时段且现场计量有效时，系统应每5分钟比较计划流量与实测流量；连续3个周期超过批准容限时，创建唯一偏差事件并通知值班员。”其中角色、条件、频率、判断、结果和去重要求都可以设计测试。

\subsection{需求评审检查单}

SRS评审不是逐字找错，而是确认每项需求都具备实现、验证和追踪所需的证据。评审会开始前由需求负责人发出固定版本，参与者分别从业务、架构、开发、测试、安全和运维角度预读；会议中只讨论带有编号和上下文的条目，结论写成接受、补证据、修改或拒绝，并由责任人和截止时间承接。表\ref{tab:ch02-srs-review-checklist}给出一份可直接使用的检查单，打勾必须同时填写证据位置，不能只写“已确认”。

\begin{table}[htbp]
\centering
\caption{SRS评审检查单与证据}
\label{tab:ch02-srs-review-checklist}
\begin{tabular}{p{2.4cm}p{5.4cm}p{3.5cm}}
\toprule
检查维度 & 评审问题 & 证据位置 \\
\midrule
范围与角色 & 系统内外边界、角色权限和不在范围内的活动是否明确 & 范围图、角色清单 \\
完整性 & 正常、拒绝、超时、重复和恢复路径是否都有需求编号 & 用例、异常流、状态图 \\
一致性 & 术语、单位、时间区间、质量码和接口字段是否前后一致 & 术语表、数据字典 \\
可行性 & 现场网络、设备协议、数据容量和团队能力能否支撑实现 & 约束清单、原型或试验记录 \\
可验证性 & 每条需求是否有可观察结果、阈值、测试数据和通过条件 & 验收准则、测试用例 \\
安全合规 & 权限、审计、敏感字段和高风险动作是否有责任边界 & 权限矩阵、审计样例 \\
可运维性 & 指标、日志、告警、备份和回滚是否能支持运行值守 & 运行手册、演练记录 \\
追踪与版本 & 来源、设计、接口、测试和变更记录是否能双向定位 & 追踪矩阵、版本库 \\
\bottomrule
\end{tabular}
\end{table}

检查单的使用顺序也很重要。先检查范围和角色，再检查对象与数据，最后检查质量属性和实现约束；否则团队可能在一个尚未确定的业务边界上争论接口字段。对于“待补证据”，评审记录要写出最小验证动作，例如安排一次夜班观察、运行一组脱敏压测或让审批人走一遍原型；验证完成后更新同一需求编号，不要另起一条相似需求掩盖原问题。评审通过的版本成为基线，后续意见必须进入变更流程。

评审SRS时，应让业务、开发、测试、运维和安全角色从各自角度走查。评审通过形成基线；后续变更需提交原因、影响、优先级和批准记录，并沿追踪矩阵更新相关模型、接口、测试和运行文档，而不是直接修改共享文档。

SRS中的功能需求最好采用“条件—动作—结果—证据”的句式。条件说明触发事件、角色、数据版本和前置状态，动作说明系统必须执行的处理，结果说明用户或外部系统能观察到的状态，证据说明用什么测试数据和日志判定完成。例如“当审批人确认可供水量校核通过的计划时，系统应生成唯一配水任务并记录计划版本、确认人和确认时间；若任务编号已存在，系统应返回原任务并记录重复请求”。这比“系统应可靠地生成任务”更容易拆成接口、数据库约束和验收用例。每条需求还要注明优先级、来源、责任人和目标版本，避免同一条规则在不同章节出现两个阈值。

评审角色的关注点不同。业务代表核对流程是否符合规程和岗位责任，审批人核对状态和授权边界，专业分析员核对模型输入、单位和版本，开发人员核对接口、数据结构和异常处理，测试人员核对判据与测试数据，安全与运维人员核对审计、备份、监控和回滚。会议主持人应按检查单逐项点名，不能只由最熟悉系统的人代替所有角色发言。对于有争议的条目，先记录双方的事实和假设，再安排最小验证动作；未验证项保持“待补证据”状态，不进入已批准基线。

\subsection{需求变更控制流程}

需求基线不是冻结一切变化，而是让变化有入口、有影响分析和可回退结果。任何变更申请至少包含提出人、业务理由、受影响需求、紧急程度、期望版本和验证证据；涉及水量规则、权限、数据结构或设备接口时，还要标注安全与合规影响。图\ref{fig:ch02-change-control}把申请、分析、决策、实施和验证连成闭环，拒绝或暂缓的申请也保留记录，便于以后复盘。

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=7mm and 9mm,
  ctrlstep/.style={draw=Primary,rounded corners,fill=PrimaryLight,minimum width=2.25cm,minimum height=.68cm,align=center,font=\small},
  gate/.style={draw=Accent,diamond,aspect=2.0,fill=Accent!10,align=center,inner sep=1pt,font=\scriptsize}]
\node[ctrlstep] (submit) {提交变更\\编号与理由};
\node[ctrlstep,right=of submit] (impact) {影响分析\\范围与风险};
\node[gate,right=of impact] (review) {批准？};
\node[ctrlstep,right=of review] (implement) {实施与迁移\\更新基线};
\node[ctrlstep,below=of implement] (verify) {回归验证\\发布或回退};
\node[ctrlstep,below=of review] (defer) {暂缓/拒绝\\保留记录};
\draw[->,thick] (submit)--(impact);
\draw[->,thick] (impact)--(review);
\draw[->,thick] (review)--node[above,font=\scriptsize]{是}(implement);
\draw[->,thick] (review)--node[left,font=\scriptsize]{否}(defer);
\draw[->,thick] (implement)--(verify);
\draw[->,thick] (verify) -| node[near start,below,font=\scriptsize]{发现回归}(impact);
\end{tikzpicture}
\caption{需求变更从申请到验证的控制闭环}
\label{fig:ch02-change-control}
\end{figure}

以“增加按管理站批量调整计划”的申请为例，影响分析应列出页面、接口、权限、数据库索引、审计事件和培训材料；如果调整会绕过单计划水量校核，就必须先拆成“批量编辑草稿”和“批量提交校核”两个需求。审批人依据业务价值、安全合规、成本和依赖决定进入哪个版本，实施时同时更新SRS、数据字典、接口契约和测试用例。迁移完成后先在隔离环境验证旧数据、并发提交、回滚和审计，再发布到生产观察窗口；任何一项硬门槛失败都回到影响分析，不用临时手工修库代替变更控制。

变更记录要区分“需求改变”和“实现选择改变”。例如把列表分页从服务端实现改为数据库游标，若外部行为、性能阈值和接口契约不变，可以作为设计决策更新，不必改写业务需求；若分页导致用户能看到的时间范围、权限过滤或排序语义改变，就必须提出需求变更并重新验收。这样可避免把技术重构伪装成无影响操作，也避免每次内部重命名都触发不必要的业务评审。

评审通过后要做一次追踪矩阵演练。先从需求出发向下找到设计、接口、实现和测试，再从失败测试反向定位受影响需求；矩阵中的空白格就是尚未闭合的交付风险。表\ref{tab:requirements-traceability}用案例灌区四条需求展示双向追踪，状态栏不能只写“进行中”，应标出当前版本和下一步证据。

\begin{table}[htbp]
\centering
\caption{案例灌区需求追踪矩阵实操}
\label{tab:requirements-traceability}
\begin{tabular}{p{1.8cm}p{3.1cm}p{2.8cm}p{2.5cm}p{2.2cm}}
\toprule
需求ID & 来源/业务目标 & 设计或接口 & 验收用例 & 状态与证据 \\
\midrule
REQ-PLAN-01 & 用水户申报场景 & POST /water-plans & TC-PLAN-01～04 & 基线v1.2；正常/拒绝已测 \\
REQ-PLAN-07 & 渠道能力约束 & 计划校验服务 & TC-CAP-01～03 & 待补夜班数据；责任人李某 \\
REQ-AUDIT-02 & 审批责任追踪 & 审计事件接口 & TC-AUDIT-05 & 通过；含人、时、理由、版本 \\
REQ-DEV-03 & 执行偏差复核 & 偏差规则与工单 & TC-DEV-01～06 & 观察窗口中；回执缺测待复核 \\
\bottomrule
\end{tabular}
\end{table}

追踪矩阵的粒度应与变更影响相匹配。一个需求若同时关联多个接口和测试用例，变更时不能只勾掉其中一格；应先标记受影响的设计决策、数据迁移、权限、监控和培训，再决定是否需要回归整条业务链。反向追踪同样重要：如果测试发现“重复提交生成两个任务”，测试人员应能回到唯一需求编号，确认是接口幂等约束、数据库唯一索引还是状态转换规则缺失。

矩阵可以放在版本库中由构建检查，或由项目管理工具维护，但字段含义必须固定。需求ID、版本、来源、责任人、设计对象、接口路径、测试用例、验收结果和变更号构成最小闭环；删除一条需求时保留废止原因和替代需求，不能让历史测试失去解释。发布前由需求负责人抽样反向追踪，确保“已完成”至少有一项可复现证据，未完成项明确进入下一版本。




变更申请可按影响分为一般、重要和紧急三类。一般变更只影响页面文案或查询排序，产品负责人评审后更新故事和测试；重要变更影响数据结构、权限、接口兼容性或质量阈值，需要架构、安全、测试和运维共同签字，并安排迁移和回滚演练；紧急变更用于正在发生的安全或运行事故，允许先采取受控缓解措施，但必须在事后补齐影响分析、批准记录和回归测试。分类依据是影响范围与失败后果，不是提出人的职位或声音大小。每类变更都要有观察窗口和关闭条件，观察结束后把实际结果写回变更记录。

实施变更时要维护“旧版本可读、新版本可验证、失败可回退”三条边界。数据库字段增加可以先向后兼容，再在新版本使用；字段含义改变时应新建版本或迁移脚本，保留旧字段读取期；接口响应新增字段要确认旧客户端忽略它不会破坏流程。涉及代理键和个人信息时，迁移脚本只搬运业务标识，不把身份证号等敏感字段复制到新主键；回滚时要检查审计记录和外部消息是否已经发出，不能只恢复数据库快照而留下无法解释的任务状态。

追踪矩阵还可以支持发布说明和缺陷复盘。发布前按版本筛选所有“已批准且未关闭”的需求，核对每条都有测试结果、迁移记录和运行指标；发布后若出现偏差事件重复、计划时段错位或权限越界，先从告警或测试编号反查到需求，再判断是实现缺陷、需求遗漏还是现场假设失效。若属于需求遗漏，新增需求必须关联原缺陷和变更号，避免只修代码而让基线继续描述错误行为。矩阵因此既是文档，也是团队共同维护的质量索引。

接口需求应同时写出请求、响应、错误和幂等语义。以提交用水计划为例，SRS要说明必填字段、时间格式、代理键、重复请求的判定、权限不足的状态码、校核失败的业务错误码以及审计事件的字段；仅写一条URL和“返回成功”无法支撑前后端联调。数据字典的字段编号、接口契约的字段名和数据库迁移脚本应互相引用，任何一个字段改名都要先评估客户端、报表和历史数据的影响。接口版本可以通过路径或媒体类型演进，但兼容期、下线日期和通知对象必须写进变更记录。

质量属性需求应配套测量方案。性能条目要写负载模型、数据规模、缓存状态和统计分位数；可用性条目要区分计划维护、网络中断和服务进程故障；可靠性条目要指定重试、幂等和最大恢复时间；安全条目要说明授权边界、审计留存和敏感字段处理。评审人看到阈值时，还要追问谁采集指标、在哪里保存原始数据、失败后谁负责响应。若阈值来自试点假设，应标注“待验证”，并在观察窗口结束后决定升级、放宽或撤销，避免把未经测量的口号写进验收合同。

基线发布需要一个可复现的版本包。除了SRS正文，还应冻结引用的模型文件、数据字典、接口示例、测试数据、评审纪要和待确认问题；版本包生成校验摘要，发布人和审批人签名。开发、测试和运维使用同一摘要确认自己拿到的是同一份需求，遇到争议时先核对版本再讨论实现。修订时保留差异说明，指出新增、修改、废止和迁移字段，不能用“全文更新”四个字替代影响分析。

变更委员会的职责可以按四类角色分工：业务代表确认价值和范围，安全与合规人员确认硬约束，技术负责人确认实现和迁移风险，测试与运维负责人确认验证、观察和回退条件。小团队可以由同一人兼任多个角色，但每个角色的决策记录仍要单独填写。涉及闸门控制或防洪调度的变更，即使只是调整默认参数，也应至少经过审批人和专业分析员复核；普通报表排序可以采用轻量流程，但仍要留下版本号和回归用例。

作为课堂练习，学生可以选取“新增计划撤回”作为变更申请：先填写理由和受影响状态，再沿图\ref{fig:ch02-change-control}列出接口、数据、权限、审计和测试影响，最后在表\ref{tab:requirements-traceability}新增需求编号并给出一条正常、一条拒绝和一条重复撤回用例。教师检查的重点是旧需求是否仍能解释历史数据、回滚是否会产生悬空任务，以及矩阵是否能从失败用例反向定位到责任人。

一份合格的SRS还应在交付时附上阅读说明：先读范围和角色，再读功能用例与数据契约，最后对照质量指标、追踪矩阵和变更历史。读者按照同一路径检查，能在不依赖作者口头解释的情况下复现关键决策和验收步骤。

需求基线发布后还要安排一次“基线复核”，确认文档、模型、接口示例和测试数据来自同一版本摘要。复核不是重复朗读SRS，而是随机抽取几条需求，分别从业务目标追到验收证据，再从一条测试失败反查到责任人和变更号；如果任一方向出现断点，就把断点登记为发布风险并规定关闭条件。对案例灌区这类持续接收设备数据的系统，复核还应覆盖跨日时段、重复提交、权限变化和网络恢复等边界场景，确保文字规则在真实运行节奏下仍然可执行。只有通过基线复核，版本包才适合作为开发、测试、培训和运维共同引用的唯一依据。

\subsection{从灌区回到水库：方法迁移}

本章一直用案例灌区的用水计划做需求分析练习，而全书主线是水利工程安全监测平台。选灌区不是偏离主线：配水申报天然带有多方利益冲突、状态流转和水量守恒约束，是练习需求获取、建模与验证的更好素材；安全监测的需求冲突要到运行期才充分暴露，不适合作第一个练习对象。方法本身是可迁移的，下面按本章的四类交付物给出映射，读者可对照完成一次水库版的需求分析，其结果正是第3章架构设计和第8章验收的输入。

干系方与用例：灌区的“用水户—管理站—调度审批”对应水库的“测站设备—值班员—专业分析员—审批人”；“提交申报—校核—批准—执行—回执”闭环对应“观测入库—质量检查—预警评估—人工确认—工单处置—回执复核”。数据字典：用水计划的 planId/status/requestedVolume 对应观测记录的 assetId/occurredAt/quality/value，两者都要求全局稳定 ID、受控枚举与左闭右开时段。状态图：计划的“草稿—待校核—已批准—执行中—已完成/已退回”对应预警事件的“新建—已通知—已确认—处置中—已闭环/已误报”，两张图共享“超时升级不跳过审批责任”的原则。追踪矩阵：灌区示例中的 REQ-PLAN/REQ-AUDIT 系列编号规则同样用于水库需求；第8章的阶段验收表将按同一编号回查需求来源，形成“需求—架构—实现—验收”的完整证据链。

\section*{小结}

软件生命周期覆盖需求、设计、实现、验证、运行、维护到退役；瀑布、原型、增量、螺旋和敏捷并不是按先进程度排序的，而是对应不同的风险来源和反馈条件。需求稳定、验收标准明确时瀑布可行；界面和交互不确定时先做原型；业务边界还在变化时用增量或敏捷缩短反馈周期。选错模型的代价通常不是进度慢，而是把不确定性推到了验收阶段才暴露。

案例灌区的一个冲突可以把需求工作的整条链路串起来：用水户希望提交申报后立刻得到批准以便安排劳力和设备，审批人和专业分析员则要求先完成水量平衡校核。处理办法不是在两个按钮之间二选一，而是先拆出双方各自真正要解决的问题——一方要“尽快知道结果”，一方要“结果可执行且可追溯”——再组合成两段式反馈：提交即返回受理编号与缺失字段，校核通过后才由审批人确认并发布计划。冲突决策按安全与合规、业务价值、成本与依赖三层顺序记录，硬门槛在前，任何快捷路径都不能绕过。

系统流程图、数据流图、数据字典、状态转换图和决策表的作用，就是让这类判断有据可查，而不是停留在会议室的讨论里。需求规格说明与变更控制闭环则保证需求基线可以变化，但每一次变化都留下提出人、影响分析、批准记录和回退路径。写不出通过／失败判据的条目，通常不是表述问题，而是这条需求还没有想清楚。
\section*{章末交付物}

以“案例灌区用水计划申报与配水”为对象，提交：干系方清单、3个业务场景、MoSCoW优先级表、上下文DFD、一级DFD、数据字典、决策表、精简SRS和需求追踪矩阵。

\section*{思考题与练习题}

\begin{enumerate}
\item 软件生命周期为什么包含退役与数据处置？
\item 对需求稳定、接口明确且审查严格的设备接入项目，生命周期模型应如何选择？说明理由与风险。
\item 探索性原型为什么不能直接视为生产系统？
\item 为灌区用水计划系统识别6类干系方，并分别写出一个核心目标。
\item 使用MoSCoW方法排列5条需求，写明优先级理由和依赖。
\item 将“系统应快速显示渠道状态”改写为可验证需求。
\item 按图\ref{fig:order-process-flowchart}增加“计划过期”异常路径，说明回退位置。
\item 检查图\ref{fig:dfd-level-zero}与图\ref{fig:dfd-level-one}是否保持输入输出平衡。
\item 使用DFD描述“用水计划申报—审批—配水—执行反馈”，不得把施工进度等非数据对象画成数据流。
\item 在正文已列出的执行回执字段之外，再补充不少于6个数据字典字段（如计量方式、修订标记、审计信息等），标明类型、单位和校验规则。
\item 编写一张至少含3个条件、4条规则的配水审批决策表。
\item 按2.6节目录编制一份5～8页精简SRS，并为5条关键需求建立追踪矩阵。
\end{enumerate}
