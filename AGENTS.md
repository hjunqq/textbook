# 《智慧水利平台架构与开发》教材仓库 —— 执行契约

本文件对所有在本仓库工作的自动化代理生效。**每次开工前先完整读一遍，每次提交前逐条自检。**

## 一、本轮任务的性质（第十一轮，2026-09-21 起）

本轮是**教学性重编**：读者是水利大三学生，学过一门编程语言，未系统学过Web前后端和软件工程。
工作分支`revision/undergraduate-r11`，计划见`revision/r11/PLAN.md`，实际进度与迁移记录见`revision/r11/PROGRESS.md`；
每次开工先读这两份文件，不另立新计划。未经作者决定，不合并master、不发布正式版本。

本轮保留专业原理、必要推导和水利数据语义，调整概念出现顺序，合并重复代码，删去自我限定和面向审稿人的口径，
使正文可以连续阅读。不把教材改成操作手册，不用新增标签、免责声明或验收要求代替改写。

第九轮的篇幅上限、字数基线、清单/图数量下限和载体密度自本轮起只作提示（决策 D-2026-09-21-8）。
第三轮误删39%的教训仍然有效：**任何成段删除或迁移都要登记去向**。

## 二、铁律

1. **删减与迁移必须登记。** 单章一次提交减少超过1000字，先在`tools/migration_ledger.json`
   登记允许减少的字数与原因，随本包提交，提交后清空；同时在`revision/r11/PROGRESS.md`
   写明“原位置—去向—保留/合并/删除的理由”。**迁移不等于丢弃**：迁出的内容必须能在去向处找到；
   确属重复或空泛而删除的，写明它与哪一处重复。字数基线与篇幅上限只作提示。

2. **代码清单：配套工程完整，正文讲清变化。** 配套工程必须完整可运行。正文中第一次出现的最小示例完整展示；
   后续在其上新增功能时只印变化部分，并写明文件位置、上下文入口、运行方法和可观察结果。
   不设统一行数上限或下限，不为保清单数量重复打印完整程序。与配套文件对应的清单登记进
   `tools/check_listings.py`的`PAIRS`。

3. **图、表、公式必须随文引出。** 每个 `\label` 至少被 `\ref` 引用一次，正文里写
   "如图6-3所示""见表8-4""按式(6-7)计算"。新增浮动体同样适用。

4. **不许把改稿批注写进正文。** 禁止出现"原稿""不再混用""不能写成""不应表述为"
   "过时表述""本节不再"等面向上一稿作者或审稿人的句式。读者从未见过被否定的说法，
   这类句子只会制造困惑。一律改写为面向学生的正面表述。

5. **不许注水。** 讲一个做法，就给出学生能看到的结果：代码清单、图、表、公式推导、请求过程追踪、
   故障情形记录（现象→原因→处理→验证）或带答案要点的自测题。新增教学单元应有一个可观察结果和一个失败例。
   载体密度不再按字数折算考核。
   禁止只罗列技术名词（"Hadoop、Spark、Flink 各有特点"这类段落一律不收）。

6. **技术基线锁定，不得漂移。** Vue 3.4+ / Vite 5+ / Pinia 2+ / Java 17 /
   Spring Boot 3.2+ / Spring Security 6 / Jakarta EE / jjwt 0.11.x /
   Three.js r160+ / CesiumJS 1.12x+ / PostgreSQL + PostGIS + TimescaleDB / Redis / Kafka。
   容器镜像锁定：`timescale/timescaledb-ha:pg16`、`redis:7.2-alpine`、**`apache/kafka:3.7.2`**
   （决策 D6：`bitnami/kafka` 已被上游下架并迁入 `bitnamilegacy/` 归档仓库，不得回退）。
   正文举例、习题、案例一律用这套，不得再出现 Django、MySQL、Oracle 作为推荐项。

7. **工程参数与接口契约全书唯一。** 案例水库的坝高、特征水位、测点数量、编码规则、
   预警等级、表结构，以第8章 8.1 节的参数表（`tab:case-parameters`，宏在 `output/case-params.tex`）
   为唯一来源；**接口路径、字段、错误体以 8.1 节的接口契约表（`tab:api-contract`）为唯一来源**，
   资源模型为 asset / reading / warning / work-order，与 companion/water-platform-demo 一致。
   其他章一律引用不得自设（第九轮 D4：ch03/ch04/ch05 残留的 `/api/stations`、`/api/monitoring`、
   `/api/readings` 路径在 R1/R4 改名归一）。新增参数或端点必须先写进 8.1 的表，再在正文使用。
   第4章的教学接口按 `tab:api-teach-faults` 用查询参数 `teach=` 注入故障，真实后端忽略该参数。

8. **一个工作包一次提交。** 提交前必须 `python tools/check_textbook.py --build` 输出"通过"（口径见第五节）。
   提交说明里写清：工作包编号、正文字数 before → after、新增了哪些小节/代码/图表、
   删除了什么（如有）。

9. **扩写先回收，不许凭空写。** 本书有三层素材，加起来 35 万字，目标只有 20 万字——
   这是选材题不是作文题。任何扩写工作包的第一步都是去这三个地方找现成内容：
   - **L1 删除前的 tex**：`git show d8beab0:output/chapters/chapterNN.tex`
     （第三轮删掉了 48,473 字，全在这里，且是经过 2026-04 四阶段精修的版本，质量最高）
   - **L2 原始手稿**：已归档至 `archive/docs-legacy/`（第七轮迁站时移出 docs/；
     352,429 字，543 块代码），**不在** `docs/chapters/` 下——docs/ 现为 tex2site 生成物，禁止手改
   - 取用顺序先 L1 后 L2。具体哪个工作包对应哪些文件，见
     《旧版内容回收清单-第五轮.md》第五节的映射表。

   **回收必须过三道闸**（详见回收清单第六节）：
   (a) 概念、原理、公式、流程、水利实例、图表——直接回收；
   (b) 一切技术细节——**逐条对照本文件第二节第 6 条的技术基线更新**。L2 手稿是 2025 年的，
       里面有 Vue 2 选项式 API、Vue CLI、Spring Boot 2 的 `javax.*`、
       `WebSecurityConfigurerAdapter`、jjwt 0.9、Three.js 的 `outputEncoding`、
       Cesium `readyPromise`、Chart.js、MySQL/InfluxDB 等一大批过时写法。
       **原样搬进来等于把上一轮好不容易修掉的硬伤又请回去**；
   (c) 有几块第三轮删得对，**不要回收**：ch03 的 SafeHome 家庭安防案例、
       ch05 的 Python/Flask 平行实现、ch06 的"典型应用案例/关键技术挑战/发展趋势与展望"
       三节论文腔、ch09 的 10 道错位习题与"截至2026年4月"时点表述、
       任何无出处的项目数量与成效百分比。

   每个工作包的提交说明必须写明：本包回收了哪些段落（来源到文件与小节名）、
   回收时更新了哪些技术点、新写了哪些部分。

## 三、术语与体例（全书统一）

| 项 | 统一为 | 不再使用 |
|---|---|---|
| 业务侧结论 | 预警 | 报警 |
| 系统侧对象 | 告警事件、告警服务 | 报警构件、报警接口 |
| 预警等级 | 蓝、黄、橙、红四级 + 无预警(NONE) | 五级颜色、0–3 数字、`none` 字符串 |
| 角色 | 值班员、专业分析员、审批人、运维员 | 工程管理人员、系统管理员、运维管理员、专家用户 |
| 贯穿案例名 | 水利工程安全监测平台（工程载体为虚构的案例水库；第2章方法线为案例灌区） | 清源水库安全监测平台、智慧水库安全监测平台、东湖水库 |
| 章节引用 | 阿拉伯数字，"第4章""8.5节" | 中文数字"第四章" |
| 引号 | 全角弯引号 “ ” | ASCII 直引号 " " |
| 不编号小标题 | `\paragraph{...}` | 手工 `\textbf{...}` 顶行 |
| Transactional Outbox | 事务发件箱 | 事务外盒 |

## 四、必须保留的既有约束

- `pandoc/`、`build.bat` 是合法 GBK 编码，**不要动**。
- 原理、流程、精确几何和数据关系图继续用 TikZ。依据作者2026-09-12对“插图不够真实、插图太少”的反馈，
  允许在正文中使用原创写实教学渲染和配套程序实际运行截图（`output/images/generated/`、`output/images/runtime/`）；
  渲染图须注明“AI生成教学渲染”，运行图须记录代码、数据与采集方法，均须随文引出并登记来源。
  不得将生成场景称为现场照片、测绘成果或配套程序实际运行效果；插图应服务于对象识读或操作验证。
  例外（第三方素材清单）：第8章8.5节6张51WIM产品截图（output/images/chapter08/51wim-*.jpg），
  经北京五一视界书面授权，仅限纸质/PDF版刊出；线上站点(docs/)一律使用TikZ重绘示意图，
  不得将授权截图加入网站或对外再许可（MIT不覆盖该组图片）。
- 参考文献用 `biblatex + gb7714-2015 + sorting=none`，bib 条目必须全部被引用。
- 新增 `\lstdefinelanguage` 一律写在 `output/main.tex` 导言区，不得写在章文件正文里。

## 五、门禁

```bash
python tools/check_textbook.py            # 默认：不得倒退模式
python tools/check_textbook.py --build    # 含完整编译（xelatex→biber→xelatex×2）
python tools/check_textbook.py --report   # 只看报表，永远 exit 0
python tools/check_textbook.py --strict   # 严格模式：所有软指标必须归零（M1 与终验用）
python tools/check_listings.py           # 核验书中清单与 companion 文件是否逐字一致
```

`check_listings.py` 补的是门禁查不到的一条：**书上印的代码和配套仓库里的文件是不是同一段**。
清单与配套漂移后，读者照书敲出来的东西跑不起来，而 `check_textbook.py` 依然全绿。
新增"与书中清单逐字一致"的阶段包文件时，把它登进该脚本的 `PAIRS`；
确有必要的差异登进 `ALLOWED` 并写明原因——不写原因的豁免等于把这个检查关掉。

门禁分两类：

**硬失败**——任何时候都不允许，一票否决：未登记的单章成段删除（>1000 字/次提交）；环境未配对；悬空 `\ref`；重复 label；
参考文献表有未引用条目；编译出现 Error / 未定义引用 / Overfull / 缺字。

**软指标**——存量问题，只要不比 `tools/gate_baseline.json` 记录的起点更差就放行：
未被 `\ref` 引用的 label 数、**未被 `\ref` 引用的代码清单数（第九轮起点 63）**、改稿批注处数、缺失的章末要件数、代码清单缺口、
图缺口、载体密度缺口。全书起点是 86 个未引用 label、17 处改稿批注、3 项缺失要件、
64 个清单缺口、19 个图缺口——这些要到 O5、O6 和整个 A 批做完才归零，
所以每个工作包只需保证**不制造新的同类问题**，并把本包负责的那部分往下压。

第十一轮起，字数基线、篇幅上限、清单缺口、图缺口、载体密度只作提示（决策 D-2026-09-21-8）。

输出"通过"才可以 `git commit`。**不许修改 `check_textbook.py` 或 `gate_baseline.json`
来让自己通过**——那是绕路，不是完成。
