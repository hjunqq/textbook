# 第十轮修改记录：R10-01 教学衔接与阶段复现一致性

日期：2026-09-10。起点提交 `6705c8b`。本包对应《教材审核与修改方案-第十轮-2026-09-10.md》的 A01–A09。

## 已落实的修订

1. **九章学习衔接。** 保留九章、49个编号节和245个小节。在现有层次标注后补足前置知识，统一第4章4.5节的层次写法。各节清楚区分核心理论阅读、使用骨架的指导实践和拓展选读。前言明确：完成课程成果仍需完成相应实践，不能用“只读核心”代替实验验收。
2. **前言与教师资源。** Kafka镜像说明与 D6、Compose统一为 `apache/kafka:3.7.2`；配套资源入口改指当前发行文件与阶段索引，保留数据集的原始版本说明。教师课时从34讲授＋22实验校正为正文的30＋26，总计56。O1–O3收取解释、需求或模型证据，O4–O7收取运行结果；个人解释与脚本覆盖范围分开评分。
3. **第4、5章阶段边界。** `lesson45.html`明确为原生JavaScript核心页，Vue与详情路由是基于现有骨架的指导实践。修订4.8验收表、5.2启动步骤及预期结果：必须指定 `Lesson52Application`；S3起点无认证、只有固定数据子集，不能直接承接自动登录页。完成持久化、错误体和认证的S3终点才进行完整页面替换。同步 STAGES、TEACHING、MAPPING；顺手修正第4章正文残留 `useStationStore()` 为已有清单中的 `useAssetStore()`。
4. **第7章真实API边界。** 水位折线 `line` 通过 `setOption` 更新；`appendData`限支持增量渲染的系列，不能与 `dataset`混用。将原误用清单改为完整的 `createWindowChart` 模块，提供300点窗口、缺测空值及水位/雨量不同单位轴。同步 `window-chart.js`，并登记 `check_listings.py` 的 PAIRS；没有新增豁免。
5. **S5异步联动。** 增加 `series-controller.js`：选择改变时立即解绑并清空旧图，取消旧请求且校验序号；当前空结果、网络错误与重试均有页面反馈；卸载后迟到的结果不得创建图表。事件总线跳过本次分发中已解绑的旧处理器。首次对象高亮先于异步加载，避免初始化完成时覆盖用户的新选择。正文补足可观察结果、故障步骤和自测答案。
6. **生产入口。** Vite显式构建六个HTML入口，阶段页使用ES2022；主入口补齐文档类型、语言、字符集、视口和标题。浏览器验证发现并修正主入口缺少标题的问题。原有未跟踪 `frontend/dist/` 保留；本轮构建输出位于 `temp/r10-frontend-dist/`。
7. **事实表达。** 教师“按发生频率排序”“几乎每届”等无记录的判断改为待课堂验证的教学情境；第7章“某次课堂联调”改成可亲手制造并恢复的故障练习。第7章练习改为从现有水位数据截取1000条，避免误称数据集总量。
8. **单一编辑源与生成格式。** 前言的附录A编辑入口统一为实际参与编译和站点生成的 `output/appendix/answers.tex`，避免将旧Markdown误作现役编辑源。版本快照表回指实际镜像基线，并说明健康检查通过前端代理访问。核对Git原始字节后确认站点原来采用CRLF，tex2site显式保留这一约定，避免跨平台生成造成整份文件的格式差异；生成内容继续来自LaTeX，未手改网站正文。

## 素材来源与新写范围

- L1：检查 `d8beab0:output/chapters/chapter04.tex` 的“异步编程与Promise”和 `chapter07.tex` 中图表集成、面板生命周期等段落。保留其“应用状态与展示分工、异步过程、资源生命周期”的思路；这些思路已在现稿中展开，本包没有重复移植段落。
- L2：检查 `archive/docs-legacy/chapter07/section07-02.md`“Chart.js在三维场景中的集成”和 `section07-04.md`“图表实例管理”。旧Chart.js等技术写法不回填。没有引入旧技术栈或旧接口。
- 实际复用：现稿4.5.4“切换测点时旧数据覆盖新数据”、7.2“渲染器、性能预算与生命周期”“图表与三维对象的双向联动”、6.1.5对象绑定及8.1契约。S5把已经教授的两道竞态防线用于后续章节。
- 新写：节级导航、阶段起终点边界、S5故障记录与自测；两个可测试JS模块与8个回归用例；当前学习层次审计脚本、方案与核验记录。未新增编号节、图或表，替换1个错误代码清单，原标签保留。

## 篇幅与删减

门禁口径：235,962 → **238,086**（+2,124）；距240,000上限余**1,914**。逐章均未净减、均高于安全线。PDF为**477页**；第九轮历史记录为473页。本包未通过缩字号或改变门禁压页数。

没有删除或迁移实质教学单元。替换内容及保留位置：5.2错误的启动/替换承诺在原小节改为起点与终点两次验证；7.2错误的 `appendData` 折线实现由原位 `setOption` 清单替代，“增量更新、窗口、结构切换”教学目标均保留；原“课堂事件”在原位改为教学情境。STAGES重组后保留S0–S6及其验收说明。没有登记迁移量，`migration_ledger.json`仍为空。

历史方案还曾提出450页的版面目标。现稿在本轮开始时已超过这一目标；477页满足现行文字门禁，但不能据此宣称已经完成出版社版面终审。后续压页应按内容迁移或正式版式方案处理，不暗改字体或删掉失败例。

## 实际验证结果

| 验证 | 本轮结果 | 范围与证据 |
|---|---|---|
| `check_textbook.py --strict --build` | **通过** | XeLaTeX→Biber→XeLaTeX×2；477页，Error/Overfull/未定义引用/缺字均为0，70条文献全部引用，七项软指标均为0 |
| `check_listings.py` | **10组一致** | 从9组增加到10组；已有11行有理由差异保留，新清单无豁免 |
| `npm test -- --reporter=dot` | **55/55通过，8个文件** | 原有47项＋S5异步6项＋ECharts真实库2项；ECharts测试采用Node下SVG服务端渲染，不以jsdom伪装Canvas |
| `npm run build -- --outDir ../../../temp/r10-frontend-dist` | **通过，六个入口** | 使用原有Vite5.4.8与锁文件；输出目录不清空，保留用户原dist |
| Edge无界面浏览器＋生产构建 | **12项通过，0个pageerror** | 六入口HTTP200；S5绑定28对象、实际画布点击、旧响应、空数据、失败后重试；S2登录与空状态。见 `tools/review/r10-browser.json` |
| 学习路径审计 | **49节、245小节、49节有前置说明；无层次号错误** | 起点/终点台账见 `tools/review/r10-before/`、`r10-after/` |
| tex2site | **已从LaTeX重新生成** | 各章、附录均无转换悬空引用；图形未修改，无须重新绘制TikZ |
| `mkdocs build --strict -f mkdocs-ci.yml` | **通过，零警告** | 使用项目现有 `.venv`，只构建本地站点 |
| PDF人工抽看 | **完成** | 物理页5、181、195、319、322：前言、S1/S2表、S3启动说明、折线清单与S5故障教学；文字和表格在版心内 |
| `git -c core.whitespace=cr-at-eol diff --check` | **通过** | 正确识别网站原有CRLF；仍检查其他多余空白，保留原文件换行口径 |

首次完整编译遇到未定义引用，稳定源文件后重新执行完整流程并通过；最终结论采用最后一次完整门禁结果。新增ECharts测试最初在jsdom中触发Canvas未实现提示，已改用该文件独立的Node环境，最终测试输出无此提示。

构建保留既有大vendor分包方式，仍有大于500kB的体积提示；不是编译失败。本轮解决阶段页面漏发，后续性能专题可再按使用路径拆分依赖。S5测试未覆盖所有设备与所有相机姿态，不将一次无界面测试称为全平台兼容认证。

## 学习路径检查的限度

`audit_learning_path.py`逐项核对现役层次号，导出节/小节位置，并查找核心片段中显式 `ref/eqref/autoref` 指向拓展内容的记录，本轮为0。它不理解纯文字的章节号引用，也不能自动证明所有概念依赖已经解释；因此本包还按方案逐章人工核对导学与交付。原R0台账留作历史，不覆盖为当前统计。

## 保留的后续事项

- 学生试教：需要真实课堂完成时间、失败记录与个人答辩，尚未发生的记录不计作通过。
- S2详情路由等指导实践由学生在骨架上完成；阶段资料已经明确入口、任务与验收范围，不宣称所有书中代码均为配套成品。
- 本轮未修改后端、数据库和Compose，未重跑Docker整栈与Maven；第九轮已有结果属于历史证据，本轮不冒记为新验收。
- GitHub Pages发布与出版社最终页数安排沿用既有决策范围，本轮未发布、推送或更改仓库可见性。

## 提交前契约自检

未触碰 `pandoc/`、`build.bat`、既有文字门禁或基线；未新增外部图片；授权截图未进入网站；没有新增参数或端点；技术版本保持锁定；新代码清单有随文引出和配套入口；不设置代码行数上限；新增教学说明包含结果、故障和自测；回收来源、技术更新、新写范围、字数与替换位置均已登记。按一个工作包提交。
