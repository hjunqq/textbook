# 修改记录-第六轮（付印前外审整改）2026-08-07

依据《审查报告-第六轮-2026-08-07.md》，按 R1–R9 批次执行。每批过门禁，全程「通过」。
正文 221,940 → 222,672 字（+732，111%）。删除段落均逐段报备（见 R5、扫尾）。

## R1 全书代码清单编号修复（P0-1）
- 198 个 `\label{lst:...}` 由环境外移入 lstlisting 选项 `label={}`；删除 ch08 8.6 节 5 处内外双写。
  依据：main.aux 中环境外标签解析为小节号（如 ch03 六清单全为 3.2.2），选项内标签正确解析为 8.43 式清单号。
- tools/check_textbook.py 标签采集同步支持 `label={}` 写法（能力补齐，未改任何阈值）。

## R2 chapter08（P0-2/3/6 与 13 项 P1）
- 闸门例题数值改正：Q₁ 226→278、三孔 678→834、反算开度 5.25→4.34 m（复算依据见待核清单第15条）。
- reading 表：删单列 UNIQUE(event_id)（超表唯一索引必须含分区列，原 DDL 必报错）；
  create_hypertable 去掉不存在的双维度签名；新增复合唯一索引 (occurred_at, event_id)；
  ON CONFLICT、JPA 实体注释、正文说明全链同步，并把该限制写成教学内容。
- 角色口径：8.1.2 增加「审计员为四角色之外的只读扩展」声明；「场景编辑者/模型运行者」注明为专业分析员的操作面；时间线表责任角色改专业分析员。
- 幂等消费：DataIntegrityViolationException 捕获移出 @Transactional（原写法必抛 UnexpectedRollbackException 造成无限重投），正文补讲 rollback-only 陷阱。
- WarningEntity/WorkOrderEntity 补 @GeneratedValue；Controller 返回 Page<ReadingDto>（新增 DTO），实体补 getUnit。
- compose：postgres 镜像改 timescale/timescaledb-ha:pg16；kafka/redis 补 healthcheck；测试角色 DUTY→DUTY_OFFICER（2处）；消费 Topic 统一 qingyuan.reading.v1。
- 水量平衡公式补未分配水量 R 项并明确 R/U/ε 三者语义，计算题二同步。
- 工单状态机补 start 入口（实体+Controller+正文）；Nginx 补 SPA history 回退；resize 监听卸载；store 默认聚合改 raw；
  emissive 恢复保存原值；missing_eventId/duplicate_eventId 拆分；stateVersion/traceId 契约与实现对齐；
  「」直角引号、残句合并、超长段分为5段、「访问路径\ref」等句式修正、习题补「基于前章成果增量开发」。

## R3 chapter04（P0-4 与 12 项）
- 令牌键名统一：request.js 导出 TOKEN_KEY='access_token'，守卫与 Login.vue 导入使用（原三处 'token'/'access_token' 分裂，照抄必 401 且登录态永不失效）。
- request.js 补 Content-Type: application/json（原登录请求必 415）；401 豁免 /auth/ 路径（原密码错误会整页跳转吞掉提示）。
- 虚拟滚动补占位层与绝对定位（原示例滚动上限≈96px，1000 条只能看 3 行）。
- \texttt 内 4 处 \" 分音符隐患改直引号；A4-1/A4-5/S2 内部编号 4 处改为清单引用；「东湖水库」→清源水库；
  3 处 caption 与内容错位改正；240rem 注释修正；.env 注释 // → #；analyze 脚本换 vite-bundle-visualizer；
  检查脚本补 node:fs/path 导入；部署清单语言标注改 bash；环境变量名统一 VITE_API_BASE；
  4.5/4.6 两处与前文逐句重复的开头段改写为承接/收束句；client.js 标注为对照版。

## R4 chapter05（17 项）
- WaterReading 实体补 create/replaceIfVersion/archived（5.4 主线原调用三个不存在的成员，不可编译）。
- record 式访问器 2 处改 getter；BadCredentialsException::new 改带消息构造（原无此构造器）；
- SecurityFilterChain 补 .cors(Customizer.withDefaults())（原 CORS Bean 是死代码）；
- 发件箱发布器改事务内同步确认（原 whenComplete 回调越出事务），中断处理规范化；
- JwtService 补 parseRefreshAllowExpired 实现；迁移脚本补 reading_idempotency 建表；
- 重复定义收敛：CreateReadingRequest 精简版更名 ReadingValidationPayload、JwtProperties 复用 5.2 定义、消费组命名区分注释；
- ReadingMessage level double→BigDecimal；并发测试补 @Autowired/@Test、字符串水位改 BigDecimal；
- 6 处 caption/label 错位改正（含 lst:ch05-kafka-listener→lst:ch05-jwt-parse 更名）；A5-5 内部编号改 5.4 节；
- Maven 清单语言 Java→XML；MockMvc 测试契约对齐 ProblemDetail（$.code）与无版本路径；50.0 阈值 2 处注明 8.1 唯一来源。

## R5 chapter06（15 项）
- 【删除报备】删除 255 行旧稿段（273 字）：与 256 行为同段新旧两版并存，保留含 WCS/3D Tiles 的扩写版。
- WMTS 示例补 tileMatrixLabels（GeoServer 矩阵标识为 EPSG:4490:N 形式，缺失则全部 404）、style 改 default，正文补联调说明；
- MSE→中误差（RMSE）全链改正（正文、表、代码参数、习题）；CH/Z 3004 外业/3003 内业/3005 航摄归属改正；
- 「Cesium 1.11x」→「CesiumJS 1.12x+」；正文 2 处 TODO-核对 残留清理；
- 6 个清单 label+caption 按内容重命名（原 caption 系统性错位）；事件契约图补控制命令链路（原图文不符）；
- 三处残句合并成段；「不虚构十余个未定义类」等元话语删除；「现有式」及引用顺序修正；
- three/addons 表述更新（r156+ 包内置 exports 映射）；数学下标 \mathrm；分带「约为」删除；
- 高程转换代码补 ζ/N 参数注释；WFS BBOX 轴序说明；gridset 分辨率与 extent 自洽（0.703125→0.00390625）。

## R6 chapter07（13 项）
- 4 处 caption/label 错位改正（长按定时器/PointPicker/Möller–Trumbore/像素换算）；
- inspectReading 补缺测优先级守卫（原 unit/time 检查会把 missing 覆写为 suspect）；
- alignReadings：桶内质量按 suspect 合并、有值不判 missing、取值用过滤后数组、derived→aggregated、合并冗余分支；
- appendTail 缺测点以 null 保位断线（原直接丢弃会把两侧连成连续线，自违 258 行）；
- 长按定时器入口先清理（多指覆盖泄漏）并修正正文表述；专题图补 geo 组件与 registerMap 说明；
- 新增页面四态（正常/关注/告警/离线）与全书 NONE/蓝/黄/橙/红口径的映射说明；
- 补库引入方式说明（npm/import 与 CDN 全局两种）；SL 651 错配引用撤除（正式标准号入待核清单第12条）；
- 数据准备链图补「时间对齐/降采样」节点（原图文不符）；质量码五情形段厘清三值口径；
- Markdown 反引号 2 处改 \texttt；160,m 排版修正；scene 封装对象注释；pixelsToWorld 双签名统一；习题标数据来源。

## R7 chapter02（12 项）
- 一级 DFD 重画（云端渲染验证）：补用水户/调度审批/计量闸门三个外部实体与全部 6 条边界流，内部流全部命名，
  审批结果写入 D1，执行回执由计量系统写入 D3（原图违反本章自述的父子图平衡规则，习题8还要求学生核对它）。
- 二级图正文四处理列举与图对齐；D2 名称统一「水源与工程能力」；补回执反馈并入说明。
- 流程图与符号表对齐：处理节点改直角矩形，补开始/结束圆角端点。
- status 枚举补 checking（与状态图「待校核」对应）；ER 图 WaterPlan 补 FK canalId；2NF 行归属改正；规范化表补 \midrule。
- 段落归位：工作坊段→2.3.3，用例边界/故事粒度→2.3.5，决策表后的综合段独立为「结构化分析的交叉校验」小节，SRS 评审三段→评审检查单小节。
- 干系方清单补值班员、运维员口径、0层图→上下文图术语、受控撤回改「偏差中止（受控）」、引号后空格、习题防抄改法、NFR 表词汇贴合灌区。

## R8 chapter03（20 项）
- 【P0】活动图「椭圆形」→圆角矩形（与本章图一致）；类图 MonitoringRecord–AlarmEvent 多重性放反改正（记录端1..*、事件端0..*）。
- «extend» 示例语义修正：新增「查看历史同期对比」扩展用例，撤销「维护预警规则 extend 查看态势」；
- 规则/阈值维护职责统一归专业分析员（用例图执线者、283、463、1158 四处）；用例图第二执线者改专业分析员；
- 状态图正文「已通知→已确认」改「已通知→处置中」（与图一致）；MVC 图重画数据流（请求入 Controller、Model→View 单向、user 绕行下方）；
- 编号体例统一：12 处裸编号/汉字编号 + 32 处点编号 + 18 处括号编号全部转 \paragraph；
- 39 处全角引号旁多余半角空格清除；性能基线双源改指 8.1；AlertManager→AlarmManager 并按告警口径改写；
- ISO 25010 usability 译名「易用性」；泛化/继承术语统一；「中者」句改写；「在文档所涉及的软件项目中」删除；
- 引言开篇「模块详细设计」→「架构设计」；28测点注明 8.1；1123 承诺句与实际内容对齐；
- 【改写报备】「扩展性与复用性」空转段 262→180 字，改为清源案例可检验表述（原文见 git 历史）。

## R9 preface/ch01/ch09 与配套仓库（14 项）
- preface：资源发布状态自相矛盾句改写；案例名冠「清源水库」；扩展阅读去 Django；
  位移测点如实说明（仅元数据，观测留作练习生成）；warnings.json 口径改「无预警（NONE）与未评估」；
  compose 镜像描述同步 timescaledb-ha；\paragraph 标题体例 2 处（适用读者/先修知识）。
- companion 同步修复：docker-compose.yml 镜像改 timescale/timescaledb-ha:pg16（原 postgis 镜像无 timescaledb 扩展，
  init.sql 第2行必失败）；init.sql event_id 单列 UNIQUE 改复合唯一索引（与 ch08 口径一致）；README 内部编号清理。
- ch01：「专业人员」→专业分析员；两处碎句成段合并；权限表「提交调度建议」行改「起草上报/允许」；\cite 位置修正。
- ch09：9.2 导语与实际结构对齐并消除与 9.2.1 的重复；全角斜杠、9.2.2节、4 处范围号统一。
- ch08/README：14+1 处 A8-x/A4-5/S3 内部工作包编号改为章节或一般表述；迁移版本串 A8-1-001→QY-DDL-001。
- 待核事实清单补充第 11–15 条（高程异常区间、质量标识标准号、CH/Z bib 条目、companion 快照号、闸门公式复核）。

## 门禁与终验
- python tools/check_textbook.py --build：通过。编译输出 414 页，Overfull 0（新画的 ch02 一级 DFD 与 ch07 处理链
  首次编译各超宽一次，改用真实中文＋书版面宽度在探针文档中收敛后归零）。
- 正文 222,672 字（111%），label 403、悬空 0，文献 54/54，六项软指标全 0。
- main.aux 实证：全书清单标签解析为 3.1/4.1/8.43 式连续清单号，小节号形态残留 0 处（R1 生效）。
- 成书抽页视检（第 51、284、385 页）：一级 DFD 六条边界流与上下文图逐条对应、处理链含时间对齐节点、
  方案分支图与"6.4.17节"式跨章引用均正确渲染，无越界与标签相撞。
- 提示项 3 处"编者自述式/否定式排比"经人工逐条判断均为面向学生的正当表述（ch01:23、ch08 建设目标句、ch09 AI 定位句），保留。
