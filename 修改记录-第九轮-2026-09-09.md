# 修改记录（第九轮：基础递进与案例贯通）

## M0（2092c45 / 453fbb0 / f937f46）
- 决策 D1–D3 落地：篇幅上限 240,000 字、迁移登记机制、85% 安全线；学生起点与 56 学时；两段式路线。
- 门禁 check_textbook.py 新增 TOTAL_CEILING 与 migration_ledger；AGENTS 第一节与铁律 1/5/9 改写。

## R0 盘点（819fcde）
- tools/r0/：359 标题层次草表、203 清单台账、参数与接口核查、盘点报告。

## R1-0 接口契约统一（a123f19，决策 D4）
- 8.1 节新增 `tab:api-contract`（全书唯一接口契约）与 `tab:api-teach-faults`（教学接口故障注入）；8.3 原接口表并入删除；AGENTS 铁律 7 扩展。

## R1 第4章样章"数据请求与页面状态"（本次提交）
- 正文：4.5 节由"异步编程与Promise"重组为"数据请求与页面状态"，按"业务问题→所需知识→原理→完整例子→运行结果→状态模型→故障单元→验证记录→自测"组织：
  - 新增 4.5.2 完整例子（清单 detail.js，预期运行记录表）、4.5.3 页面状态模型（状态表、清单 state.js）、4.5.4 故障单元"切换测点时旧数据覆盖新数据"（时序图、原因、两道防线、清单 controller.js、验证记录表）、4.5.10 自测与下一步。
  - 原"事件循环与任务调度"降为 4.5.9 拓展；"认证契约测试与故障复盘"标为拓展，其竞态段落与清单 lst:ch04-a49-race-test 并入 4.5.4；原 lst:ch04-a45-abort 并入 lst:ch04-r1-race。
  - request.js / Vite 代理 / 登录联调三节标为指导实践；登录字段 account→username、/api/stations→/api/assets、TOKEN_KEY 统一从 request.js 导入，与契约一致。
  - 4.7/4.9 节网络路径改为契约路径（/api/assets、/readings/latest、loadLatest）；HTML/本地数据示例中的 ST-01 等标识留待 R3 全章递进时统一。
- 配套：companion/water-platform-demo/teaching-api/server.mjs（零依赖教学接口，已用脚本验证 10 种响应）；frontend/lesson45.html + src/lesson45/{detail,state,controller,main}.js（S2 阶段页，代码与书中清单逐字一致，已用无依赖 harness 验证四种状态与竞态修复）；tests/lesson45.test.js（vitest，本机未装依赖，未运行）；stores/monitoring.js 加序号校验（教师终点）；vite.config.js 加 /api 代理。
- 字数（门禁口径）：ch04 26,438 → 28,078（+1,640）；全书 226,201 → 228,3xx，余量约 11.6k。
- 未验证：`--build` 完整编译（本机需作者执行，重点看 4.5.4 TikZ 时序图与新增三张表的排版）；vitest 用例；课堂试教。
