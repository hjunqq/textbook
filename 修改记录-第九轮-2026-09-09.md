# 修改记录（第九轮：基础递进与案例贯通）

## M0（2092c45 / 453fbb0 / f937f46）
- 决策 D1–D3 落地：篇幅上限 240,000 字、迁移登记机制、85% 安全线；学生起点与 56 学时；两段式路线。
- 门禁 check_textbook.py 新增 TOTAL_CEILING 与 migration_ledger；AGENTS 第一节与铁律 1/5/9 改写。

## R0 盘点（819fcde）
- tools/r0/：359 标题层次草表、203 清单台账、参数与接口核查、盘点报告。

## R1-0 接口契约统一（a123f19，决策 D4）
- 8.1 节新增 `tab:api-contract`（全书唯一接口契约）与 `tab:api-teach-faults`（教学接口故障注入）；8.3 原接口表并入删除；AGENTS 铁律 7 扩展。

## R1 第4章样章"数据请求与页面状态"（本次提交）
- 正文：4.5 节由"异步编程与Promise"重组为"数据请求与页面状态"，按"业务问题→所需知识→原理→完整例子→运行结果→状态模型→故障单元→验证记录→自测"组织：
  - 新增 4.5.2 完整例子（清单 detail.js，预期运行记录表）、4.5.3 页面状态模型（状态表、清单 state.js）、4.5.4 故障单元"切换测点时旧数据覆盖新数据"（时序图、原因、两道防线、清单 controller.js、验证记录表）、4.5.10 自测与下一步。
  - 原"事件循环与任务调度"降为 4.5.9 拓展；"认证契约测试与故障复盘"标为拓展，其竞态段落与清单 lst:ch04-a49-race-test 并入 4.5.4；原 lst:ch04-a45-abort 并入 lst:ch04-r1-race。
  - request.js / Vite 代理 / 登录联调三节标为指导实践；登录字段 account→username、/api/stations→/api/assets、TOKEN_KEY 统一从 request.js 导入，与契约一致。
  - 4.7/4.9 节网络路径改为契约路径（/api/assets、/readings/latest、loadLatest）；HTML/本地数据示例中的 ST-01 等标识留待 R3 全章递进时统一。
- 配套：companion/water-platform-demo/teaching-api/server.mjs（零依赖教学接口，已用脚本验证 10 种响应）；frontend/lesson45.html + src/lesson45/{detail,state,controller,main}.js（S2 阶段页，代码与书中清单逐字一致，已用无依赖 harness 验证四种状态与竞态修复）；tests/lesson45.test.js（vitest，本机未装依赖，未运行）；stores/monitoring.js 加序号校验（教师终点）；vite.config.js 加 /api 代理。
- 字数（门禁口径）：ch04 26,438 → 28,078（+1,640）；全书 226,201 → 228,3xx，余量约 11.6k。
- 未验证：`--build` 完整编译（本机需作者执行，重点看 4.5.4 TikZ 时序图与新增三张表的排版）；vitest 用例；课堂试教。

## R2（进行中）全书分层与预备单元
- R2-1（cc6fd54）门禁新增软指标"未被 \ref 引用的代码清单"（起点 63，只降不升）。
- R2-2（d8550fe）人工复核层次草标 13 处改判（tools/r0_layers.py OVERRIDES），49 个编号节写入"本节层次"段（tools/r2_stamp_layers.py，幂等）；前言新增"每节的学习层次"说明与 56 学时方案表（tab:preface-56-hours）。复核后全书：核心 79k / 指导 56k / 拓展 79k。
- R2-3（eac9da1）附录B《开发环境与语言衔接预备》：P0 终端/端口/开发者工具（含读错误信息清单、面板表）、P2 Java→Maven→Spring（pom 片段、最小 Controller、依赖注入）、P3 表/键/SQL/事务（两张表、四条语句、事务清单），各带自测；第4章 4.5、第5章开头指向对应单元。取消 P1（学生已有编程基础）。tex2site 支持 prep 并对旧版 pandoc 回退。
- R2-4a（1da22cc）第3章接口路径归一到契约。
- **未做（等 R1 试教反馈再动）**：第 1–3 章按审核方案单元重组（含 ch03 5–7 单元重组、3.1.2 架构描述方法仅 132 字的核心小节充实、ch01 新增"一次请求的完整追踪"核心单元、ch02"含糊需求→验收条件"实例）。
- **验收（2026-09-09 作者本机）**：`python tools/check_textbook.py --build` 通过，PDF 451 页，Error 0 / Overfull 0（修 5 处表格列宽后）；R1、R2-1～R2-4a 的排版层面据此接受。
- **待作者本机**：`tools/tex2site`（build-tikz.sh + convert.py）重新生成 docs/ 后把附录B加入 mkdocs.yml / mkdocs-ci.yml 的 nav（两处均在"附录A"后加一行 `- 附录B 开发环境与语言衔接预备: appendix/prep.md`），再提交。
- 字数（门禁口径）：全书 229,826，余量约 10.2k。附录不计入门禁口径（与附录A一致）。

## R3-a 第4章标识与数据模型归一（本次提交）
- 全章代码里的数据字段统一为契约：`{assetId, displayName, value, unit, quality}`，示例对象编码 DAM-A-WL-01（案例库水位01，与数据集 stations.csv 一致）/ DAM-A-PZ-07；阈值与量程改用 8.1 节参数（汛限水位 165.5 m）与测点台账（148.0–171.6 m），不再用无出处的 5.0 m。
- 组件/仓库/路由改名：StationCard→AssetCard、StationRow→AssetRow、StationFilterPage→AssetFilterPage、StationList/Detail/Map→AssetList/Detail/Map、useStationStore→useAssetStore、路由 /stations→/assets（name asset-detail 等）。DOM id 与 CSS 类名（station-card 等）作为界面命名保留。
- 修正一处真实缺陷：setup 式 Pinia 仓库中动作 `loadLatest` 与导入的 `loadLatest` 同名导致无限递归，改为 `import { loadLatest as fetchLatest }`。
- 4.5 节与 lesson45 测试中的“案例水位01”改为数据集真实名称“案例库水位01”。
- 字数基本不变（ch04 28,244）；门禁通过（未含编译）。仍待 R3：4.4 与 4.6 DOM 重复开讲的合并、其余小节按七段模板改写。

## 第二段（决策 D5：不等试教全面推进）
- R2-4b（838ce06）第1章新增核心单元 1.2.1“一次查询的完整过程”：六环节追踪图、各环节失败原因与证据表、不写代码的验收。
- R2-4c（5343a8b）第2章 2.6 开头新增“含糊需求→验收条件”改写实例（四处空缺追问表、可判定性标准）。
- R2-4d（e78449a）第3章重组为六单元：从需求到模块 / 分层与职责（新增同一查询的三层时序图与职责表）/ 接口与数据流 / 模块化与依赖 / 架构选择 / 架构评审；21 个小节按单元重排，无删减；类图 pointId→assetId；层次：核心 20.5k / 指导 9.3k / 拓展 3.7k。
- R3-b（1d6e073）第4章结构收束为 8 节：原 4.6 DOM 收束练习并入 4.4；原 4.7 模块化 + 4.10 Vite 并为 4.8“调试、构建与阶段验收”，新增 S1/S2 阶段验收检查单；编号引用与 MAPPING 同步。
- R4（1d041a0）第5章：新增 5.2“第一个 HTTP 接口：从固定数据到契约”（三小节、两清单、三表、一个必遇失败、自测）；全章模型归一到契约（Reading/assetId/value/occurredAt、/api/assets、DAM-A-* 编码）；契约表补第5章写路径；原 5.8 性能与可观测性（拓展 2,698 字、5 清单）迁至新附录C“拓展专题”，正文留指引段（migration_ledger 登记，提交后清空）。
- R5（32bafbb）第6章新增核心单元 6.1.1“从一个几何体开始”（完整 Three.js r160 清单、三种黑屏排查表、自测）与 6.1.5“把测点挂到坝体上”（绑定/释放/查找清单、坐标与映射检查表）；第7章拾取回指绑定清单。
- R6（本次）第9章全书回顾改为 O1–O7 课程成果核对表。
- 层次重标：全书 49→50 个编号节均有“本节层次”；全书门禁口径 232,5xx 字，上限余量约 7.5k。
- **已完成（见 R7-b）**：`--build`、tex2site 生成 docs/、nav 加附录B与附录C。
- **R7 配套（未做）**：S3–S6 阶段包在 companion 中尚不存在（MAPPING 已如实标“课程实现”）；本轮只交付 S1/S2（lesson45 + teaching-api）。

## R7-b 验收修复与站点同步（本次提交）
- 编译：`--build` 首轮拦下 2 处 Overfull hbox——ch05“路径参数、查询参数与错误体”开头的契约路径 `/api/assets/{assetId}/readings/latest`（超 1.8pt）、ch06 表 `tab:ch06-bind-check` 第三列的 `scene.getObjectByName('assets')`（超 42.2pt）。按 preface 与 ch07 已有的 `llowbreak` 惯例在分隔符处加断点，不改文字、不改列宽。复跑通过：PDF 469 页，Error 0 / Overfull 0 / 未定义引用 0。
- 站点：`tools/tex2site/convert.py` 重新生成 docs/，追平 R2-4d/R4/R5/R6 的正文（ch03 重组、ch05 归一与 5.8 迁出、ch06 两个新单元、ch09 O1–O7 核对表、附录C 首次生成）；mkdocs.yml 与 mkdocs-ci.yml 两处 nav 在附录B 后加入附录C；`mkdocs build --strict -f mkdocs-ci.yml` 零警告通过。
- 图：本轮只有 2 张新图（ch01 图1.6 一次查询追踪、ch03 图3.10 三层时序），其余 91 张片段编号未位移、docs/ 中旧 SVG 继续有效，故只编这 2 张，避免 93 个文件的无谓变更。
- 环境：Windows TeX Live 按字族名找不到 Noto CJK。已装 Noto Sans CJK SC / Noto Sans Mono CJK SC 的 Regular+Bold 静态 OTF 到 `texmf-dist/fonts/opentype/google/notocjk/` 并 `mktexlsr`；用户级字体安装、`OSFONTDIR`、系统自带的可变字体 NotoSansSC-VF 三条路都验证不通，已连同可行做法写进 tools/tex2site/README.md，README 原“WSL/Linux 下运行”的限定随之取消。
- 字数（门禁口径）：232,504 → 232,504（`llowbreak` 不计汉字）；上限余量 7,496。软指标：未引用清单 62（基线 63），其余六项均为 0。无删减，迁移登记为空。

## R7-c S1 阶段包（第4章 4.2–4.4 节）（本次提交）
- **缺口**：S0–S6 七个阶段中，S1（不依赖框架与后端的静态监测页）此前只有验收标准（表 tab:ch04-stage-acceptance 的 S1 行）而没有实物。本包补齐。
- **配套**：`frontend/lesson44.html`（列表）+ `lesson44-detail.html`（详情）+ `lesson44.css`；`src/lesson44/` 下 assets.js（固定数据）、query.js（筛选/排序/编码校验，纯函数）、render.js（DOM 安全写入）、list.js（事件委托装配）、detail.js（?assetId= 深链接前身）；`tests/lesson44.test.js`。只需 `npm run dev`，不启动任何后端。
- **与书的对应**：不是另写一套代码，而是装配 4.2–4.4 已有清单——骨架 lst:ch04-html-shell、样式 lst:ch04-css-responsive、渲染 lst:ch04-a44-dom、委托 lst:ch04-a44-events、校验 lst:ch04-html-vue + lst:ch04-a44-dom-script。MAPPING.md 逐文件登记。
- **数据同源**：28 个测点的最新观测从 companion/datasets 的 stations.csv 与三份观测 CSV 取 occurredAt 最大的一条，两位小数；DAM-A-WL-01 = 166.84 m、DAM-A-PZ-07 = 185.09 kPa，与清单 lst:ch04-a44-dom 中的字面量一致。8 个位移测点无观测，value 为 null。
- **正文**：4.8.1 阶段验收表后新增两段——“S1 阶段包在哪里”（逐条指明每个文件装配了哪个清单，说明字段名即 8.1 契约，4.5 节换 fetch 不改字段）与“S1 有三处必须自己走一遍”（null 排序、空态“暂无测点”、故障 DAM-A-XX-99）。顺带把 lst:ch04-html-shell、lst:ch04-css-responsive、lst:ch04-html-vue 三个此前未被引用的清单纳入 
ef。
- **参数归一**：4.2 页面骨架清单里遗留的 `4.20 m` 与 2026-08-05 时间戳改为案例水库的实测值 166.84 m 与数据集快照时间 2026-07-05T03:55，符合铁律 7。
- **验证**：本机未装 npm 依赖，vitest 未运行；改用零依赖 node harness（最小 DOM 桩）实跑 21 条断言全通过，覆盖 S1 行要求的四种行为与注入故障。`--build` 通过，PDF 469 页，Error 0 / Overfull 0；`mkdocs build --strict` 零警告。
- **字数（门禁口径）**：232,504 → 232,902（+398），上限余量 7,098。软指标未引用清单 62 → 59（基线 63）。无删减。
- **R7 剩余**：S3（第5章，backend 已有骨架但无阶段包）、S5（第7章联动）、S6（第8章闭环）、S0 演示记录，以及教师资源与核心教学路线汇总。

## R7-d S3 阶段包与契约一致性（第5章）（本次提交）
- **S3 起点**：`backend/src/main/java/edu/example/lesson52/`（Lesson52Application + AssetController），由清单 lst:ch05-first-controller 与 lst:ch05-first-params 合并而成，除合并所需 import 外与书中逐字一致。刻意放在独立根包 `edu.example.lesson52`，否则会被完整工程的组件扫描一起扫到，两个 `/api/assets` 映射冲突、启动即失败。
- **S3 终点补缺**：契约端点 `GET /api/assets/{id}/readings/latest` 此前只有教学接口有，完整后端一直没实现（决策 D4 记录的遗留）。本包补齐：ReadingRepository 加派生查询、ReadingService 加 `latest()`、AssetController 加端点，按契约区分 404（对象不存在）与 204（尚无观测）。
- **契约错误体**：后端骨架此前没有任何全局异常处理，契约要求的 `{code, message, field?}` 从未落地。新增 `ApiExceptionHandler`，并兜住 5.2.2 节“一个会遇到的失败”——时间参数解析失败发生在进入控制器之前，控制器内的 `@ExceptionHandler` 接不到。
- **正文修的两处不一致**：(1) 清单 lst:ch05-first-controller 里位移测点写作 `DAM-A-DP-01`，台账（stations.csv）是 `DAM-A-D-01`，按铁律 7 归一；(2) 5.5 节清单 lst:ch05-problem-advice 产出的错误体是 `title` + `fields`（复数），与 8.1 契约的 `{code, message, field?}` 不符——8.1 是唯一来源，故改写清单使四个处理器产出契约形状，并补 MethodArgumentTypeMismatchException 处理器；相应改写一句正文。
- **正文新增**：5.2.3 加“S3 阶段包与契约核对脚本”一段（起点位置、独立根包的原因、三阶段核对命令）。
- **可执行验收**：新增 `teaching-api/contract-check.mjs`，把表 tab:ch05-first-verify 四行与契约错误体形状写成断言，`--stage=teaching|lesson52|full` 对三种数据来源跑同一套契约断言（按阶段裁剪对数据深度的期望）。**实跑教学接口 20 条断言全通过**。
- **编译验证**：本机无 mvn，改用 javac + 本地 .m2 依赖（Spring Boot 3.3.5 / Spring 6.1.14，`--release 17`）编译，lesson52 两个类与 qingyuan 侧改动的 8 个类均**编译通过**。**未运行**：Spring Boot 实际启动、`--stage=lesson52` 与 `--stage=full` 的核对（需 mvn 与数据库，留作者本机）。
- **门禁**：`--build` 通过，PDF 471 页，Error 0 / Overfull 0；`mkdocs build --strict` 零警告。字数 232,902 → 233,146（+244），上限余量 6,854。软指标未引用清单 59（基线 63）。无删减。

## R7-e S5 阶段包（第7章 曲线与三维联动）（本次提交）
- **配套**：`frontend/lesson74.html` + `src/lesson74/`——scene-bus.js（应用层场景封装：事件总线 + focusAsset，不 import three）、link.js（与清单 lst:ch07-link-controller 逐字一致）、picker.js（与清单 lst:ch07-point-picker 逐字一致，THREE 改模块导入；另加 firstAsset 跳过坝体回指测点）、main.js（装配，复用 S4 场景与教学接口观测）；`tests/lesson74.test.js`。运行依赖 S3 与 S4。
- **补书里点到但没给的那块**：清单 lst:ch07-link-controller 的注释写着“scene 是应用层的场景封装对象（带事件总线），不是 THREE.Scene 本身”，书中没有对应清单，本包用 scene-bus.js 补上；因为 chart 与 scene 都是注入的，整个联动逻辑不需要 echarts 与 WebGL 即可测试。
- **修一处书与包对不上的地方**：6.1.1 的“可观察结果”要求在控制台敲 `dam.position.y` 与 `camera.position.distanceTo(dam.position)`，但 ES 模块顶层变量不是全局的，照做得到 undefined。lesson61/first-scene.js 末尾改为 `Object.assign(window, {scene, camera, renderer, dam, controls})`（该行在书中清单之外，属阶段页附加），S5 的射线拾取也要用 camera 与 renderer。
- **正文**：7.3 节在多图表联动清单前加“S5 阶段包”一段，指明 scene 封装的位置与三处必试边界（下标越界、曲线里没有该对象、换对象前先解绑）。
- **验证**：零依赖 node harness 实跑 13 条断言全通过（高亮切换与复原、场景无此球、dispose、点图表→高亮、data 自带 assetId 优先、下标越界、点三维→highlight+showTip、曲线无该对象不发 action、解绑后不响应、拾取跳过坝体/祖先节点/空命中）。**未运行**：浏览器中的真实渲染与 vitest（本机无 node_modules）。
- **门禁**：`--build` 通过；`mkdocs --strict` 零警告。字数 233,146 → 233,341（+195），上限余量 6,659。软指标未引用清单 59。

## R7-f S6 阶段包（第8章 质量·预警·工单闭环）（本次提交）
- **定级模块**：`frontend/src/lesson84/classify.js`——`evaluable` 与 `level` 两个维度（质量不足是“未评估”，`evaluable=true` 且 `NONE` 才是“无预警”），质量码优先于分数，`score()` 在权重和不为 1 时抛错而不是悄悄归一化。阈值 0.30/0.50/0.70/0.85 与 warnings.json 的四条样例（0.36/0.58/0.74/0.92）一致。
- **教学接口扩展**：新增 `POST /api/warnings/{id}/ack`、`POST /api/work-orders`、`POST /api/work-orders/{id}/complete`（内存态），按 8.1 契约应答，实现 8.4 状态流转图的受控转移 open→acknowledged→assigned→handled→closed，没有 open 直接到 closed 的边。
- **闭环核对**：`teaching-api/closeloop-check.mjs`，覆盖观测→质量→预警→确认→工单→回写→归档，以及三条约束（未评估不能确认、没确认不能派单、已完成不能重复完成）。**实跑教学接口 20 条断言全通过**。
- **顺带修的工程问题**：两个核对脚本原用 `process.exit()`，在 Windows 上与 undici 未关闭的 keep-alive 套接字竞争，偶发 libuv 断言、退出码 127——断言全过却会被 CI 判为失败。改为设 `process.exitCode` 并主动关闭连接池，实测两脚本退出码均为 0。
- **正文**：8.4 节补齐橙红两级的示例阈值（此前只有蓝色 0.30 与 0.50 分界写在书里，0.70/0.85 只存在于数据集），并说明 warnings.json 的四条样例按这组分界生成；质量码下游处理表前新增“S6 阶段包”一段。
- **测试**：`tests/lesson84.test.js`，前三个用例与清单 lst:ch08-classification-test 的三个 JUnit 断言一一对应（同一份口径在前后端各钉一次）。零依赖 harness 实跑 16 条断言全通过。
- **门禁**：`--build` 通过；`mkdocs --strict` 零警告。字数 233,341 → 233,545（+204），上限余量 6,455。软指标未引用清单 59。

## R7-g 配套发行汇总（本次提交）
- **S0 演示记录**：新增 `teaching-api/record-demo.mjs`，对运行中的教学接口**实录**一次“查看某测点历史观测”的完整过程（五个环节 + 四种失败情形），生成 `S0-demo-record.md`（163 行，已入库）。第1章不写代码但要能说明六环节各去哪里找证据，没有演示环境的课堂可直接投影。
- **STAGES.md**（学生）：S0–S6 总表——入口、运行依赖、必须能演示的行为、注入故障、验收证据，逐阶段给出启动命令；末尾说明七个阶段共用同一份数据与同一份契约。
- **TEACHING.md**（教师）：核心教学路线与 56 学时分配表、O1–O7 验收证据表、学生最常卡住的六处（端口冲突、竞态、时间参数加号、三维黑屏、suspect 进评分、未评估当无预警）、实验课收法（能跑的东西 + 一条四行失败记录）、机房环境清单。
- **README**：新增“先看哪一份”导航表与“三个可执行的验收脚本”一节；修掉过时的一句（“vitest：6 个断言”，现有 6 个测试文件）。
- **MAPPING 准确性核对**：逐条检查表中引用的文件是否存在，全部命中；v0 行补 S0 演示记录，末尾增加三份读者索引。
- **门禁**：`check_textbook.py` 通过。本包只动配套与文档，正文未改，字数仍为 233,545。
- **R7 完成情况**：S0–S6 七个阶段全部有实物与验收方式；R7 定义的四项交付（每阶段可复现、映射表准确、教师资源、核心教学路线）齐备。

## R8 全书验收（本次提交）
- **严格模式通过**：`python tools/check_textbook.py --strict --build` 输出“通过”，七项软指标**全部归零**（起点：未引用 label 86、未引用清单 63、清单缺口 64、图缺口 19、改稿批注 17、缺失要件 3、载体密度 0）。PDF 473 页，Error 0 / Overfull 0 / 未定义引用 0。
- **补齐 59 处清单随文引出**（铁律 3）：ch04 14、ch05 16、ch06 15、ch07 12、ch08 1、附录C 1。每处都写明该清单回答什么问题、要注意哪一行，不用统一句式——门禁的反模板规则会拦下同一句话盖三次以上。多数是并入既有段落而非另起一段。
- **顺带修的参数不一致**：4.4 节正文“某水库汛期警戒水位统一设为5.0米”与清单里的 165.5 m（8.1 参数表）矛盾，改为按汛限水位表述。
- **新增 `tools/check_listings.py`**：核验书中清单与 companion 文件是否逐字一致——这是“书中代码与发行包对应”这条工程验收要求此前唯一没有自动检查的部分。9 组清单全部通过；6 行已确认差异各写明原因（ES 模块 export 前缀、包名避让、清单合并后通配导入覆盖具名导入）。核验中发现并修掉一处真差异：lesson52 的 AssetController 原用逐个导入，书中是通配导入，以书为准改配套（改后 javac 复验通过）。AGENTS 第五节登记该工具与维护方式。
- **门禁提示的 4 处人工判断**：ch01 的“本教材把智慧水利理解为”是面向学生的工作定义，ch08/ch09 的三处“……而不是……”是给学生的概念对照，均不指向上一稿或审稿人，不属于铁律 4 要清的改稿批注，保留。
- **字数（门禁口径）**：233,545 → 235,771（+2,226），上限余量 **4,229**。
- **未完成，需作者**：学生试用反馈（R8 交付项之一，须真实课堂试教产生，本轮无法代做）；`--stage=lesson52` 与 `--stage=full` 的契约核对（需 mvn 与数据库）；vitest（需 npm 依赖）。

## R8-b 实跑验证（vitest / mvn / lesson52 契约核对）（本次提交）
上一包标为“需作者本机”的三项在本机装齐工具链后实跑。**跑起来一共暴露 6 个此前未发现的缺陷**，其中 4 个会让配套工程根本用不了。

**vitest（npm ci 后）**：首跑 41 通过 / 6 失败。
1. `vite.config.js` 从未配 `test.environment`，vitest 默认 node 环境，所有操作 DOM 的用例以 `document is not defined` 失败——jsdom 早就写在 devDependencies 里却没接上。R1 写的 lesson45 竞态用例因此**从未真正跑过**。已补 `test: { environment: 'jsdom' }`。
2. `src/lesson45/detail.js` 末尾的页面入口块（查 #latest、登录、渲染 PZ-07）写在模块顶层，任何 import 都会触发：controller.js 与 main.js 导入它时会重新登录并抢先渲染一次，测试里也会发真实请求。该块属于 4.5.2 阶段（那时 detail.js 是页面唯一脚本），装配后应删除。已从配套删除，并在 4.5.2 新增“最后四行只属于本小节”一段说明原因；check_listings 登记为已确认差异。
3. 连带发现 `main.js` 从不调用 `login()`，一直靠 detail.js 顶层那次登录。删除入口块后必须自己登录，已补。
   修复后 **47/47 全部通过**（6 个测试文件）。

**Maven（本机无 mvn，取 3.9.9 免安装包；`~/.m2/settings.xml` 配了未启动的 127.0.0.1:7891 代理，改用临时 settings 绕开，未改动用户配置）**：
4. `mvn package` 失败：R7-d 新增 `Lesson52Application` 后工程有两个 `@SpringBootApplication`，`spring-boot-maven-plugin:repackage` 报“候选主类不唯一”。**这意味着 `docker compose up --build` 连镜像都构建不出来**。已在 pom 的 repackage execution 上指定主类——注意只能配在 execution 级，配在插件级会连 `spring-boot:run` 一起锁死，命令行的 `-Dspring-boot.run.main-class` 失效（此坑已实测并写进 pom 注释）。
5. `Lesson52Application` 启动即失败：完整工程 classpath 上有 JPA/Kafka/Security，自动配置会去连 PostgreSQL 并要求认证，而 5.2 明说“数据库到 5.4 节才接入”“本节还没有认证”。已排除 6 项自动配置（含 Actuator 的 ManagementWebSecurityAutoConfiguration）。
6. **影响整个后端**：pom 没有继承 `spring-boot-starter-parent`（只导入 BOM），因此从未传 `-parameters`；`@PathVariable String assetId` 这类不写参数名的写法在运行期抛 `parameter name information not available via reflection` 并返回 500。也就是说完整工程所有带路径/查询参数的接口都是坏的。已显式声明 maven-compiler-plugin 并置 `<parameters>true</parameters>`（属性写法 `maven.compiler.parameters` 实测不生效），用 javap 确认 MethodParameters 已写入字节码。
   `mvn test`：JUnit **5/5 通过**。

**`--stage=lesson52` 契约核对**：修完上述问题后 **16/16 通过**。其中一条按阶段做了区分——5.2.2 节“一个会遇到的失败”预言此阶段的时间参数 400 还不是契约错误体（类型转换发生在进入方法之前，5.5 节才用 `@RestControllerAdvice` 统一），脚本原来一律断言“必须是契约错误体”，与书矛盾；改为在 lesson52 阶段断言“尚未统一”，起点包若在这条上通过反而说明书里那个失败复现不出来。
   回归确认教学接口仍全绿：契约 20/20、闭环 20/20。

**顺带修的安全问题**：`.gitignore` 没有任何 secrets 规则，`companion/water-platform-demo/secrets/*.txt` 一旦生成就可能被提交，而 README 明写“正式文件不入库”。已加忽略规则（保留 `*.txt.example`）。

**`--stage=full` 未完成**：`docker compose up -d --build` 拉不到镜像，`registry-1.docker.io` 在本机网络层不通（curl 同样超时，而 npm registry 与 maven central 正常），应为那个未启动的代理所致。数据库栈起不来，`--stage=full` 与 `smoke.sh` 仍未验证。

**门禁**：`--strict --build` 通过，`check_listings.py` 9 组一致，`mkdocs --strict` 零警告。字数 235,771 → 235,923（+152），上限余量 4,077。
