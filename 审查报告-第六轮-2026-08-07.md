# 审查报告-第六轮（付印前外审）2026-08-07

审查身份：外审专家组三视角——内容专家（智慧水利+软件工程）、出版社资深编辑、一线任课教师。
方法：8 个并行审读代理逐行读完当前版全部章稿（md5 与磁盘一致，行号已核对），关键指控由主审用编译产物（main.aux）、数值复算、原文比对逐条复核。修改权限：允许删改，逐段报备（编者已批准）。

## 一、已复核确认的 P0（付印硬伤，6 项）

P0-1 全书性【LaTeX】约 190 个代码清单的 `\label{lst:...}` 写在 lstlisting 环境之外，
  绑定到小节计数器。main.aux 实证：ch03 六个清单全部解析为"3.2.2"，正文所有
  "清单\ref{...}"印出的是小节号且互相重复；写进 lstlisting 选项 `label={...}` 的
  （lst:ch08-compose）正确解析为"8.43"。另 ch08 8.6 节 5 处内外双写，multiply-defined。
  → 修法：全书 \label 移入 lstlisting 选项；删 5 处重复。

P0-2 ch08 闸门例题数值错误【内容】：例题给 H=16.2m，但 Q₁=226、三孔 678、反算开度
  5.25m 对应的是 H≈10.7m。复算：Q₁=0.65×8×3×√(2×9.81×16.2)=278.1，三孔≈834，
  a=1200/(3×0.65×8×√(2×9.81×16))=4.34m。学生最会照抄的示范例题。

P0-3 ch08 数据库 DDL 不可执行【内容】：reading 表 `event_id UNIQUE` 不含分区列，
  create_hypertable 必报错；且 create_hypertable(relation, by_range(...), by_hash(...))
  签名不存在（第二维度须 add_dimension）。706 行 ON CONFLICT(event_id)、JPA unique=true
  的幂等设计全建在这条不成立的约束上。

P0-4 ch04 令牌键名分裂【内容】：request.js 读写 'token'（1133/1151），路由守卫与
  Login.vue 用 'access_token'（1807/1867）。照抄书中代码：登录成功但所有请求 401，
  且 401 清理 'token'、守卫查 'access_token'，登录态永不失效。1232 行正文恰好警告过此事。

P0-5 ch03 UML 图文互反【内容】：181 行类图多重性放反（图：记录0..1/事件1..*；正文285行：
  记录可不触发告警、事件至少关联一条记录——正确应为记录端0..*/事件端1..*方向对调）；
  189 行"活动…通常用椭圆形表示"（正确：圆角矩形；本章自己的图 209 行画的就是圆角矩形）。

P0-6 ch06 整段重复【编辑】：255 行（旧稿）与 256 行（扩写版）为同段新旧两版并存，付印事故。

## 二、按章 P1/P2 清单（外审代理原文，主审抽核）

各章问题以审读代理输出为准，逐条修复时再二次核对原文。分布概览：
- preface/ch01/ch09（12条）：配套资源自洽性（发布状态自相矛盾、位移观测数据缺位、
  TimescaleDB 镜像缺位待核）、"水利工程安全监测平台"未冠案例名、9.2 导语与结构错位、
  9.1 与学习目标组织不一、碎句成段 2 处、范围号/斜杠/引用体例。
- ch02（14条）：一级 DFD 边界流全缺（自违 551 行平衡规则，习题8还让学生核对）；
  二级图与正文四处理不一致；流程图违反自设符号约定；status 枚举缺"待校核"；
  ER/字典/规范化三处 canalId 归属矛盾；表缺 \midrule；小节错挂 3 处；重复段压缩。
- ch03（16条）：extend 示例语义不成立；状态图超时转移图文不符；规则维护职责在
  运维员/专业分析员间漂移；MVC 图请求指向 View；五套编号体例并存；19 行引号旁空格；
  性能基线双源；AlertManager 命名；"中者"等语病；空转段落 3 处压缩。
- ch04（14条）：request.js 无 Content-Type 致登录 415；401 一刀切重定向吞掉登录错误提示；
  虚拟滚动示例无占位层不可用；\"\# 分音符隐患；A4-x/S2 内部编号泄漏 5 处；caption 与
  内容错位 4 处；200rem≠3840px；"东湖水库"；DOM 章节整段重复；.env 用 // 注释。
- ch05（17条）：replaceIfVersion/create/isArchived 方法不存在（5.4 主线不可编译）；
  record 访问器风格与 getter 实体矛盾；BadCredentialsException::new 无此构造器；
  CORS Bean 未接入过滤链；发件箱 whenComplete 回调越事务；caption/label 错位 6 处；
  A5-5 残留；重复定义 record/JwtProperties/@RestControllerAdvice；消费者组名三处漂移；
  迁移脚本给不存在的表建索引。
- ch06（15条）：WMTS 缺 GeographicTilingScheme 说明+tileMatrixLabels 必 404；
  MSE/RMSE 混用（正文+表+代码+习题）；CH/Z 3004/3005 标准归属错（内业是 3003）；
  "Cesium 1.11x"违反基线口径；TODO-核对残留在正文非注释；three/addons 别名表述过时；
  清单 caption 错位 5 处；风险枚举 "warning" 不合四色口径；残句成段 3 处；元话语 2 处。
- ch07（13条）：caption 错位 4 处；inspectReading 质量码优先级被覆写；alignReadings
  聚合语义两缺陷；appendTail 丢缺测点自违 258 行；Markdown 反引号 2 处；160,m 排版错；
  专题图 geo 组件缺失必报错；页面四态与预警五级映射全章未交代；slt6512014 引用键
  张冠李戴；库引入方式未交代。
- ch08（23条）：五角色"审计员"+8.5 多个未映射角色；DataIntegrityViolationException
  catch 后必 UnexpectedRollbackException；实体缺 @GeneratedValue；compose 镜像无
  TimescaleDB 扩展+healthcheck 缺失；Topic 名与部署配置不一致；测试角色 DUTY vs
  DUTY_OFFICER；Controller 直接返回 Page<Entity> 自违 DTO 原则；resize 监听未清理；
  store 默认 agg=15m 与组件解析结构不匹配；水量平衡计算题二"未分配水量"在公式中无
  对应项；「」直角引号；残句成段；Nginx 无 SPA 回退；工单状态机缺 start 入口。

## 三、修改计划（批次）

R1 全书 lst 标签体例修复（P0-1）＋ch08 双写清理 —— 机械改动，先行
R2 ch08 三处 P0（闸门数值、hypertable/幂等链、角色口径声明）＋ch08 P1 代码链
R3 ch04 认证链路（键名统一、Content-Type、401 豁免）＋caption/内部编号清理
R4 ch05 代码自洽（实体方法补齐、访问器统一、CORS 接入、发件箱事务）＋caption 修复
R5 ch06（重复段、WMTS、RMSE、CH/Z、版本口径、TODO、caption）
R6 ch07（caption、质量码守卫、appendTail、映射表、排版）
R7 ch02 图文自洽（DFD 补边界流、流程图、状态枚举、ER 口径）
R8 ch03 图文对照＋体例统一（编号、引号空格、职责归属、MVC 图）
R9 preface/ch01/ch09 零散＋全书元话语/术语扫尾
R10 终验：--build 编译、aux 抽查、渲染抽页、汇总报告

每批完成即跑门禁；删除段落逐条记入修改记录-第五轮.md。
