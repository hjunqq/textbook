\section{}\label{section}

\subsection{}\label{section-1}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
\item
  Chart.jsThree.js
\item
  Three.js
\item
\end{enumerate}

\subsection{}\label{section-2}

Web

\subsection{}\label{section-3}

!!! info ``\,''

\begin{verbatim}
### [ ](section07-01.md)
- Chart.js
- Three.js
- ### [ ](section07-02.md)
- - 
- ### [ ](section07-03.md)
- - 
- ### [ ](section07-04.md)
- - 
- ## 
\end{verbatim}

\subsubsection{}\label{section-4}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****GPS
\item
  ****Three.js
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \textbf{(LOD)}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsubsection{Chart.jsThree.js}\label{chart.jsthree.js}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

Chart.jsThree.js - \textbf{Canvas}Chart.jsCanvasThree.js - **** - ****

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

Chart.jsCPUThree.jsGPU - ****Chart.jsCanvas - **** - ****

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{}\label{section-5}

\subsubsection{Chart.jsThree.js}\label{chart.jsthree.js-1}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

Chart.jsThree.js

\begin{verbatim}
// Canvas
class ChartTextureBridge {
    constructor(width = 512, height = 512) {
        // Canvas
        this.canvas = document.createElement('canvas');
        this.canvas.width = width;
        this.canvas.height = height;
        
        // Three.js
        this.texture = new THREE.CanvasTexture(this.canvas);
        this.texture.needsUpdate = true;
    }
    
    updateChart(chartData) {
        // Chart.js
        const ctx = this.canvas.getContext('2d');
        new Chart(ctx, {
            type: 'line',
            data: chartData,
            options: {
                responsive: false,
                animation: false
            }
        });
        
        // Three.js
        this.texture.needsUpdate = true;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

Chart.js - ****Three.js - **** - ****

\begin{verbatim}
const chartConfig = {
    animation: false,
    responsive: false,
    plugins: {
        tooltip: { enabled: false },
        legend: { display: true }
    }
};
\end{verbatim}

\subsubsection{}\label{section-6}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{verbatim}
// 
class CoordinateConverter {
    constructor(originLng, originLat) {
        this.origin = { lng: originLng, lat: originLat };
    }
    
    // 
    geoToScene(lng, lat, elevation = 0) {
        // 
        const x = (lng - this.origin.lng) * 111320;
        const z = (lat - this.origin.lat) * 110540;
        return new THREE.Vector3(x, elevation, z);
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{verbatim}
// 
class SensorRenderer {
    constructor(scene) {
        this.scene = scene;
        this.sensors = [];
    }
    
    // 
    addSensor(data) {
        const geometry = new THREE.SphereGeometry(2, 16, 12);
        const material = new THREE.MeshBasicMaterial({
            color: this.getStatusColor(data.status)
        });
        
        const mesh = new THREE.Mesh(geometry, material);
        mesh.position.copy(data.position);
        mesh.userData = data;
        
        this.scene.add(mesh);
        this.sensors.push(mesh);
    }
    
    // 
    getStatusColor(status) {
        const colorMap = {
            'normal': 0x00ff00,
            'warning': 0xffff00, 
            'error': 0xff0000
        };
        return colorMap[status] || 0x888888;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsubsection{}\label{section-7}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{verbatim}
// 
class RaycastController {
    constructor(camera, renderer) {
        this.camera = camera;
        this.renderer = renderer;
        this.raycaster = new THREE.Raycaster();
    }
    
    // 
    detectObject(event, objects) {
        // 
        const rect = this.renderer.domElement.getBoundingClientRect();
        const mouse = new THREE.Vector2(
            ((event.clientX - rect.left) / rect.width) * 2 - 1,
            -((event.clientY - rect.top) / rect.height) * 2 + 1
        );
        
        // 
        this.raycaster.setFromCamera(mouse, this.camera);
        
        // 
        const intersects = this.raycaster.intersectObjects(objects);
        return intersects.length > 0 ? intersects[0] : null;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{verbatim}
// 
class InteractionController {
    constructor(sensors, raycastController) {
        this.sensors = sensors;
        this.raycastController = raycastController;
        this.selectedSensor = null;
    }
    
    // 
    handleClick(event) {
        const intersection = this.raycastController.detectObject(event, this.sensors);
        
        if (intersection) {
            this.selectSensor(intersection.object);
            this.showInfoPanel(intersection.object.userData);
        } else {
            this.deselectSensor();
            this.hideInfoPanel();
        }
    }
    
    // 
    selectSensor(sensor) {
        if (this.selectedSensor) {
            this.selectedSensor.material.emissive.setHex(0x000000);
        }
        
        this.selectedSensor = sensor;
        sensor.material.emissive.setHex(0x444444);
    }
}
\end{verbatim}

\subsection{}\label{section-8}

\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
& & \\
Canvas & 2D Canvas3D & \\
& & \\
& & \\
\end{longtable}

\subsection{}\label{section-9}

!!! note ``\,''

\begin{verbatim}
**Chart.jsThree.js**
- Canvas
- CanvasThree.js
- ****
- - 
- ****
- - 
- ## 
\end{verbatim}

Chart.jsThree.js

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
\item
  Chart.jsThree.js
\item
\item
\end{enumerate}

\subsection{}\label{section-10}

!!! tip ``\,''

\begin{verbatim}
****
- - 
- ****
- - 
- ****
- - 
- ## 
\end{verbatim}

\subsubsection{}\label{section-11}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  ****Chart.jsThree.jsCanvas
\item
  ****Three.js
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsubsection{}\label{section-12}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  ****1000
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsubsection{}\label{section-13}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsection{}\label{section-14}

{[}1{]} Three.js Development Team. Three.js Documentation{[}EB/OL{]}.
{[}2024-08-27{]}. https://threejs.org/docs/.

{[}2{]} Chart.js Team. Chart.js Documentation{[}EB/OL{]}.
{[}2024-08-27{]}. https://www.chartjs.org/docs/.

{[}3{]} WebGL Working Group. WebGL Specification{[}S{]}. Khronos Group,
2023.

\section{7.1}\label{section-15}

\subsection{}\label{section-16}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsection{7.1.1}\label{section-17}

\subsubsection{}\label{section-18}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsubsection{}\label{section-19}

\begin{longtable}[]{@{}lllll@{}}
\toprule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
**** & (m) & 5-15 & ±1cm & \\
**** & /(m³/s) & 15-30 & ±3\% & \\
**** & (kPa) & 1-5 & ±0.5\% & \\
**** & (mm) & 1 & ±0.1mm & \\
**** & (℃) & 30 & ±0.2℃ & \\
**** & & 2-4 & & \\
\end{longtable}

\paragraph{}\label{section-20}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****N
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{verbatim}
// 
class WaterLevelProcessor {
    processReading(reading) {
        // 1. 
        if (!this.validateReading(reading)) {
            throw new Error('');
        }
        
        // 2. 
        const smoothedValue = this.calculateSmoothedValue();
        
        // 3. 
        const alertLevel = this.determineAlertLevel(reading.value);
        
        return {
            originalValue: reading.value,
            smoothedValue: smoothedValue,
            alertLevel: alertLevel
        };
    }
}
```xml
****

- ****
- ****
- ****
- ****

## 7.1.2 

### 

****

- ****
- ****
- ****
- ****

****

****

**1. **

- ****10-30
- ****
- ****

**2. **

- ****
- ****
- ****

**3. **

- ****
- ****
- ****

```javascript
// 
class RealTimeDataProcessor {
    async startProcessingStream(dataSourceId, config) {
        // 
        const dataQueue = new DataQueue({ maxSize: config.queueSize });
        
        // 
        const processingNode = new ProcessingNode({
            dataQueue: dataQueue,
            processors: this.createProcessorChain(config)
        });
        
        // 
        processingNode.start();
    }
    
    async receiveData(dataSourceId, rawData) {
        // 
        const processedData = {
            ...rawData,
            receiveTime: Date.now(),
            sourceId: dataSourceId
        };
        
        // 
        await this.getDataQueue(dataSourceId).enqueue(processedData);
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsubsection{}\label{section-21}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****TB
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{7.1.3}\label{section-22}

\subsubsection{}\label{section-23}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{longtable}[]{@{}lllll@{}}
\toprule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
**** & & \textless1\%:, 1-5\%:, \textgreater5\%: & 25\% & \\
**** & & \textless2\%:, 2-5\%:, \textgreater5\%: & 30\% & \\
**** & & :, : & 20\% & \\
**** & & \textless30s:, 30s-2min: & 15\% & \\
**** & & \textgreater95\%:, 90-95\%: & 10\% & \\
\end{longtable}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{verbatim}
// 
class DataQualityAssessment {
    async assessDataQuality(dataSet, metadata) {
        const results = {};
        
        // 
        results.completeness = await this.assessCompleteness(dataSet);
        results.accuracy = await this.assessAccuracy(dataSet);
        results.consistency = await this.assessConsistency(dataSet);
        
        // 
        const overallScore = this.calculateWeightedScore(results);
        
        return {
            dimensions: results,
            overallScore: overallScore,
            qualityLevel: this.mapScoreToLevel(overallScore)
        };
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{7.1.4}\label{section-24}

\subsubsection{}\label{section-25}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\tightlist
\item
  A''water\_level'' → ``waterLevel''
\item
  B''WL'' → ``waterLevel''
\item
  C''\,'' → ``waterLevel''
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{itemize}
  \tightlist
  \item
    psibar
  \end{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  UnixISO 8601
\item
  \begin{itemize}
  \tightlist
  \item
    UTC
  \end{itemize}
\end{itemize}

\begin{verbatim}
// 
class DataStandardizer {
    async standardizeData(sourceData, sourceType) {
        const rule = this.conversionRules[sourceType];
        
        // 1. 
        let standardData = await this.mapFields(sourceData, rule.fieldMapping);
        
        // 2. 
        standardData = await this.convertUnits(standardData, rule.unitConversion);
        
        // 3. 
        standardData.timestamp = this.standardizeTimestamp(standardData.timestamp);
        
        // 4. 
        standardData = this.normalizeDataTypes(standardData);
        
        return standardData;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{}\label{section-26}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{7.2Chart.jsECharts}

\section{7.2}\label{section-27}

\subsection{}\label{section-28}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Chart.js/ECharts}2D3D
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsection{7.2.1 Chart.js}\label{chart.js}

\subsubsection{Chart.js}\label{chart.js-1}

\textbf{Chart.js}JavaScriptAPIWebChart.js

\paragraph{Chart.js}\label{chart.js-2}

\begin{longtable}[]{@{}llll@{}}
\toprule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
**** & & & \\
**** & & & \\
**** & / & & \\
**** & & & \\
**** & & UI & \\
\end{longtable}

\subsubsection{Chart.jsThree.js}\label{chart.jsthree.js-2}

Chart.js

\textbf{Chart.jsThree.js}DOMWebGL

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{1. HTML} Chart.jsHTML CanvasThree.jsGPUHTMLWebGL - ****HTML -
\textbf{html2canvas}html2canvasDOMCanvas - ****CanvasWebGL

\textbf{2. }

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\textbf{3. }

\begin{itemize}
\item
  ****Chart.js
\item
  ****WebGL
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{verbatim}
// Chart.jsThree.js
class ChartIntegrationManager {
    createChart(chartId, config, position) {
        // 1. HTML
        const container = this.createOffscreenContainer(chartId);
        
        // 2. Chart.js
        const chart = new Chart(container.getContext('2d'), {
            type: config.type,
            data: config.data,
            options: this.getThreeDOptimizedOptions(config)
        });
        
        // 3. 
        const chartPlane = this.createTexturedPlane(container, position);
        this.threeScene.add(chartPlane);
        
        return { chart, plane: chartPlane };
    }
    
    updateChartData(chartId, newData) {
        const chart = this.getChart(chartId);
        // 
        chart.data = newData;
        chart.update('none'); // 
        // WebGL
        this.refreshTexture(chartId);
    }
}
```xml
****

- ****2512x384GPU
- ****
- ****Canvas
- ****

## 7.2.2 ECharts

### ECharts

**ECharts**ECharts

#### ECharts

- ****
- ****-
- ****
- ****

**ECharts**Chart.jsECharts

**ECharts**

**1. -**
- - ****
- ****-
- ****
- ****

**2. **

- **Y**Y
- ****
- ****/
- ****

**3. **

- ****
- ****
- ****
- ****

```javascript
// ECharts
class WaterChartsManager {
    createStageDischargeChart(chartId, data) {
        const chart = echarts.init(this.getContainer(chartId));
        
        // 
        const option = {
            title: { text: '-' },
            tooltip: {
                formatter: (params) => {
                    const point = params[0];
                    return `: ${point.value[0].toFixed(2)}m<br/>
                            : ${point.value[1].toFixed(2)}m³/s`;
                }
            },
            xAxis: { name: ' (m)', type: 'value' },
            yAxis: { name: ' (m³/s)', type: 'value' },
            series: [
                { name: '', type: 'scatter', data: data.observed },
                { name: '', type: 'line', data: data.fitted, smooth: true }
            ],
            // 
            brush: { toolbox: ['rect', 'polygon'] },
            dataZoom: [{ type: 'slider' }, { type: 'inside' }]
        };
        
        chart.setOption(option);
        return chart;
    }
    
    createRealTimeStreamChart(chartId, config) {
        const chart = echarts.init(this.getContainer(chartId));
        let dataBuffer = [];
        const maxPoints = config.maxDataPoints || 50;
        
        // 
        chart.addRealTimeData = (timestamp, value) => {
            dataBuffer.push([new Date(timestamp).toLocaleTimeString(), value]);
            if (dataBuffer.length > maxPoints) dataBuffer.shift();
            
            chart.setOption({
                xAxis: { data: dataBuffer.map(item => item[0]) },
                series: [{ data: dataBuffer.map(item => item[1]) }]
            });
        };
        
        return chart;
    }
}
```xml
**ECharts**

- ****LTTBLargest Triangle Three Buckets
- ****
- ****
- **Canvas**

## 7.2.3 

### 

****

#### 

||  ||  |
|----------|----------|----------|----------|
|****|  ||  |
|****|  ||  |
|****|  ||  |
|****| LRU ||  |

****

****

**1. **

- ****Douglas-Peucker
- ****
- ****
- **Canvas**Canvas

**2. **

- ****
- ****
- ****
- ****

**3. **

- **LTTB**Largest Triangle Three Buckets
- ****
- ****
- ****

```javascript
// 
class TimeSeriesVisualization {
    constructor(container, config) {
        this.container = container;
        this.dataBuffer = new TimeSeriesBuffer(config.bufferSize);
        this.compressionEngine = new DataCompressionEngine();
        this.chart = null;
    }
    
    addRealTimeData(timestamp, value) {
        // 1. 
        if (this.isAnomalous(value)) {
            value = this.correctAnomalousValue(value);
        }
        
        // 2. 
        this.dataBuffer.add(timestamp, value);
        
        // 3. 
        const displayData = this.getOptimizedDisplayData();
        
        // 4. 
        this.updateChartWithAnimation(displayData);
    }
    
    getOptimizedDisplayData() {
        const rawData = this.dataBuffer.getData();
        const zoomLevel = this.getCurrentZoomLevel();
        
        // 
        if (zoomLevel > 1000) {
            return this.compressionEngine.lttbDownsample(rawData, 1000);
        } else if (zoomLevel > 100) {
            return this.compressionEngine.douglasPeucker(rawData, 0.1);
        }
        return rawData;
    }
}

// 
class DataCompressionEngine {
    // LTTB
    lttbDownsample(data, targetPoints) {
        if (data.length <= targetPoints) return data;
        
        const sampled = [data[0]]; // 
        const bucketSize = (data.length - 2) / (targetPoints - 2);
        
        for (let i = 0; i < targetPoints - 2; i++) {
            // bucket
            const maxAreaPoint = this.findMaxTrianglePoint(
                sampled[sampled.length - 1],
                this.getBucketData(data, i, bucketSize),
                this.getNextBucketAverage(data, i + 1, bucketSize)
            );
            sampled.push(maxAreaPoint);
        }
        
        sampled.push(data[data.length - 1]); // 
        return sampled;
    }
    
    // Douglas-Peucker
    douglasPeucker(points, epsilon) {
        if (points.length <= 2) return points;
        
        let maxDistance = 0;
        let splitIndex = 0;
        
        // 
        for (let i = 1; i < points.length - 1; i++) {
            const distance = this.perpendicularDistance(
                points[i], points[0], points[points.length - 1]
            );
            if (distance > maxDistance) {
                maxDistance = distance;
                splitIndex = i;
            }
        }
        
        // 
        if (maxDistance > epsilon) {
            const left = this.douglasPeucker(points.slice(0, splitIndex + 1), epsilon);
            const right = this.douglasPeucker(points.slice(splitIndex), epsilon);
            return [...left.slice(0, -1), ...right];
        }
        
        return [points[0], points[points.length - 1]];
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****Canvas
\item
  ****Worker
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{7.2.4}\label{section-29}

\subsubsection{}\label{section-30}

****Web

\paragraph{}\label{section-31}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{1. }

\begin{itemize}
\item
  ****breakpoint
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\textbf{2. } PC - **** - **** - **** - ****

\textbf{3. }

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{verbatim}
// 
class ResponsiveChartAdapter {
    constructor() {
        this.breakpoints = {
            mobile: 768,
            tablet: 1024,
            desktop: 1200
        };
        this.currentDevice = this.detectDevice();
    }
    
    applyResponsiveConfig(chart, baseConfig) {
        const deviceConfig = this.getDeviceSpecificConfig();
        const mergedConfig = this.deepMerge(baseConfig, deviceConfig);
        
        // 
        if (chart.setOption) {
            chart.setOption(mergedConfig); // ECharts
        } else if (chart.update) {
            Object.assign(chart.options, mergedConfig); // Chart.js
            chart.update();
        }
        
        return mergedConfig;
    }
    
    getDeviceSpecificConfig() {
        const configs = {
            mobile: {
                title: { textStyle: { fontSize: 14 } },
                legend: { show: false }, // 
                grid: { left: '10%', right: '10%' },
                tooltip: { position: 'top' },
                toolbox: { show: false } // 
            },
            tablet: {
                title: { textStyle: { fontSize: 16 } },
                grid: { left: '8%', right: '8%' },
                toolbox: { show: true, iconStyle: { borderWidth: 1 } }
            },
            desktop: {
                title: { textStyle: { fontSize: 18 } },
                toolbox: {
                    show: true,
                    feature: {
                        dataZoom: { show: true },
                        saveAsImage: { show: true }
                    }
                }
            }
        };
        
        return configs[this.currentDevice] || configs.desktop;
    }
    
    // 
    createMobileInteractions(chartInstance) {
        const container = this.getChartContainer(chartInstance);
        if (!container) return;
        
        let touchState = { startX: 0, startY: 0, startTime: 0 };
        
        container.addEventListener('touchstart', (e) => {
            e.preventDefault();
            const touch = e.touches[0];
            touchState = {
                startX: touch.clientX,
                startY: touch.clientY,
                startTime: Date.now()
            };
            
            // 
            setTimeout(() => {
                if (Date.now() - touchState.startTime >= 500) {
                    this.handleLongPress(chartInstance, touchState.startX, touchState.startY);
                }
            }, 500);
        });
        
        container.addEventListener('touchend', (e) => {
            e.preventDefault();
            const duration = Date.now() - touchState.startTime;
            
            if (duration < 500) {
                // 
                this.handleTap(chartInstance, touchState.startX, touchState.startY);
            }
        });
    }
}

// 
class MobileOptimizedChartFactory {
    createMobileChart(container, data, options = {}) {
        const mobileConfig = {
            responsive: true,
            maintainAspectRatio: false,
            elements: {
                point: {
                    radius: 3,
                    hitRadius: 10 // 
                }
            },
            plugins: {
                legend: { display: false }, // 
                tooltip: {
                    enabled: true,
                    mode: 'nearest',
                    intersect: false
                }
            },
            scales: {
                x: {
                    ticks: {
                        maxTicksLimit: 5, // 
                        font: { size: 11 }
                    }
                },
                y: {
                    ticks: {
                        font: { size: 11 }
                    }
                }
            }
        };
        
        const chart = new Chart(container, {
            type: options.type || 'line',
            data: data,
            options: mobileConfig
        });
        
        return chart;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****44px×44px
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{}\label{section-32}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Chart.js}2D3D
\item
  \textbf{ECharts}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{7.3}

\section{7.3}\label{section-33}

\subsection{}\label{section-34}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****LOD
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsection{7.3.1}\label{section-35}

\subsubsection{}\label{section-36}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{longtable}[]{@{}lllll@{}}
\toprule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{WGS84} & GPS & & & \\
**** & & & & \\
**** & GIS & & & \\
**** & & & & \\
**** & WebGL3D & & & \\
\end{longtable}

\subsubsection{}\label{section-37}

\begin{verbatim}
// 
class MonitoringPointPositionManager {
    geoToScenePosition(geoPosition) {
        // 1. 
        const projectedCoords = this.transformToProjection(
            geoPosition.longitude, geoPosition.latitude
        );
        
        // 2. 
        const sceneX = projectedCoords.x - this.sceneOrigin.x;
        const sceneY = projectedCoords.y - this.sceneOrigin.y;
        
        // 3. 
        const sceneZ = geoPosition.altitude - this.sceneOrigin.z;
        
        return new THREE.Vector3(sceneX, sceneZ, -sceneY);
    }
}
```xml
****

****

**WGS84**
WGS84
- ****
- ****
- ****

**UTM**
(UTM)
- ****606
- ****
- ****UTM0.9996

****

- ****
- ****
- ****PromisesetTimeout

****

- ****WGS84
- ****JavaScript
- ****

## 7.3.2 

### 

****

```javascript
// 
class DeviceStatusVisualizer {
    constructor() {
        // 
        this.statusColors = {
            NORMAL: { primary: '#4CAF50', glow: '#A5D6A7' },    // 
            WARNING: { primary: '#FF9800', glow: '#FFCC02' },   //   
            ALERT: { primary: '#F44336', glow: '#FF8A80' },     // 
            OFFLINE: { primary: '#9E9E9E', glow: '#E0E0E0' }    // 
        };
    }
    
    createDeviceVisual(deviceInfo) {
        const deviceGroup = new THREE.Group();
        
        // 1. 
        const mainBody = this.createDeviceBody(deviceInfo.type, deviceInfo.status);
        deviceGroup.add(mainBody);
        
        // 2. 
        const statusIndicator = this.createStatusIndicator(deviceInfo.status);
        statusIndicator.position.set(0, 2, 0);
        deviceGroup.add(statusIndicator);
        
        return deviceGroup;
    }
    
    createStatusIndicator(status) {
        const statusColor = this.statusColors[status];
        const indicator = new THREE.Group();
        
        // 
        const ring = this.createStatusRing(statusColor.primary);
        indicator.add(ring);
        
        // 
        const glow = this.createGlowEffect(statusColor.glow);
        indicator.add(glow);
        
        // 
        if (status === 'WARNING' || status === 'ALERT') {
            this.addBreathingAnimation(indicator);
        }
        
        return indicator;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\tightlist
\item
  ****550nm
\item
  ****590nm
\item
  ****700nm
\item
  ****``\,''
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****sRGB
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****12-20
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****scale
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****+
\item
  ****+
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{PBR} (PBR) - \textbf{Phong} - ****emissive - ****alpha -
****DoubleSide

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{7.3.3 LOD}\label{lod}

\subsubsection{}\label{section-38}

\textbf{LOD}

LODLevel of Detail

\begin{verbatim}
// LOD
class MonitoringPointLODRenderer {
    constructor(config) {
        this.camera = config.camera;
        // LOD
        this.lodThresholds = {
            DETAILED: 100,    // <100
            MEDIUM: 500,      // 100-500
            SIMPLE: 2000,     // 500-2000
            BILLBOARD: 5000   // 2000-5000
        };
        this.activeDevices = new Map(); // 
    }
    
    updateLOD(camera) {
        this.activeDevices.forEach((deviceGroup, deviceId) => {
            const distance = camera.position.distanceTo(deviceGroup.position);
            
            // 
            if (!this.isInView(deviceGroup.position, camera)) {
                this.hideDevice(deviceGroup);
                return;
            }
            
            // LOD
            const requiredLOD = this.calculateLODLevel(distance);
            if (deviceGroup.userData.currentLOD !== requiredLOD) {
                this.switchLODModel(deviceGroup, requiredLOD);
            }
        });
    }
    
    calculateLODLevel(distance) {
        if (distance < this.lodThresholds.DETAILED) return 'DETAILED';
        if (distance < this.lodThresholds.MEDIUM) return 'MEDIUM';
        if (distance < this.lodThresholds.SIMPLE) return 'SIMPLE';
        if (distance < this.lodThresholds.BILLBOARD) return 'BILLBOARD';
        return 'HIDDEN';
    }
    
    // LOD
    adaptiveLODThresholds(currentFPS) {
        const performanceRatio = currentFPS / 60; // 60FPS
        
        if (performanceRatio < 0.8) {
            // LOD
            Object.keys(this.lodThresholds).forEach(level => {
                this.lodThresholds[level] *= 0.9;
            });
        }
    }
}
```xml
**LOD**

LODLevel of Detail

**LOD**

****
LOD
- ****α = 2 * arctan(object_size / (2 * distance))
- ****10.017°
- ****Detail_Level = k / distance^nkn1-2

**LOD**
LOD

**DETAILED**
- ****0-100
- ****
- ****300-1000
- ****

**MEDIUM**
- ****100-500
- ****
- ****100-300
- ****Lambert

**SIMPLE**
- ****500-2000  
- ****
- ****50
- ****

**BILLBOARD**
- ****2000-5000
- ****Sprite
- ****Canvas
- ****4

****

****

- ****z = near_plane
- ****z = far_plane  
- ****FOV
- ****FOV

****
FrustumcontainsPoint
\end{verbatim}

for each plane in frustum\_planes: if dot(point, plane.normal) +
plane.constant \textgreater{} 0: return false // return true //

\begin{verbatim}
**LOD**

****
LOD
- ****performance.now()
- **GPU**drawcallGPU
- ****

****
\end{verbatim}

adjustment\_factor = current\_fps / target\_fps if adjustment\_factor
\textless{} 0.8: // LOD threshold \emph{= 0.9 elif adjustment\_factor
\textgreater{} 1.2: // LOD\\
threshold }= 1.1

\begin{verbatim}
**LOD**

****
LOD
- ****alpha
- ****
- ****

****

****
LOD
- ****
- ****
- ****

**Atlas**
GPU
- **UV**Atlas
- **Mipmap**Atlas
- ****2GPU

LOD60fps

## 7.3.4 

### 

****

```javascript
// 
class MonitoringPointClusterManager {
    constructor(config) {
        this.clusterRadius = config.clusterRadius || 50; // 
        this.clusters = new Map();
        this.points = new Map();
        this.zoomLevel = 12; // 
    }
    
    updateClusters(camera, zoomLevel) {
        this.zoomLevel = zoomLevel;
        const visiblePoints = this.getVisiblePoints(camera);
        
        // 
        if (zoomLevel >= 15) {
            this.showIndividualPoints(visiblePoints); // 
        } else if (zoomLevel >= 12) {
            this.clusterRadius = 30; // 
            this.performClustering(visiblePoints);
        } else {
            this.clusterRadius = 80; // 
            this.performClustering(visiblePoints);
        }
    }
    
    performClustering(points) {
        // 
        const gridSize = this.clusterRadius * Math.pow(2, 15 - this.zoomLevel);
        const grid = new Map();
        
        points.forEach(point => {
            const gridX = Math.floor(point.position.x / gridSize);
            const gridY = Math.floor(point.position.y / gridSize);
            const key = `${gridX}_${gridY}`;
            
            if (!grid.has(key)) grid.set(key, []);
            grid.get(key).push(point);
        });
        
        // 
        grid.forEach(gridPoints => {
            if (gridPoints.length > 1) {
                const clusterVisual = this.createClusterVisual(gridPoints);
                this.scene.add(clusterVisual);
            }
        });
    }
    
    createClusterVisual(points) {
        const group = new THREE.Group();
        const pointCount = points.length;
        
        // 
        const radius = Math.min(2 + Math.sqrt(pointCount) * 0.5, 8);
        const circle = new THREE.Mesh(
            new THREE.CircleGeometry(radius, 32),
            new THREE.MeshBasicMaterial({
                color: this.getDominantStatusColor(points),
                transparent: true, opacity: 0.8
            })
        );
        group.add(circle);
        
        // 
        const countLabel = this.createCountLabel(pointCount);
        group.add(countLabel);
        
        return group;
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****gridSize = baseRadius × 2\^{}(maxZoom - currentZoom)
\item
  ****(gridX, gridY)O(1)
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\tightlist
\item
  \textbf{7±2}
\item
  ****30-50
\item
  ****2-4
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \textbf{+}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\tightlist
\item
  ****O(1) -
\item
  ****O(1) -
\item
  ****O(n) - n
\item
  ****O(k) - k
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****Stevens
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****r = base\_r + sqrt(count) × scale\_factor
\item
  ****alpha
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****5\%
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****300ms
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****100ms
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  ****Bezier
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \textbf{Atlas}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****GPU
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{}\label{section-39}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  ****UTM
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \textbf{LOD}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\emph{7.4}

\section{7.4}\label{section-40}

\subsection{}\label{section-41}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\subsection{7.4.1}\label{section-42}

\subsubsection{}\label{section-43}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{verbatim}
// 
class RaycastPickingManager {
    constructor(camera, scene, renderer) {
        this.camera = camera;
        this.raycaster = new THREE.Raycaster();
        this.mouse = new THREE.Vector2();
        // 
        this.boundingBoxCache = new Map();
        this.lastPickTime = 0;
        this.pickingInterval = 16; // 60fps
    }
    
    performRaycast(screenX, screenY, domElement) {
        // 
        const now = performance.now();
        if (now - this.lastPickTime < this.pickingInterval) {
            return this.lastPickResult || [];
        }
        
        // NDC
        const rect = domElement.getBoundingClientRect();
        this.mouse.x = ((screenX - rect.left) / rect.width) * 2 - 1;
        this.mouse.y = -((screenY - rect.top) / rect.height) * 2 + 1;
        
        // 
        this.raycaster.setFromCamera(this.mouse, this.camera);
        const intersects = this.raycaster.intersectObjects(scene.children, true);
        
        this.lastPickTime = now;
        return intersects;
    }
    
    // Möller-Trumbore-
    rayTriangleIntersection(ray, vertices, faceIndex) {
        // 
        const v0 = this.getVertexFromArray(vertices, faceIndex * 3);
        const v1 = this.getVertexFromArray(vertices, faceIndex * 3 + 1);
        const v2 = this.getVertexFromArray(vertices, faceIndex * 3 + 2);
        
        // 
        const edge1 = v1.sub(v0);
        const edge2 = v2.sub(v0);
        const h = ray.direction.cross(edge2);
        
        // 
        const a = edge1.dot(h);
        if (Math.abs(a) < Number.EPSILON) return null;
        
        // 
        const f = 1.0 / a;
        const s = ray.origin.sub(v0);
        const u = f * s.dot(h);
        
        if (u < 0.0 || u > 1.0) return null;
        
        const q = s.cross(edge1);
        const v = f * ray.direction.dot(q);
        const t = f * edge2.dot(q);
        
        // 
        if (v >= 0.0 && u + v <= 1.0 && t > Number.EPSILON) {
            return {
                distance: t,
                point: ray.origin.add(ray.direction.multiplyScalar(t)),
                uv: new THREE.Vector2(u, v)
            };
        }
        return null;
    }
}
```xml
****

****

****
**R(t) = O + t·D**
- **O**
- **D**
- **t**t≥0

**NDC**

- ****
- **NDC**[-1,1]
- ****
- ****

`NDC_x = (screen_x / width) * 2 - 1``NDC_y = -(screen_y / height) * 2 + 1`

**Möller-Trumbore**

****
Möller-Trumbore-
- ****P P = (1-u-v)·V0 + u·V1 + v·V2
- ****u≥0, v≥0, u+v≤1
- ****

****

****

- **(BVH)**
- ****8
- ****O(n)O(log n)n

****

- ****UUID
- ****LRU
- ****

****

- **60fps**
- ****
- ****

****

****
- 1. ****
2. ****
3. ****

****

- **EPSILON**Number.EPSILON
- ****
- ****

**GPU**

**GPU**
GPU
- **Compute Shader**GPU
- ****GPU
- ****

## 7.4.2 

### 

****

```javascript
// 
class MonitoringPointInfoPanel {
    constructor(container) {
        this.container = container;
        this.panels = new Map();
        this.deviceConfigs = {
            WATER_LEVEL: { title: '', icon: '', primaryMetric: 'waterLevel' },
            FLOW_METER: { title: '', icon: '', primaryMetric: 'flow' },
            PRESSURE_SENSOR: { title: '', icon: '', primaryMetric: 'pressure' }
        };
    }
    
    async showPanel(deviceInfo, worldPosition, camera, renderer) {
        const panel = await this.createPanel(deviceInfo);
        const screenPosition = this.worldToScreen(worldPosition, camera, renderer);
        this.positionPanel(panel, screenPosition);
        
        this.container.appendChild(panel);
        this.panels.set(`panel_${deviceInfo.id}`, { element: panel, deviceInfo });
        
        // 
        requestAnimationFrame(() => {
            panel.style.opacity = '1';
            panel.style.transform = 'scale(1) translateY(0)';
        });
    }
    
    async createPanel(deviceInfo) {
        const config = this.deviceConfigs[deviceInfo.type] || {};
        const panel = document.createElement('div');
        
        panel.className = 'monitoring-info-panel';
        panel.style.cssText = this.getPanelStyles();
        
        // 
        panel.innerHTML = `
            <div class="panel-header">${this.createHeaderHTML(deviceInfo, config)}</div>
            <div class="realtime-section">${await this.createRealtimeHTML(deviceInfo, config)}</div>
            <div class="chart-section">${this.createChartHTML()}</div>
        `;
        
        this.initializeChart(panel, deviceInfo, config);
        return panel;
    }
    
    worldToScreen(worldPosition, camera, renderer) {
        const vector = worldPosition.clone();
        vector.project(camera);
        const rect = renderer.getBoundingClientRect();
        
        return {
            x: (vector.x * 0.5 + 0.5) * rect.width + rect.left,
            y: (-vector.y * 0.5 + 0.5) * rect.height + rect.top
        };
    }
}
```xml
****

****

**1. **

- ****
- **7±2**
- ****

**2. **

- ****`left + panelWidth ≤ screenWidth - margin`
- ****
- ****

**3. **

- ****DOM
- ****`cloneNode(true)`300%
- ****DOM

****

**4. **

- **-**()()()
- **-**
- **-**

**5. **

- ****16247
- ****50
- ****tension=0.4

****

**6. DOM**

DOM
- ****DocumentFragmentDOM
- ****CSS
- ****

**7. **

- ****10
- ****Chart.js
- ****120API

****

**8. **

`cubic-bezier(0.25, 0.8, 0.25, 1)`
- ****-
- ****
- ****300ms""

**9. **

- ****
- ****1Hz
- ****2Hz

## 7.4.3 

### 

****

```javascript
// 
class MultiLevelInfoDisplay {
    constructor(scene, camera, renderer) {
        this.scene = scene;
        this.camera = camera;
        this.renderer = renderer;
        
        // 
        this.infoLevels = {
            TOOLTIP: 0,      // 
            SUMMARY: 1,      // 
            DETAILED: 2,     // 
            COMPREHENSIVE: 3  // 
        };
        
        this.currentLevel = this.infoLevels.TOOLTIP;
        this.activeDevice = null;
        this.hoverTimer = null;
        this.dwellTime = 0;
    }
    
    handleDeviceHover(deviceInfo, position, mouseEvent) {
        this.activeDevice = deviceInfo;
        this.currentLevel = this.infoLevels.TOOLTIP;
        this.dwellTime = 0;
        
        // 
        this.showTooltip(deviceInfo, mouseEvent);
        this.startHoverTimer(deviceInfo, position);
    }
    
    handleDeviceClick(deviceInfo, position, mouseEvent) {
        this.activeDevice = deviceInfo;
        
        if (this.currentLevel < this.infoLevels.DETAILED) {
            this.currentLevel = this.infoLevels.DETAILED;
            this.showDetailedInfo(deviceInfo, position);
        } else {
            this.currentLevel = this.infoLevels.COMPREHENSIVE;
            this.showComprehensiveAnalysis(deviceInfo, position);
        }
        
        this.hideLowerLevelInfo();
    }
    
    startHoverTimer(deviceInfo, position) {
        if (this.hoverTimer) clearInterval(this.hoverTimer);
        
        this.hoverTimer = setInterval(() => {
            this.dwellTime += 100;
            
            // 1
            if (this.dwellTime >= 1000 && this.currentLevel === this.infoLevels.TOOLTIP) {
                this.currentLevel = this.infoLevels.SUMMARY;
                this.showSummaryInfo(deviceInfo, position);
            }
            
            // 3
            if (this.dwellTime >= 3000 && this.currentLevel === this.infoLevels.SUMMARY) {
                this.currentLevel = this.infoLevels.DETAILED;
                this.showDetailedInfo(deviceInfo, position);
            }
        }, 100);
    }
    
    async showSummaryInfo(deviceInfo, position) {
        const realtimeData = await this.fetchRealtimeData(deviceInfo.id);
        const summaryPanel = this.createSummaryPanel(deviceInfo, realtimeData);
        
        this.positionSummaryPanel(summaryPanel, position);
        document.body.appendChild(summaryPanel);
        
        // 
        requestAnimationFrame(() => {
            summaryPanel.style.opacity = '1';
            summaryPanel.style.transform = 'scale(1)';
        });
    }
    
    worldToScreen(worldPosition) {
        const vector = worldPosition.clone();
        vector.project(this.camera);
        
        const rect = this.renderer.domElement.getBoundingClientRect();
        const screenX = (vector.x * 0.5 + 0.5) * rect.width + rect.left;
        const screenY = (-vector.y * 0.5 + 0.5) * rect.height + rect.top;
        
        return { x: screenX, y: screenY };
    }
}
```xml
****

****

**1. **

- ****
- ****
- ****

**2. **

- **(0-200ms)**
- **(1-3)**
- **(>3)**

**3. **

- ****
- ****
- ****

****

**4. **
\end{verbatim}

InfoNeed(t) = 1 - e\^{}(-t/τ)

\begin{verbatim}
- t
- τ2
- InfoNeed(t)0-1

13

**5. **

- ****
- ****
- ****
- ****

****

**6. **

- ****
- ****
- ****
- ****

**7. **

- ****100ms
- ****200-500ms
- ****800ms

****

**8. **

- ****
- ****
- ****

**9. DOM**

DOM
- ****
- ****DOM
- ****CSS

****

**10. **

```javascript
adaptiveThreshold = baseThreshold * (1 + userSpeedFactor)
\end{verbatim}

\begin{itemize}
\tightlist
\item
  \begin{itemize}
  \tightlist
  \item
  \end{itemize}
\end{itemize}

\textbf{11. }

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{7.4.4}\label{section-44}

\subsubsection{}\label{section-45}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{verbatim}
// 
class TouchInteractionOptimizer {
    constructor(renderer, scene, camera) {
        this.renderer = renderer;
        this.scene = scene;
        this.camera = camera;
        this.domElement = renderer.domElement;
        
        // 
        this.touchState = {
            active: false,
            touches: new Map(),
            lastTap: { time: 0, position: null },
            gestureStart: null,
            isPinching: false,
            isRotating: false
        };
        
        // 
        this.touchConfig = {
            tapThreshold: 10,        // 
            doubleTapInterval: 300,  // 
            longPressDelay: 500,     // 
            pinchThreshold: 10,      // 
            hapticFeedback: true     // 
        };
        
        this.gestureRecognizer = new TouchGestureRecognizer(this.touchConfig);
        this.isMobile = this.detectMobileDevice();
        this.setupTouchEvents();
    }
    
    detectMobileDevice() {
        return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(
            navigator.userAgent
        ) || (navigator.maxTouchPoints && navigator.maxTouchPoints > 2);
    }
    
    setupTouchEvents() {
        // 
        this.domElement.style.touchAction = 'none';
        
        // 
        this.domElement.addEventListener('touchstart', this.handleTouchStart.bind(this), { passive: false });
        this.domElement.addEventListener('touchmove', this.handleTouchMove.bind(this), { passive: false });
        this.domElement.addEventListener('touchend', this.handleTouchEnd.bind(this), { passive: false });
    }
    
    handleTouchStart(event) {
        event.preventDefault();
        this.touchState.active = true;
        
        // 
        for (let i = 0; i < event.changedTouches.length; i++) {
            const touch = event.changedTouches[i];
            this.touchState.touches.set(touch.identifier, {
                id: touch.identifier,
                startX: touch.clientX,
                startY: touch.clientY,
                currentX: touch.clientX,
                currentY: touch.clientY,
                startTime: Date.now(),
                moved: false
            });
        }
        
        const touchCount = this.touchState.touches.size;
        if (touchCount === 1) {
            this.handleSingleTouchStart(event);
        } else if (touchCount === 2) {
            this.handlePinchStart(event);
        }
    }
    
    handleSingleTouchStart(event) {
        const touch = event.changedTouches[0];
        const touchInfo = this.touchState.touches.get(touch.identifier);
        
        // 
        const currentTime = Date.now();
        const lastTap = this.touchState.lastTap;
        
        if (currentTime - lastTap.time < this.touchConfig.doubleTapInterval && lastTap.position) {
            const distance = Math.sqrt(
                Math.pow(touch.clientX - lastTap.position.x, 2) +
                Math.pow(touch.clientY - lastTap.position.y, 2)
            );
            
            if (distance < this.touchConfig.tapThreshold) {
                this.handleDoubleTap(touch);
                return;
            }
        }
        
        // 
        touchInfo.longPressTimer = setTimeout(() => {
            if (this.touchState.touches.has(touch.identifier) && !touchInfo.moved) {
                this.handleLongPress(touch);
            }
        }, this.touchConfig.longPressDelay);
        
        // 
        this.performTouchPicking(touch);
    }
    
    performTouchPicking(touch) {
        const rect = this.domElement.getBoundingClientRect();
        const x = touch.clientX - rect.left;
        const y = touch.clientY - rect.top;
        
        // 
        const pickingRadius = this.isMobile ? 20 : 10;
        
        const raycaster = new THREE.Raycaster();
        const mouse = new THREE.Vector2();
        
        mouse.x = (x / rect.width) * 2 - 1;
        mouse.y = -(y / rect.height) * 2 + 1;
        
        raycaster.setFromCamera(mouse, this.camera);
        raycaster.params.Points.threshold = pickingRadius;
        
        const intersects = raycaster.intersectObjects(this.scene.children, true);
        
        if (intersects.length > 0) {
            const deviceGroup = this.findDeviceGroup(intersects[0].object);
            if (deviceGroup && deviceGroup.userData.deviceInfo) {
                this.handleDeviceTouch(deviceGroup, touch, intersects[0]);
            }
        }
    }
    
    provideTactileFeedback(type) {
        if (!this.touchConfig.hapticFeedback || !navigator.vibrate) {
            return;
        }
        
        const patterns = {
            'tap': 10,
            'double-tap': [10, 50, 10],
            'long-press': [50, 100, 50],
            'selection': 20
        };
        
        const pattern = patterns[type] || 10;
        navigator.vibrate(pattern);
    }
}
\end{verbatim}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{1. }

\begin{itemize}
\item
  ****8-10mm24-30120dpi
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  ****150-200
\item
  ****30-40
\end{itemize}

\textbf{2. }

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\textbf{3. }

\begin{itemize}
\tightlist
\item
  ****200-300Hz
\item
  ****30-40Hz
\item
  ****20ms
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{4. }

\begin{verbatim}
effectiveRadius = baseRadius + (depth_distance / max_distance) * expansion_factor
\end{verbatim}

\begin{itemize}
\tightlist
\item
  baseRadius20
\item
  depth\_distance
\item
  expansion\_factor10-15
\end{itemize}

\textbf{5. }

\begin{itemize}
\tightlist
\item
  ****\texttt{scale\_ratio\ =\ current\_distance\ /\ initial\_distance}
\item
  ****\texttt{angle\_change\ =\ atan2(v2.y,\ v2.x)\ -\ atan2(v1.y,\ v1.x)}
\item
  ****\texttt{translation\ =\ (center\_current\ -\ center\_initial)}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{6. }

\begin{itemize}
\tightlist
\item
  \textbf{touchmove}120Hz
\item
  ****16.67ms60fps
\item
  ****2
\end{itemize}

\textbf{7. }

\begin{itemize}
\item
  \textbf{Map}IDO(1)
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{8. }

\begin{verbatim}
adaptedSize = baseSize * (devicePixelRatio / standardDPI * scaleFactor)
\end{verbatim}

\begin{itemize}
\tightlist
\item
  iPhone RetinadevicePixelRatio = 2-3
\item
  AndroiddevicePixelRatio = 2.5-4
\item
  DPI96-120
\end{itemize}

\textbf{9. }

\begin{itemize}
\tightlist
\item
  \textbf{iOS}
\item
  \textbf{Android}
\item
  ****SafariChrome
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{10. }

\begin{itemize}
\tightlist
\item
  \textbf{0-50ms}
\item
  \textbf{50-200ms}
\item
  \textbf{200-500ms}
\end{itemize}

\textbf{11. }

\begin{itemize}
\item
  ****100ms
\item
  ****5
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\textbf{12. }

\begin{itemize}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{itemize}

\subsection{}\label{section-46}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\item
  \begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}
\end{enumerate}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

**