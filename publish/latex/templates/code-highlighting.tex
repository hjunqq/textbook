% ========================================
% 代码高亮配置 - 智慧水利平台架构与开发
% 支持多种编程语言的专业代码高亮
% 版本: v1.0
% 更新: 2025年8月7日
% ========================================

% ========================================
% 全局 listings 配置
% ========================================

\lstset{
    backgroundcolor=\color{backcolour},   % 背景色
    commentstyle=\color{codegreen}\itshape, % 注释样式
    keywordstyle=\color{codeblue}\bfseries, % 关键字样式
    numberstyle=\tiny\color{codegray},    % 行号样式
    stringstyle=\color{codered},          % 字符串样式
    basicstyle=\ttfamily\footnotesize,    % 基本字体
    breakatwhitespace=false,              % 只在空白处换行
    breaklines=true,                      % 自动换行
    captionpos=b,                         % 标题位置
    keepspaces=true,                      % 保持空格
    numbers=left,                         % 行号位置
    numbersep=8pt,                        % 行号与代码间距
    showspaces=false,                     % 不显示空格
    showstringspaces=false,               % 不显示字符串中的空格
    showtabs=false,                       % 不显示tab
    tabsize=4,                            % tab大小
    frame=single,                         % 边框样式
    rulecolor=\color{framecolor},         % 边框颜色
    framexleftmargin=8pt,                 % 左边距
    framexrightmargin=8pt,                % 右边距
    framextopmargin=8pt,                  % 上边距
    framexbottommargin=8pt,               % 下边距
    xleftmargin=15pt,                     % 整体左边距
    xrightmargin=15pt,                    % 整体右边距
    escapeinside={\%*}{*\)},              % 转义字符
    extendedchars=true,                   % 支持扩展字符
    inputencoding=utf8,                   % 输入编码
    literate={·}{{\textperiodcentered}}1  % 中文标点支持
        {…}{{\ldots}}1
        {—}{{\textemdash}}1
        {'}{{\textquotesingle}}1
        {'}{{\textquotesingle}}1
        {"}{{\textquotedblleft}}1
        {"}{{\textquotedblright}}1
}

% ========================================
% JavaScript/TypeScript 配置
% ========================================

\lstdefinestyle{javascript}{
    language=JavaScript,
    morekeywords={
        let, const, var, function, return, if, else, for, while,
        do, break, continue, switch, case, default, try, catch,
        finally, throw, new, this, typeof, instanceof, in, of,
        class, extends, super, static, get, set, async, await,
        import, export, from, default, as, yield, delete,
        undefined, null, true, false, NaN, Infinity,
        Vue, computed, ref, reactive, watch, onMounted,
        axios, Promise, Array, Object, JSON, console
    },
    morecomment=[l]{//},
    morecomment=[s]{/*}{*/},
    morestring=[b]",
    morestring=[b]',
    morestring=[b]`,
    showstringspaces=false,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    emphstyle=\color{codepurple}\bfseries,
    emph={document, window, localStorage, sessionStorage, fetch, WebSocket},
    title=\lstname
}

% ========================================
% Java 配置
% ========================================

\lstdefinestyle{java}{
    language=Java,
    morekeywords={
        abstract, assert, boolean, break, byte, case, catch, char,
        class, const, continue, default, do, double, else, enum,
        extends, final, finally, float, for, goto, if, implements,
        import, instanceof, int, interface, long, native, new,
        package, private, protected, public, return, short, static,
        strictfp, super, switch, synchronized, this, throw, throws,
        transient, try, void, volatile, while, true, false, null,
        String, Integer, Double, Boolean, List, Map, Set, ArrayList,
        HashMap, HashSet, Optional, Stream, Collection
    },
    morecomment=[l]{//},
    morecomment=[s]{/*}{*/},
    morestring=[b]",
    morestring=[b]',
    showstringspaces=false,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    emphstyle=\color{codepurple}\bfseries,
    emph={System, out, println, Scanner, Exception, Override, Autowired, Service, Repository, Controller},
    title=\lstname
}

% ========================================
% Python 配置
% ========================================

\lstdefinestyle{python}{
    language=Python,
    morekeywords={
        and, as, assert, break, class, continue, def, del, elif,
        else, except, exec, finally, for, from, global, if,
        import, in, is, lambda, not, or, pass, print, raise,
        return, try, while, with, yield, True, False, None,
        async, await, nonlocal, self, cls, len, str, int, float,
        list, dict, tuple, set, range, enumerate, zip, open,
        file, Exception, ValueError, TypeError, KeyError
    },
    morecomment=[l]{\#},
    morestring=[b]",
    morestring=[b]',
    morestring=[s]{"""}{"""},
    morestring=[s]{'''}{'''},
    showstringspaces=false,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    emphstyle=\color{codepurple}\bfseries,
    emph={pandas, numpy, matplotlib, requests, flask, django, fastapi},
    title=\lstname
}

% ========================================
% SQL 配置
% ========================================

\lstdefinestyle{sql}{
    language=SQL,
    morekeywords={
        SELECT, FROM, WHERE, JOIN, LEFT, RIGHT, INNER, OUTER,
        ON, GROUP, BY, ORDER, HAVING, AS, DISTINCT, UNION,
        INSERT, INTO, VALUES, UPDATE, SET, DELETE, CREATE,
        TABLE, DATABASE, INDEX, VIEW, PROCEDURE, FUNCTION,
        ALTER, DROP, TRUNCATE, GRANT, REVOKE, COMMIT, ROLLBACK,
        TRANSACTION, BEGIN, END, IF, ELSE, WHILE, FOR, DECLARE,
        EXEC, EXECUTE, RETURN, PRIMARY, KEY, FOREIGN, REFERENCES,
        UNIQUE, NOT, NULL, AUTO_INCREMENT, DEFAULT, CHECK,
        VARCHAR, CHAR, INT, INTEGER, BIGINT, SMALLINT, TINYINT,
        DECIMAL, NUMERIC, FLOAT, DOUBLE, DATE, TIME, DATETIME,
        TIMESTAMP, YEAR, BOOLEAN, BINARY, VARBINARY, TEXT, BLOB
    },
    morecomment=[l]{--},
    morecomment=[s]{/*}{*/},
    morestring=[b]",
    morestring=[b]',
    showstringspaces=false,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    basicstyle=\ttfamily\footnotesize,
    title=\lstname
}

% ========================================
% XML/HTML 配置
% ========================================

\lstdefinestyle{xml}{
    language=XML,
    morestring=[b]",
    morestring=[s]{>}{<},
    morecomment=[s]{<?}{?>},
    morecomment=[s]{<!--}{-->},
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    keywordstyle=\color{codeblue}\bfseries,
    title=\lstname
}

% ========================================
% JSON 配置
% ========================================

\lstdefinestyle{json}{
    morestring=[b]",
    stringstyle=\color{codered},
    literate=
        *{0}{{{\color{codepurple}0}}}{1}
         {1}{{{\color{codepurple}1}}}{1}
         {2}{{{\color{codepurple}2}}}{1}
         {3}{{{\color{codepurple}3}}}{1}
         {4}{{{\color{codepurple}4}}}{1}
         {5}{{{\color{codepurple}5}}}{1}
         {6}{{{\color{codepurple}6}}}{1}
         {7}{{{\color{codepurple}7}}}{1}
         {8}{{{\color{codepurple}8}}}{1}
         {9}{{{\color{codepurple}9}}}{1}
         {:}{{{\color{codeblue}{:}}}}{1}
         {,}{{{\color{codeblue}{,}}}}{1}
         {\{}{{{\color{codeblue}{\{}}}}{1}
         {\}}{{{\color{codeblue}{\}}}}}{1}
         {[}{{{\color{codeblue}{[}}}}{1}
         {]}{{{\color{codeblue}{]}}}}{1},
    title=\lstname
}

% ========================================
% YAML 配置
% ========================================

\lstdefinestyle{yaml}{
    keywords={true,false,null,y,n},
    keywordstyle=\color{codeblue}\bfseries,
    basicstyle=\ttfamily\footnotesize,
    sensitive=false,
    comment=[l]{\#},
    morecomment=[s]{/*}{*/},
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    moredelim=[l][\color{codeorange}]{\&},
    moredelim=[l][\color{codepurple}]{*},
    title=\lstname
}

% ========================================
% Bash/Shell 配置
% ========================================

\lstdefinestyle{bash}{
    language=bash,
    morekeywords={
        if, then, else, elif, fi, for, while, do, done, case, esac,
        function, return, in, select, until, break, continue,
        echo, printf, read, cd, ls, pwd, mkdir, rmdir, cp, mv, rm,
        chmod, chown, grep, sed, awk, sort, uniq, wc, head, tail,
        cat, more, less, find, locate, which, whereis, ps, top,
        kill, killall, jobs, bg, fg, nohup, screen, tmux,
        export, source, alias, history, sudo, su, ssh, scp, rsync
    },
    morecomment=[l]{\#},
    morestring=[b]",
    morestring=[b]',
    showstringspaces=false,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    title=\lstname
}

% ========================================
% Docker 配置
% ========================================

\lstdefinestyle{dockerfile}{
    morekeywords={
        FROM, RUN, CMD, LABEL, MAINTAINER, EXPOSE, ENV, ADD, COPY,
        ENTRYPOINT, VOLUME, USER, WORKDIR, ARG, ONBUILD, STOPSIGNAL,
        HEALTHCHECK, SHELL
    },
    morecomment=[l]{\#},
    morestring=[b]",
    morestring=[b]',
    showstringspaces=false,
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    identifierstyle=\color{black},
    title=\lstname
}

% ========================================
% 配置文件样式
% ========================================

\lstdefinestyle{config}{
    basicstyle=\ttfamily\footnotesize,
    morecomment=[l]{\#},
    morecomment=[l]{;},
    commentstyle=\color{codegreen}\itshape,
    stringstyle=\color{codered},
    keywordstyle=\color{codeblue}\bfseries,
    showstringspaces=false,
    title=\lstname
}

% ========================================
% 命令行样式
% ========================================

\lstdefinestyle{terminal}{
    basicstyle=\ttfamily\footnotesize\color{white},
    backgroundcolor=\color{black},
    morecomment=[l]{\#},
    commentstyle=\color{green},
    showstringspaces=false,
    frame=none,
    title=\lstname
}

% ========================================
% 伪代码样式
% ========================================

\lstdefinestyle{pseudocode}{
    mathescape=true,
    morekeywords={
        算法, 输入, 输出, 开始, 结束, 如果, 那么, 否则, 当, 执行,
        重复, 直到, 对于, 每个, 返回, 调用, 函数, 过程
    },
    keywordstyle=\color{codeblue}\bfseries,
    commentstyle=\color{codegreen}\itshape,
    basicstyle=\ttfamily\footnotesize,
    showstringspaces=false,
    frame=single,
    title=\lstname
}
