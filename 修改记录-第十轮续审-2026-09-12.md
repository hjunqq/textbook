# R10-02 修改记录：数据语义、关键清单与题答一致性

日期2026-09-12，起点 `e4e1597`。本包落实《教材审核与修改方案-第十轮续审-2026-09-12.md》，由主审与三个独立子审任务协作完成。源文件为现役LaTeX及companion，网站仍由tex2site生成。

## 已完成修订

1. **时间查询。** 前端把ISO字符串直接比较，跨时区时可能拒绝正确窗口或放过反向窗口；教学接口仅用宽松Date.parse，又会接受无时区和溢出日期。共享time.mjs明确检查带时区格式、日历日期与时间范围，按UTC毫秒比较，请求保留原字符串。后端原Between把终点纳入结果，现用明确的`>=from`、`<to`查询；相邻窗口不重复计入边界观测。
2. **第4章可运行参考。** 登录路径由`/auth/login`改为`/api/auth/login`，请求字段由account改为username。两种Pinia store都增加请求序号，清空旧结果并阻止旧成功、旧失败和旧finally覆盖当前状态；详情组件卸载时使请求失效。把对所有测项直接比较水位阈值的isWarning改成needsAttention，仅对有效、有限、单位为m的指定水位测点作界面提示；测试核对8.1宏中的汛限值，提示不替代服务端预警。新增7个参考模块与书中清单一致，现有路由仍由学生按指导实践补齐。
3. **第7章数据处理。** 重采样按实际事件时间排序取末值；雨量显式检查采样周期与完整性，部分缺测标为suspect，missing携带数值也不参加聚合；拒绝重复时间、混合测点/单位/版本和结果溢出。质量检验保留上游suspect/missing，拒绝非有限值、数值字符串、未知质量码与非法时间。标准化不把null或空字符串转成零，编码映射同时约束测项和单位，未映射设备、错误测项及未知单位明确失败。三个模块补齐导入、导出和独立调用清单，附正常输出与失败例。
4. **水印与历史回放。** 以已见最大事件时间减乱序容忍度推进水印，接收时间用于记录延迟；事件早于此前水印才进入修订队列。历史数据不会因为在今天回放而全部变成迟到。演示明确单流、课堂修订队列的边界，未宣称实现完整流处理引擎。
5. **评分输入。** S6原先只排除NaN，Infinity可被评为红色，-Infinity可被评为正常，继承属性名还会通过质量枚举判断。现要求分数、指标和权重均为有限值，并使用自有属性校验质量码。
6. **刷新凭据。** 第5章允许过期refresh继续轮换，与附录要求和有效期语义冲突。现统一到期即拒绝，新增完整RefreshTokenVerifier与真实jjwt测试，检查签名、签发者、类型、主体、jti和严格未来到期时间。刷新解析与刷新服务使用同一注入时钟。轮换示例改为原子消费旧会话并在同一数据库事务登记新会话，Redis撤销缓存不能代替会话事实；TTL保留实际剩余时长，取消秒数向下取整。独立校验器已实现，持久化会话仓储仍是明确的指导实践，不冒称现有后端新增了刷新端点。另将MockMvc示例中的STATION_NOT_FOUND改为契约规定的ASSET_NOT_FOUND。
7. **题答与概念。** 核对9章110题和110项答案编号，修订7处：第2章第10题答案确实增加题设之外字段；第4章第9题区分204空态与HTTP失败；第6章第8题准确说明WMS可选属性查询和WFS事务能力；第7章第7题由“消除投影变形”改为控制变形，第14题以相同点数预算如实比较LTTB而不保证必然更优；第8章第15题题答都包含Kafka五服务；第9章第4题明确旧模型结果仅供历史参考，当前证据不足时标记未评估。第8章补入预警表中缺少的“未评估”行，修正闸门题自由出流条件的文字错误。

## 来源与替换登记

- L1：查阅`d8beab0`的第4章路由/Pinia概念、第5章身份认证机制与JWT工具、第7章数据质量和时间标准化段落。保留既有概念，不搬回旧认证链式API、硬编码密钥或旧框架写法。
- L2：核对`archive/docs-legacy/chapter05/section05-05.md`认证片段与`chapter07/section07-01.md`的数据质量/时间标准化。旧材料没有本次所需的完整竞态与边界实现，未重新移植整段。
- 实际复用现稿第4章竞态防线、8.1唯一契约、第7章缺测和质量分级及第5章令牌轮换教学目标。新增内容为完整参考模块、明确的调用/失败示例、回归测试与审核证据。
- 原位替换第4章登录、Pinia及使用清单；第5章过期refresh分支、轮换调用与TTL计算；第7章重采样、质量、标准化、水印清单及紧随说明。各教学目标保留，未把内容迁往无法找到的位置。新增2个代码清单（刷新校验器、数据调用示例），未新增编号节、图或独立表；已有预警表补1行。
- 本包没有跨文件迁移或成段删除，迁移账本仍为空；第7章部分解释压缩用于准确陈述代码行为，相关段落净减73汉字，含其余增补后该章仍净增69汉字。

## 篇幅与验证

正文 **238,086 → 238,420（+334）**，距240,000上限余1,580。第4章+123、第5章+111、第7章+69、第8章+31，其余正文不变。各章均高于安全线；代码行数未人为压缩。

| 检查 | 实际结果与范围 |
|---|---|
| 前端全量回归 | **112/112，11个测试文件**；原55项加57项，包含真实Pinia/Vue、ECharts及新数据边界 |
| 后端全量回归 | **22/22，0跳过**；新增JPA时间窗3项、刷新校验14项；原查询先出现3项失败，修复后通过 |
| 清单一致性 | **23组一致，0组漂移**；从10组增至23组，既有11行有理由差异保留，无新增豁免 |
| 教学接口 | **27项契约检查、20项预警—工单闭环检查通过**；新覆盖非法日期、时区等价、左右边界 |
| 生产构建与浏览器 | 六入口构建通过；**12项Edge检查通过，0个pageerror**，覆盖实际画布点击、异步切换、失败恢复和空态 |
| 学习路线 | 49节、245小节；49节有先修说明，0层次号错误，0个显式核心指向拓展引用 |
| 完整编译 | **`check_textbook.py --strict --build`通过，479页**；Error、Overfull、未定义引用、缺字为0，7项软指标均0，70条文献全部引用 |
| 网站生成 | tex2site从LaTeX生成，0转换悬空引用；**MkDocs严格构建通过**，无警告；保留网站原CRLF格式 |
| PDF抽看 | 物理页176、232、309、313、315、397，登录、刷新、数据处理与预警表均在版心内；见`r10-02-pdf.json` |

测试日志与统计保存在`tools/review/r10-02-verification.json`、`r10-02-teaching.json`、`r10-02-browser.json`和`r10-02-after/`。核验依据包括仓库锁定依赖、[RFC 7519有效期语义](https://www.rfc-editor.org/rfc/rfc7519.html#section-4.1.4)、[ECMAScript日期解析](https://tc39.es/ecma262/multipage/numbers-and-dates.html#sec-date.parse)、[Spring Data JPA查询](https://docs.spring.io/spring-data/jpa/reference/jpa/query-methods.html)及方案所列OGC/PROJ文档。

## 实际边界与契约自检

H2仅作为测试依赖验证实际JPA查询条件，不能代替PostgreSQL/PostGIS/TimescaleDB整栈验收。本轮未运行Docker整栈；Maven使用Java21宿主、source/target17，未另在Java17宿主重跑。新增Surefire3.2.5确保JUnit5被实际发现，技术基线Boot3.2.10与jjwt0.11.5保持。

23组清单核验不能外推为所有印刷清单都已运行；110题编号和内容核对也不等于所有实践题均完成真实课堂试教。刷新持久化轮换和S2路由仍按教材要求由学生完成。Vite保留既有大vendor分包，仍有体积提示；未以提高警告阈值消除提示。

没有修改文字门禁、门禁基线、pandoc或build.bat；没有新增外部图片或让授权截图进入网站；没有新增工程常数或接口端点；用户既有未跟踪frontend/dist保留。网站仅从LaTeX生成，未手工改其正文。一个工作包一次提交，不推送或发布远端。课堂试教及出版社版面终审仍需真实外部结果。

首次完整编译发现新增预警表内`evaluable=false`超出版心1.80579pt，加入允许断行位置后重新执行完整编译通过，未改字号或门禁。前端测试/构建初次因沙箱禁止Vite写临时配置而未启动，获得执行权限后重跑成功；这不是测试用例失败。PDF由477页增至479页，未通过删代码压页，出版社最终页数仍未定稿。

交叉复核还发现并修正两项新问题：刷新校验不能混用系统时钟与注入时钟，PZ-07标准化映射不能接受雨量测项。相关代码与本包最终清单、测试结果已经一致。Git差异按既有CRLF口径执行`git -c core.whitespace=cr-at-eol diff --check`；本包不改变原文件格式约定。
