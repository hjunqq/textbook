# 第五轮执行手册（Runbook）

- 全程 **60 步**，每步一个工作包、一次提交。
- 每步的做法都一样：把《Codex提示词-第五轮.md》里对应的那段提示词整段复制，
  **开一个新会话**贴给 Codex，等它汇报，人工抽查，通过了再走下一步。
- 提示词里已经包含自检与提交指令，你不需要额外给它 git 命令。**唯一例外是步骤 0**，
  它是纯 git 操作，完整命令在下面。

> 更正一处：之前说"51 个工作包"，实际清点是 **60 个**（O 批 8 + A 批 43 + S 批 3 + B 批 6）。
> A 批的数量之前少算了，各章的实际包数见下表。

---

## 步骤 0 · 存档与环境自检（先做，只做一次）

第三轮 2026-08-05 的全部改动至今没有提交，`git status` 里 11 个 tex 都是 `M` 状态。
在动任何内容之前必须存档，否则一次 `git checkout .` 就会把第三轮的成果抹掉。

把下面整段贴给 Codex：

```
仓库：E:\2026\教材\智慧水利平台架构与开发

【工作包】步骤 0 · 存档与环境自检

这一步不改任何书稿内容，只做版本管理与环境确认。逐条执行，每条都把输出贴出来。

0.1 看清现状
    git log --oneline -3
    git status --porcelain | head -40

0.2 排除不该进版本库的东西
    51World/ 是厂商授权素材（手册 39 页 + 功能清单 + 5 分钟视频）。授权是"教学参考使用"，
    不等于可以公开分发；本仓库远程是 GitHub，一旦推送就等于公开发布。必须排除。
    LaTeX 中间产物（aux/log/xdv 等，其中 main.xdv 有 5.6 MB）也不该每次提交都变更。

    printf '\n# 厂商授权素材，不得公开分发\n51World/\n\n# LaTeX 中间产物\noutput/*.aux\noutput/*.log\noutput/*.out\noutput/*.toc\noutput/*.bcf\noutput/*.run.xml\noutput/*.fls\noutput/*.fdb_latexmk\noutput/*.xdv\noutput/*.blg\noutput/*.bbl-SAVE-ERROR\noutput/missfont.log\n' >> .gitignore

    git rm --cached -q output/main.aux output/main.log output/main.out output/main.toc \
      output/main.bcf output/main.run.xml output/main.xdv output/main.blg \
      output/main.bbl-SAVE-ERROR output/missfont.log 2>/dev/null || true

0.3 存档第三轮成果（书稿改动单独一个提交）
    git add -A
    git reset -q AGENTS.md tools "审查报告-第四轮-2026-08-06.md" \
      "扩写与修订方案-第五轮-2026-08-06.md" "旧版内容回收清单-第五轮.md" \
      "Codex提示词-第五轮.md" "执行手册-第五轮.md" 2>/dev/null || true
    git commit -m "存档：第三轮修改成果（2026-08-05，未经审查的状态）"

0.4 提交第五轮的执行契约与工具（与书稿分开）
    git add AGENTS.md tools "审查报告-第四轮-2026-08-06.md" \
      "扩写与修订方案-第五轮-2026-08-06.md" "旧版内容回收清单-第五轮.md" \
      "Codex提示词-第五轮.md" "执行手册-第五轮.md"
    git commit -m "第五轮：执行契约、机器门禁、扩写方案与回收清单"
    git tag round5-start

0.5 验证三层素材都能取用
    git show d8beab0:output/chapters/chapter04.tex | wc -c
      —— 应该输出一个远大于当前 chapter04.tex 的字节数
    ls docs/chapters/chapter04/
      —— 应该看到 section04-01.md 到 section04-07.md 共 7 个文件
    python -c "import re;print(len(re.findall(r'[\u4e00-\u9fff]',open('docs/chapters/chapter04/section04-03.md',encoding='utf-8').read())))"
      —— 应该输出 8022 左右

0.6 门禁自检
    python tools/check_textbook.py --report
    预期输出：合计当前 73,728 字、增量 +0、完成度 37%；并列出 18 项 FAIL
    （这些 FAIL 正是 O1–O8 和后续工作包要清掉的，现在报出来是对的，不要去修门禁）。

0.7 汇报
    贴出 git log --oneline -5、git status --porcelain（应为空或只剩未跟踪的 51World/）、
    以及 0.6 的完整输出。

不要改任何 output/chapters/*.tex，不要改 tools/check_textbook.py。做完停下汇报。
```

**步骤 0 的验收**：`git log` 出现两个新提交和 `round5-start` 标签；
`--report` 显示合计 73,728、增量 +0；`git status` 干净。三项都对了再往下走。

> 关于 `git push`：`d8beab0` 一直没有推到远程。要不要推由你决定；如果推，
> 先确认 `.gitignore` 里的 `51World/` 已生效（`git ls-files 51World | wc -l` 应为 0），
> 厂商素材不能上公开仓库。

---

## 每一步的固定流程

1. 从《Codex提示词-第五轮.md》复制对应段落 →**新会话**贴给 Codex。
   A 批的提示词由「A 系列通用开头」+「该工作包段落」两块拼成，两块都要贴。
2. Codex 汇报后，人工抽查七条（提示词文件末尾有完整版，这里是要点）：
   - 提交说明里有没有列出**回收来源**（文件名 + 小节名）。没有来源就说"新写了 4,000 字"的，退回。
   - `grep -n "javax\.\|WebSecurityConfigurerAdapter\|authorizeRequests\|beforeDestroy\|Vue\.use\|new Vue(\|outputEncoding\|readyPromise\|Chart\.js" output/chapters/*.tex`
     —— 只应在"迁移说明"语境里出现，其他位置说明把 2025 年的旧写法搬进来了。
   - `python tools/check_textbook.py --report` 看字数是不是真涨了。
   - 挑一段新代码贴进 IDE 跑一下。
   - 挑一节新文字读一遍，出现"具有重要意义""为…提供了有力支撑"就退回重写。
   - `git diff --stat HEAD~1` 看有没有大段减号。
   - `git diff HEAD~1 -- tools/check_textbook.py` 必须是空的（改门禁 = 绕路，直接退回）。
3. 通过了走下一步；没通过就在**同一个会话**里让它改，不要开新会话重来。

**某一步彻底做坏了怎么办**：每个工作包正好一次提交，所以
`git reset --hard HEAD~1` 就能干净回退到上一步，然后重贴提示词。
想看本轮至今的总体变化：`git diff round5-start HEAD --stat`。

---

## 60 步清单

「提示词段落」列是《Codex提示词-第五轮.md》里的小标题，照着找即可。

### 批次 O · 阻断项（步骤 1–8）

| 步 | 工作包 | 提示词段落 | 提交信息首行 | 该步重点抽查 |
|---:|---|---|---|---|
| 1 | O1 五维模型学术归属 | `## O1` | O1 第6章五维模型学术归属修正 | bib 新条目被引用；正文不再有"本教材把…列为第五维" |
| 2 | O2 IFC 4.3 段落重写 | `## O2` | O2 第6章IFC4.3段落重写 | 映射表里的实体名是否标了 TODO-核对 还是编的 |
| 3 | O3 质量码与预警级 | `## O3` | O3 第8章质量码与预警级代码修复 | 枚举有没有 NONE；inspect 对缺测写没写 missing |
| 4 | O4 51WIM 致谢商标 | `## O4` | O4 51WIM致谢商标与权威依据补充 | 四个权威文献键是否真的引进 8.5 节 |
| 5 | O5 清除改稿批注 | `## O5` | O5 清除全书改稿批注21处 | **各文件字数不得下降**（本包最容易翻车） |
| 6 | O6 补 \ref 与浮动体 | `## O6` | O6 全书补ref与浮动体控制 | 引导语是不是带了一句说明，还是只写"如图X所示" |
| 7 | O7 前言承诺修正 | `## O7` | O7 前言承诺修正与配套资源节 | preface 是否达到 2,500 字 |
| 8 | O8 事实核验点标注 | `## O8` | O8 事实核验点标注（10处） | **正文一个字都不许改**，字数须与基线完全相等 |

**里程碑 M1**：`python tools/check_textbook.py --build` 全绿（除字数进度外），
`git tag M1-阻断清零`。

### 批次 A · 第4章前端（步骤 9–16）

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 9 | A4-1 HTML 语义化与文档结构 | 通用开头 + `A4-1` | A4-1 HTML语义化与文档结构（+N 字） |
| 10 | A4-2 CSS 基础 | 通用开头 + `A4-2` | A4-2 CSS基础（+N 字） |
| 11 | A4-3 CSS 布局与响应式 | 通用开头 + `A4-3` | A4-3 CSS布局与响应式（+N 字） |
| 12 | A4-4 JavaScript 语言与 DOM | 通用开头 + `A4-4` | A4-4 JavaScript语言与DOM（+N 字） |
| 13 | A4-5 异步与网络 | 通用开头 + `A4-5` | A4-5 异步与网络（+N 字） |
| 14 | A4-6 Vue 3 组合式 API | 通用开头 + `A4-6` | A4-6 Vue3组合式API（+N 字） |
| 15 | A4-7 Router 与 Pinia 实战 | 通用开头 + `A4-7` | A4-7 Router与Pinia实战（+N 字） |
| 16 | A4-8 工程化与构建 | 通用开头 + `A4-8` | A4-8 工程化与构建（+N 字） |

第 14 步特别注意：手稿是 Vue 2 选项式 API，回收时必须整体改写为 Vue 3.4 组合式。
第 16 步做完 chapter04 应达 26,000 字、≥26 个代码清单、≥5 图。

### 批次 A · 第5章后端（步骤 17–25）

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 17 | A5-1 后端分层与 REST | 通用开头 + `A5-1` | A5-1 后端分层与REST（+N 字） |
| 18 | A5-2 Spring Boot 工程骨架 | 通用开头 + `A5-2` | A5-2 SpringBoot工程骨架（+N 字） |
| 19 | A5-3 读写接口与参数校验 | 通用开头 + `A5-3` | A5-3 读写接口与参数校验（+N 字） |
| 20 | A5-4 JPA 与数据访问 | 通用开头 + `A5-4` | A5-4 JPA与数据访问（+N 字） |
| 21 | A5-5 事务 | 通用开头 + `A5-5` | A5-5 事务（+N 字） |
| 22 | A5-6 安全与 JWT | 通用开头 + `A5-6` | A5-6 安全与JWT（+N 字） |
| 23 | A5-7 消息队列 | 通用开头 + `A5-7` | A5-7 消息队列（+N 字） |
| 24 | A5-8 性能与可运维 | 通用开头 + `A5-8` | A5-8 性能与可运维（+N 字） |
| 25 | A5-9 测试 | 通用开头 + `A5-9` | A5-9 测试（+N 字） |

第 17、18、22 步是旧代码风险最高的三步：手稿里满是 Python/Flask 平行实现
（不回收）、Spring Boot 2 的 `javax.*`、`WebSecurityConfigurerAdapter`、jjwt 0.9。
这三步做完各 grep 一遍。

**里程碑 M2**：两个最大缺口补齐。`--build` 全绿，`git tag M2-前后端补齐`。

### 批次 A · 第6章三维场景（步骤 26–30）

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 26 | A6-1 WebGL 与 Three.js 基础 | 通用开头 + `A6-1` | A6-1 WebGL与ThreeJS基础补全（+N 字） |
| 27 | A6-2 坐标系与投影补全 | 通用开头 + `A6-2` | A6-2 坐标系与投影补全（+N 字） |
| 28 | A6-3 OGC 服务补全 | 通用开头 + `A6-3` | A6-3 OGC服务补全（+N 字） |
| 29 | A6-4 倾斜摄影与 BIM 补全 | 通用开头 + `A6-4` | A6-4 倾斜摄影与BIM补全（+N 字） |
| 30 | A6-5 数字孪生节整理 | 通用开头 + `A6-5` | A6-5 数字孪生节整理与交叉引用（+N 字） |

第 26 步的素材 `section06-02.md` 有 36,255 字，是全书最大的单个文件，
但里面的 Three.js 是旧版；第 28 步注意 Cesium `readyPromise` → `fromUrl + await`。

### 批次 A · 第7章数据展示（步骤 31–35）

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 31 | A7-1 可视化设计基础 | 通用开头 + `A7-1` | A7-1 可视化设计基础（+N 字） |
| 32 | A7-2 色彩与可读性 | 通用开头 + `A7-2` | A7-2 色彩与可读性（+N 字） |
| 33 | A7-3 时间序列处理 | 通用开头 + `A7-3` | A7-3 时间序列处理（+N 字） |
| 34 | A7-4 ECharts 工程实践 | 通用开头 + `A7-4` | A7-4 ECharts工程实践（+N 字） |
| 35 | A7-5 LTTB 与三维交互补强 | 通用开头 + `A7-5` | A7-5 LTTB与三维交互补强（+N 字） |

第 34 步注意手稿里有 Chart.js，基线已统一为 Apache ECharts，那一节不回收。
第 34 步是本章的分水岭（图表与三维联动现在整节零代码），抽查要重。

**里程碑 M3**：技术篇完整。`--build` 全绿，`git tag M3-技术篇完整`。

### 批次 A · 第8章综合案例（步骤 36–40）＋ 数据集

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 36 | A8-1 数据库设计与 DDL | 通用开头 + `A8-1` | A8-1 数据库设计与DDL（+N 字） |
| 37 | **S3 示例数据集** | `## S3` | S3 示例数据集 |
| 38 | A8-2 后端实现切片 | 通用开头 + `A8-2` | A8-2 后端实现切片（+N 字） |
| 39 | A8-3 前端实现切片 | 通用开头 + `A8-3` | A8-3 前端实现切片（+N 字） |
| 40 | A8-4 部署与运维 | 通用开头 + `A8-4` | A8-4 部署与运维（+N 字） |
| 41 | **S2 配套代码仓库** | `## S2` | S2 配套代码仓库 |
| 42 | A8-5 案例参数补全与公式修正 | 通用开头 + `A8-5` | A8-5 案例参数补全与公式修正（+N 字） |

S3 必须排在 A8-1 之后（DDL 定死字段才能造数据），S2 必须排在 A8-4 之后
（compose 文件先有）。S2 那一步会反向核对"教材代码 = 工程代码"，
**发现不一致时以工程为准改教材**，这一步的抽查最花时间，留足。

### 批次 A · 其余各章（步骤 43–52）

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 43 | A2-1 软件过程模型补全 | 通用开头 + `A2-1` | A2-1 软件过程模型补全（+N 字） |
| 44 | A2-2 需求获取与分析 | 通用开头 + `A2-2` | A2-2 需求获取与分析（+N 字） |
| 45 | A2-3 结构化与面向对象分析 | 通用开头 + `A2-3` | A2-3 结构化与面向对象分析（+N 字） |
| 46 | A2-4 需求规格与管理 | 通用开头 + `A2-4` | A2-4 需求规格与管理（+N 字） |
| 47 | A1-1 智慧水利发展与政策 | 通用开头 + `A1-1` | A1-1 智慧水利发展与政策（+N 字） |
| 48 | A1-2 平台形态与典型系统 | 通用开头 + `A1-2` | A1-2 平台形态与典型系统（+N 字） |
| 49 | A1-3 架构原则展开与生命周期 | 通用开头 + `A1-3` | A1-3 架构原则展开与生命周期（+N 字） |
| 50 | A3-1 UML 规范补全与三图重绘 | 通用开头 + `A3-1` | A3-1 UML规范补全与三图重绘（+N 字） |
| 51 | A3-2 分类维度重写与口径统一 | 通用开头 + `A3-2` | A3-2 分类维度重写与全章口径统一（+N 字） |
| 52 | A9-1 第9章补齐章末要件 | 通用开头 + `A9-1` | A9-1 第9章补齐章末要件与重写两节（+N 字） |

第 45 步的决策表完备性、第 50 步的三张 UML 图重绘，都要**实际编译后目视核对**，
源码上看不出问题。第 51 步条目最多（15 条），做完逐条核对。

**里程碑 M4**：正文应达 200,000 字。`--report` 看合计，`git tag M4-正文达标`。

### 批次 B · 体例与终验（步骤 53–58）＋ 收尾（59–60）

| 步 | 工作包 | 提示词段落 | 提交信息首行 |
|---:|---|---|---|
| 53 | B1 术语与体例统一 | `【B1 · 术语与体例统一】` | B1 术语与体例统一 |
| 54 | B2 语言去规范腔 | `【B2 · 语言去规范腔】` | B2 语言去规范腔 |
| 55 | B3 第8章图表删并 | `【B3 · 第8章图表删并】` | B3 第8章图表删并 |
| 56 | B4 习题体系终检 | `【B4 · 习题体系终检】` | B4 习题体系终检 |
| 57 | **S1 附录A 参考答案** | `## S1` | S1 附录A习题参考答案 |
| 58 | A0 前言扩写收尾 | 通用开头 + `A0` | A0 前言扩写收尾（+N 字） |
| 59 | B5 交叉引用与前后指终检 | `【B5 · 全书交叉引用与前后指终检】` | B5 全书交叉引用与前后指终检 |
| 60 | B6 终验 | `【B6 · 终验】` | B6 终验 |

S1 必须排在 B4 之后（题号定稿了答案才不会错位）；A0 必须排在 S1/S2/S3 之后
（配套资源节要回填真实路径与版本号）。

**里程碑 M5**：`git tag M5-可付印`，并输出终验报告。

---

## 进度速查

任何时候想知道走到哪了：

```bash
python tools/check_textbook.py --report      # 各章字数与完成度
git log --oneline round5-start..HEAD | wc -l # 已完成的工作包数
git diff round5-start HEAD --stat | tail -3  # 本轮总体改动量
```

正常情况下完成度应该这样走：M1 后仍是 37%（O 批不扩写，只清障，字数微增）；
M2 后约 60%；M3 后约 78%；M4 达到 100%。

如果某个批次结束时完成度明显落后于上面的数字，**先停下来查是不是门禁被绕过了**，
不要继续往下推。
